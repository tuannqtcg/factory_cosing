# SESSION 2026-07-05 — Nạp tri thức dự án + dịch Excel model sang thuật toán/fixture
Pha hiện tại: 0 (trước Prototype — chờ frontend Pha 1 từ user) | Agent: Claude Code

## Đã quyết (nếu là quyết định kiến trúc → tạo ADR, ghi số ADR ở đây)
- Không commit file `BlazeMaster_Model_v3_3.xlsx` gốc vào repo (dữ liệu chi phí/giá
  nhạy cảm kinh doanh) — quyết định của user, không phải ADR kiến trúc nhưng ghi ở
  đây để phiên sau biết lý do `.gitignore` chặn `*.xlsx`.

## Đã làm
- Nạp bộ tri thức dự án từ costingappkit (AGENTS.md, CLAUDE.md, PROJECT_SPEC.md,
  GLOSSARY.md, ADR-001/002/003, 4 skill) → commit vào repo.
- Đọc toàn bộ 6 sheet của `BlazeMaster_Model_v3_3.xlsx` (Assumptions, Ống, Phụ
  kiện, Dashboard, Plan_SX, PriceList), đối chiếu từng công thức.
- Trích xuất số liệu vàng (giá trị đã tính, không gõ tay) sang
  `tests/fixtures/{assumptions,pipe,fitting,dashboard,price-list}.json`.
- Viết `docs/BUSINESS_MODEL.md` — dịch toàn bộ công thức Excel sang thuật toán có
  tên biến camelCase, khớp với mô hình schema dự kiến ở skill `schema-design`.

## Còn treo / việc phiên sau
- Chờ user gửi frontend/prototype (Pha 1) để bắt đầu dựng artifact mock UI theo
  skill `prototype`, dùng đúng số trong `tests/fixtures/`.
- `docs/BUSINESS_MODEL.md` bậc 2 & 4 thang giá (hòa vốn tiền mặt / hòa vốn toàn DN)
  đã viết công thức chính xác theo Excel — cần double-check lại khi viết engine
  thật vì công thức tham chiếu chéo 2 dòng sản phẩm, dễ sai khi tách hàm.
- Plan_SX (kế hoạch SX) chưa có "số vàng" để parity test (input mẫu = 0) — chỉ có
  quy tắc/công thức ghi ở BUSINESS_MODEL §6; cần bộ input mẫu > 0 khi viết test
  thật ở Pha 4.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 2 cùng ngày — Cập nhật kit v1.1 + Excel v3.4 (cơ chế khóa bảng giá)
Pha hiện tại: 0 (vẫn chờ frontend Pha 1) | Agent: Claude Code

## Đã quyết
- ADR-004 (price lock) đến từ kit ngoài, đã copy nguyên vào `docs/decisions/` —
  không phải quyết định của phiên này, chỉ đồng bộ.

## Đã làm
- Diff cell-by-cell BlazeMaster_Model_v3_3.xlsx vs v3_4.xlsx (Assumptions, Ống,
  Phụ kiện, Dashboard, Plan_SX, PriceList) để xác định chính xác thay đổi:
  giá đợt nhập compound KHÔNG đổi; thay đổi là cơ chế khóa bảng giá mới
  (Assumptions dòng 45-56) + đúng 1 điểm nối vào Ống!B5/Phụ kiện!B5.
- Cập nhật `AGENTS.md`, `CLAUDE.md`, `docs/PROJECT_SPEC.md`, `docs/GLOSSARY.md`,
  `docs/CHANGELOG.md`, `docs/CONTEXT_PASTE.md`, `docs/decisions/ADR-004-price-lock.md`,
  `.claude/skills/{excel-parity-testing,schema-design}/SKILL.md` từ kit v1.1.
- Cập nhật `docs/BUSINESS_MODEL.md`: thêm §1a (cơ chế khóa bảng giá đầy đủ), ghi
  chú lại §2.2/§3.3/§6 để phản ánh đúng điểm nối.
- Cập nhật fixture: `assumptions.json` (+`priceLock`), `dashboard.json`
  (+`priceLockSummary`), file mới `price-lock-scenarios.json` (5 kịch bản nghiệm
  thu ADR-004, kịch bản 2-5 verify bằng công thức tuyến tính vì workbook chỉ lưu
  1 trạng thái tại 1 thời điểm).

## Còn treo / việc phiên sau
- Vẫn chờ frontend/prototype Pha 1 từ user.
- Khi viết engine thật (Pha 3), nhớ cài `priceLock` như 1 bước biến đổi TRƯỚC khi
  compound price chảy vào công thức giá thành — không trộn logic khóa vào trong
  công thức chi phí sản xuất.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 3 cùng ngày — ADR-005: hoạch định hai chiều (forward + inverse solver)
Pha hiện tại: 0 (vẫn chờ frontend Pha 1) | Agent: Claude Code

## Đã quyết
- ADR-005 CHẤP NHẬN (nội dung do user soạn sẵn, ghi nguyên vào repo): engine chỉ
  có MỘT bộ logic forward (ScenarioInput → ScenarioOutput); mọi câu hỏi top-down
  (T1 kế hoạch vs nguồn lực, T2 lợi nhuận mục tiêu → sản lượng/ca, T3 giá thị
  trường → biến vận hành) giải bằng numeric solver (bisection) chạy trên chính
  forward function — CẤM viết công thức ngược tay (2 bộ công thức sẽ lệch nhau).

## Đã làm
- Thêm `docs/decisions/ADR-005-dual-direction.md`.
- Thêm skill mới `.claude/skills/inverse-solver/SKILL.md` (luật solver, biến
  giải ngược hợp lệ v1, test round-trip + case infeasible bắt buộc).
- Vá `docs/CONTEXT_PASTE.md` — thêm điểm 6 (hai chiều hoạch định) vào khối
  NGHIỆP VỤ LÕI.
- Thêm mục v1.2 vào `docs/CHANGELOG.md`.

## Còn treo / việc phiên sau
- `src/engine/solver.ts` chưa viết (đúng luật — chưa tới Pha 3). Khi viết:
  `solve({forwardFn, freeVar, target, bounds, tol})`, kiểm tra f(lo)/f(hi) kẹp
  target trước khi bisect, trả `infeasible` kèm khoảng khả thi nếu không kẹp.
- Biến nguyên (shifts 1–3) phải quét rời rạc, không bisection liên tục.
- Khi lên schema Pha 2, nhớ đưa `PriceLockPolicy` (ADR-004) VÀ khả năng solver
  chạy trên forward function (ADR-005) vào cùng một bản thiết kế — hai ADR này
  tương tác nhau: pricingPrice (đã khóa) mới là biến forward engine dùng, nhưng
  T3 (giải ngược tìm giá compound tối đa) phải hỏi ngược qua đúng biến này.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 4 cùng ngày — ADR-006: phân tầng top-down theo đối tượng xem
Pha hiện tại: 0 (vẫn chờ frontend Pha 1) | Agent: Claude Code

## Đã quyết
- ADR-006 CHẤP NHẬN: top-down tách 2 tầng theo vai — VẬN HÀNH (`production`, T1,
  Plan_SX, theo kỳ kế hoạch) và CHIẾN LƯỢC (`pricing`/`admin`, T2/T3 + giá thâm
  nhập, màn hình Target Costing riêng, khi ra quyết định giá/đầu tư). User xác
  nhận đây là điểm quan trọng vì mức độ quan tâm của CEO khác Quản lý SX — không
  được gộp 2 tầng vào 1 màn hình "what-if" chung.

## Đã làm
- Thêm `docs/decisions/ADR-006-audience-tiers.md`.
- Cập nhật `docs/PROJECT_SPEC.md` §1 (gắn 2 tầng vào bảng vai), §2 (phạm vi), §3
  (thêm `src/engine/solver.ts` vào kiến trúc).
- Cập nhật `docs/GLOSSARY.md` (+4 thuật ngữ), `docs/CONTEXT_PASTE.md` (+điểm 7).
- Thêm mục v1.3 vào `docs/CHANGELOG.md`.

## Còn treo / việc phiên sau
- Pha 1 (khi có prototype): phải dựng 2 màn hình top-down RIÊNG (Plan_SX cho
  `production`, Target Costing cho `pricing`/`admin`), không gộp chung.
- Pha 2 (schema): phân quyền đọc Target Costing giới hạn `pricing`/`admin`, tách
  hẳn khỏi phân quyền Plan_SX — cùng nguyên tắc field/doc-level tách biệt như
  security-review yêu cầu cho giá vốn.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 5 cùng ngày — Nhận prototype Pha 1, thêm khung nhìn theo vai (ADR-006), sửa 2 lỗi công thức
Pha hiện tại: 1 (Prototype — đã có bản đầu, đang phát triển tiếp) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới; áp dụng ADR-006 đã có vào prototype thật.

## Đã làm
- Nhận `prototype/blazemaster-costing-app.dc.html` (bản mock tương tác đầy đủ, tự
  chứa — 8 tab: Tổng Quan, Bảng Giá, Kế Hoạch SX, Tồn Kho, Cấu Hình, Tham Số, Ống,
  Phụ Kiện) do user cung cấp, đối chiếu engine JS với `tests/fixtures/` — khớp
  đúng MHR 1.344.175,79 và giá thành đầy đủ ống 106.204,73 ngay từ đầu.
- Thêm bộ chuyển "Xem Như Vai" (4 vai đúng PROJECT_SPEC §1), lọc tab hiển thị +
  ẩn mục II/III/IV của Dashboard cho vai Bán Hàng, theo đúng ADR-006 + luật
  security-review "sales không đọc được cost".
- Phát hiện và sửa 2 lỗi công thức thật trong lúc verify (không phải do thêm vai
  gây ra): bậc 2 (hòa vốn tiền mặt) cộng nhầm cả khấu hao Lab/UL vào chi phí tiền
  mặt; bậc 4 (hòa vốn toàn DN) chia thẳng theo kg thay vì theo tỷ trọng doanh thu
  VF — lệch nặng nhất ở Phụ Kiện (~31.000đ/kg). Verify lại bằng node smoke-test
  (trích JS ra ngoài, chạy độc lập cho cả 4 vai) — khớp tuyệt đối 100.663/104.298/
  106.205/110.604/132.756 (ống) và ~125.673/130.518/153.435/160.553/214.809 (PK).
- Ghi `prototype/README.md`: bảng vai↔tab, danh sách đã sửa, danh sách còn treo
  (tồn đầu kỳ minh họa chưa gắn ghi chú; panel top-down ống dùng đại số tay — hợp
  lệ ở Pha 1, phải thay bằng solver ở Pha 3, không nhân bản sang phụ kiện).

## Còn treo / việc phiên sau
- Tab Tồn Kho: số "Tồn đầu kỳ 80.000kg@2,60" là minh họa, chưa gắn nhãn "giả định"
  trên UI — cần sửa trước khi trình duyệt Pha 1 chính thức.
- Panel top-down mới có cho Ống, chưa có cho Phụ Kiện.
- Khi lên schema Pha 2: field/doc-level tách theo vai (đang mock ở client) phải
  làm lại đúng chuẩn Firestore rules (không lọc field, tách doc).

## Vi phạm constitution phát hiện được (nếu có)
- 2 lỗi công thức nêu trên VI PHẠM luật bất biến #1 (AGENTS.md: "mọi số liệu
  engine phải khớp Excel fixture") — đã sửa ngay trong phiên, không để sang Pha
  2/3 mới phát hiện.

---

# Phiên 6 cùng ngày — Khóa tham số cấu hình, nhãn minh họa, fix deploy static
Pha hiện tại: 1 (Prototype — đang phát triển tiếp) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới. Thực hiện yêu cầu bảo toàn dữ liệu của user
  (khóa tham số) và sửa lỗi hạ tầng deploy — không phải ADR.

## Đã làm
- **Khóa tham số cấu hình theo vai** (yêu cầu user: "khóa số máy đùn, giá tiền
  và số máy ép, giá tiền... chỉ admin sau này mới có quyền sửa"): 14 field cốt
  lõi (`capActual`, `machinePrice`, `machineCount`, `toolingMold`, `yieldRateOng`,
  `machineAPrice/ACount/BPrice/BCount`, `moldTotal`, `yieldRatePK`, `labCost`,
  `testUL`, `compliance`, `rent`) bị khóa (không sửa được) với mọi vai trừ
  `admin`. Cơ chế: bọc setter bằng `mkSafeHandler()` (no-op nếu không phải
  admin) + UI hiển thị khóa 🔒, input mờ, `pointer-events:none`. Thêm banner
  giải thích ở đầu tab Cấu Hình khi xem bằng vai không phải admin.
- Gắn nhãn "GIẢ ĐỊNH MINH HỌA" (badge màu vàng) vào dòng "Tồn đầu kỳ" ở cả 2
  bảng tồn kho (Ống, Phụ Kiện) — xử lý mục còn treo từ `prototype/README.md`
  (số 80.000kg@2,60 là minh họa, không phải số thật từ Excel).
- Thêm `render.yaml` (root) để sửa lỗi deploy Render: prototype là HTML tĩnh,
  không có bước build/`dist` — cấu hình `runtime: static`, `buildCommand: ""`,
  `staticPublishPath: ./prototype`, rewrite `/` → file `.dc.html`. Đã hướng dẫn
  user xóa service cũ (tạo thủ công, không đọc render.yaml) và tạo lại bằng
  Blueprint (New → Blueprint) để Render áp dụng file này.
- Giải thích cho user: tab Ống có bảng "BẢNG GIÁ THEO DN" (8 dòng, đủ 4 mức giá
  VF/TCG/Niêm yết/VAT) nhưng tab Phụ Kiện thay bằng "BẢNG KHUÔN THEO SIZE" (năng
  lực khuôn, không phải bảng giá) — lý do: PK có 91 SKU, bảng giá chi tiết theo
  từng SKU đã có sẵn ở tab "Bảng Giá" chung; cả 2 tab Ống/PK đều CÓ đủ thang giá
  5 bậc (quy kg) ở phần trên, không thiếu. Đồng thời giải thích nguyên tắc kép
  ADR-002 (giá tái tạo dùng định giá/chào giá vs bình quân gia quyền dùng ghi
  sổ/đo lãi kỳ — 2 số cố ý khác nhau).

## Còn treo / việc phiên sau
- User cần tự thao tác trên Render dashboard (xóa service cũ, tạo Blueprint
  mới) — chưa xác nhận deploy đã chạy được, cần theo dõi log lần tới.
- Panel top-down (III. Phân Tích Ngược) vẫn chỉ có cho Ống, chưa có cho Phụ
  Kiện — giữ nguyên từ Phiên 5, chưa làm trong phiên này.
- Vẫn chưa có phê duyệt chính thức đóng Pha 1 (chưa "duyệt UI" theo cổng ở
  AGENTS.md) — trước khi sang Pha 2 (schema) cần user xác nhận chốt prototype.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 7 cùng ngày — Bảng giá 5 mức theo SKU cho Phụ Kiện (91 SKU)
Pha hiện tại: 1 (Prototype — đang phát triển tiếp) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới. User chọn xử lý mục còn treo từ Phiên 6
  (bảng giá chi tiết theo SKU cho Phụ Kiện) qua AskUserQuestion.

## Đã làm
- Đọc `docs/BUSINESS_MODEL.md` §3.4 để lấy đúng công thức chuỗi markup cho
  từng SKU phụ kiện (`machineHoursPerUnit → materialCostPerUnit →
  processingCostPerUnit → breakEvenPerUnit → vfPricePerUnit → tcgPricePerUnit
  → listPriceBeforeVat → listPriceWithVat`) — khác công thức Ống §2.3 vì có
  thêm bước quy đổi giờ máy/MHR.
- Trích số vàng cho cả 91 SKU trực tiếp từ `tests/fixtures/fitting.json`
  (field `skus`), KHÔNG gõ tay/suy diễn — đối chiếu tên sản phẩm + size khớp
  100% với `PK_CATALOG` đã có trong prototype.
- Thêm bảng mới "BẢNG GIÁ THEO SKU — CHUỖI MARKUP (91 SKU)" vào tab Phụ Kiện,
  đặt giữa ghi chú ADR-002 và bảng khuôn hiện có (không thay thế, giữ cả 2 vì
  khác loại thông tin). 5 cột: Giá thành / Giá VF / Giá TCG / Niêm yết / Có
  VAT, cuộn dọc (max-height 520px) vì dài hơn nhiều so với bảng 8 dòng của Ống.
- Verify: parse lại 91 dòng vừa nhúng vào HTML, so từng field với fixture gốc
  bằng script Python độc lập — khớp tuyệt đối 0 sai lệch (455 giá trị = 91×5).
  Kiểm tra cú pháp JS bằng `new Function()` sau khi trích script ra — không lỗi.

## Còn treo / việc phiên sau
- Như Phiên 6: chờ xác nhận deploy Render (Blueprint), panel top-down PK vẫn
  chưa có (chặn bởi ADR-005 — cần solver thật ở Pha 3, không được viết đại số
  tay cho PK vì MHR nằm cả tử lẫn mẫu), và vẫn chưa có phê duyệt đóng Pha 1.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 8 cùng ngày — ADR-007 (khấu hao khuôn theo thời gian) + ADR-008 (ren kim loại mua ngoài)
Pha hiện tại: 1 (Prototype — vẫn đang phát triển; 2 ADR mới ảnh hưởng Pha 2 sắp tới) | Agent: Claude Code

## Đã quyết
- **ADR-007 CHẤP NHẬN (nguyên tắc)**: user xác nhận mục đích tách chi phí khuôn
  riêng từng khuôn là để **chuẩn bị cho khuôn mua sau** (mở rộng SKU theo thời
  gian) — không chỉ minh bạch CAPEX. Hệ quả: MHR không còn là hằng số cấu hình,
  phải tính động theo `asOfYear` trên danh sách `moldAsset` (giá + năm mua +
  số năm khấu hao riêng từng khuôn), thay cho `moldSetCostTotal66` gộp.
- **ADR-008 CHẤP NHẬN (nguyên tắc)**: user xác nhận ren kim loại (đồng thau,
  mua ngoài, ép vào phụ kiện ren) phải xử lý **giống hệt compound** — áp đúng
  giá vốn kép ADR-002 (bình quân gia quyền sổ sách vs giá tái tạo định giá),
  không cộng thẳng hằng số tĩnh vào `brassInsertCost` như thiết kế cũ.
- Cả 2 ADR ở trạng thái "CHẤP NHẬN nguyên tắc — CHỜ DỮ LIỆU": còn thiếu số liệu
  thật (giá/năm mua từng khuôn; đơn giá — ngoại tệ hay VND — số lượng ren/SKU,
  có vào cơ chế khóa giá ADR-004 hay không, có cần tồn kho riêng hay không) nên
  CHƯA vào schema Pha 2 chính thức, chỉ ghi nhận hướng quyết định.

## Đã làm
- Phân tích tác động của cả 2 thông tin mới lên kiến trúc hiện có (ADR-001,
  ADR-002, ADR-004, ADR-005) trước khi hỏi lại user — không tự suy diễn phạm vi.
- Dùng AskUserQuestion 2 lần để chốt đúng 2 quyết định cốt lõi ở trên (thay vì
  đoán), theo luật CLAUDE.md "yêu cầu mơ hồ → hỏi lại 1 câu chốt phạm vi".
- Viết `docs/decisions/ADR-007-time-phased-mold-depreciation.md` và
  `docs/decisions/ADR-008-purchased-metal-insert.md`.
- Đồng bộ `docs/GLOSSARY.md` (+4 thuật ngữ: moldAsset, asOfYear,
  purchasedMetalInsert, insertQtyPerUnit), `docs/CONTEXT_PASTE.md` (+điểm 8, 9),
  `docs/CHANGELOG.md` (mục v1.4).
- KHÔNG sửa `prototype/blazemaster-costing-app.dc.html` — cả 2 ADR chưa đủ dữ
  liệu để demo đúng số thật, sửa mock lúc này sẽ là bịa số (vi phạm luật bất
  biến #1).

## Còn treo / việc phiên sau
- ADR-007: cần giá + năm mua thực tế của 66 bộ khuôn hiện có (ít nhất coi là
  "đợt mua gốc") trước khi thiết kế `moldAssets` trong schema Pha 2.
- ADR-008: cần trả lời 4 câu hỏi mở trong ADR (khóa giá riêng hay gộp; VND hay
  ngoại tệ; số lượng ren/SKU theo từng size; có track tồn kho riêng không)
  trước khi thiết kế material stream thứ 2 trong schema Pha 2.
- Khi có đủ dữ liệu cho 1 trong 2 ADR, có thể cập nhật `tests/fixtures/` +
  `docs/BUSINESS_MODEL.md` trước, rồi mới sửa prototype Pha 1 để demo đúng số.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 9 cùng ngày — Chốt nốt 3 câu hỏi mở ADR-007/008
Pha hiện tại: 1 (Prototype) | Agent: Claude Code

## Đã quyết
- **ADR-007**: 66 bộ khuôn hiện có → `purchaseYear = 2026` (đợt mua gốc, tất cả
  cùng năm 0 khấu hao). Giá từng khuôn (`cost`) ĐỂ TRỐNG — user tự nhập khi có
  số liệu; field phải cho phép chưa điền ở Pha 2, KHÔNG mặc định = 0.
- **ADR-008**: chốt xong cả 3 câu hỏi mở còn lại —
  1. Ren kim loại mua **VND trong nước** (không ngoại tệ, không DUTY/logistics).
  2. **Có tồn kho riêng** nhiều đợt nhập (giống cơ chế Ống/Phụ kiện hiện có).
  3. **Khóa giá riêng** (mở rộng ADR-004) — ren có baseline + ngưỡng % độc lập
     với compound, không gộp chung 1 ngưỡng.
  → ADR-008 nay CHẤP NHẬN đầy đủ về nguyên tắc, chỉ còn thiếu SỐ LIỆU (đơn giá,
  số lượng ren/SKU, baseline/ngưỡng khóa ban đầu).

## Đã làm
- Cập nhật `docs/decisions/ADR-007-...md` và `ADR-008-...md`: chuyển trạng thái
  từ "CHỜ DỮ LIỆU (bao gồm quyết định)" sang "CHẤP NHẬN — chỉ chờ SỐ LIỆU".
  ADR-008 thêm mục 5 (tồn kho riêng) và mục 6 (khóa giá riêng — 2 policy độc lập
  compound/metalInsert trong `fitting.priceLock`), sửa công thức bỏ bước landed
  cost/thuế NK (vì xác nhận VND trong nước).
- Dùng AskUserQuestion 1 lần nữa để chốt câu hỏi khóa giá (câu duy nhất còn mơ
  hồ) — không tự suy diễn.
- Đồng bộ `docs/CONTEXT_PASTE.md` (điểm 8, 9 cập nhật lại nội dung), `docs/CHANGELOG.md`
  (thêm đoạn "Cập nhật cùng ngày" vào mục v1.4, không tạo mục version mới vì
  cùng đợt thay đổi).
- Vẫn KHÔNG sửa prototype/fixtures — đơn giá + số lượng ren/SKU + giá từng
  khuôn vẫn chưa có, sửa số lúc này vẫn là bịa số.

## Còn treo / việc phiên sau
- ADR-007: giá (`cost`) thực tế từng khuôn/lô khuôn — user tự nhập.
- ADR-008: đơn giá ren kim loại (VND) + số lượng ren/sản phẩm theo từng SKU
  trong 4 họ + baseline/ngưỡng khóa giá ban đầu — user tự nhập.
- Khi có đủ số liệu, cập nhật `tests/fixtures/fitting.json` (thêm `moldAssets`,
  material stream ren kim loại) và `docs/BUSINESS_MODEL.md` (§3.3/§3.4/§5)
  TRƯỚC khi sửa prototype Pha 1 để demo đúng số.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 10 cùng ngày — Nhận & xử lý số liệu thật: giá 66 khuôn + đơn giá ren kim loại
Pha hiện tại: 1 (Prototype) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới — chỉ NẠP số liệu thật vào 2 ADR đã chấp
  nhận nguyên tắc ở Phiên 8/9.

## Đã làm
- Đọc 2 PDF user upload: `contract_mold_price_from_David2506062.pdf` (66 dòng
  giá khuôn USD) và `Gia_phu_kien_ren.pdf` (11 dòng đơn giá ren kim loại VND).
- **Verify độc lập trước khi tin số liệu**: tính tổng 66 giá USD × tỷ giá
  26.500 = 246.900 × 26.500 = 6.542.850.000đ — khớp TUYỆT ĐỐI với
  `fitting.json.params.moldSetCostTotal66` đang dùng. Xác nhận đọc PDF đúng.
- Đối chiếu từng dòng khuôn với danh mục 91 SKU (`PK_CATALOG` trong prototype,
  so khớp cả cavity) — phát hiện 66 khuôn chỉ tạo ra 83/91 SKU vì nhiều khuôn
  DÙNG CHUNG cho nhiều biến thể (vd khuôn "Nối thẳng 50" sản xuất luôn 4 SKU
  "Nối giảm 50x25/32/40" + "Nắp bịt 50" cùng 1 bộ khuôn — đọc từ ghi chú gốc
  PDF "socket+cap+reducing trong 1 khuôn"). Không có mismatch, không trùng lặp.
- Phát hiện 8 SKU KHÔNG có trong hợp đồng khuôn (Cút ren trong ×3, Tê ren trong
  ×4, Tê giảm 50x40 ×1) — dùng AskUserQuestion hỏi lại thay vì tự suy diễn; user
  xác nhận ĐÚNG (8 SKU này chưa sản xuất/chưa mua khuôn thật) — khớp đúng kịch
  bản ADR-007 dự đoán trước (khuôn mua sau khi mở rộng SKU).
- Đối chiếu file ren kim loại: cột `unitWeightKg` khớp 100% với `PK_CATALOG` —
  xác nhận đúng SKU. Phạm vi chỉ 11 SKU (Nối ren trong 7 + Nối ren ngoài 4) —
  khớp CHÍNH XÁC với 11 SKU có khuôn ở trên (Cút/Tê ren trong không có cả 2).
- Tạo `tests/fixtures/mold-assets.json` (66 mold asset, kèm `producesSkus`,
  `costUsd`/`costVnd`, `purchaseYear:2026`, `usefulLifeYears:5`, và danh sách
  `skusWithoutMold` 8 SKU chưa có khuôn) và `tests/fixtures/metal-insert.json`
  (11 SKU, `insertQtyPerUnit:1`, `insertPricingCostVndPerUnit`) — cả 2 file
  MỚI, không đụng vào fixture Excel-parity hiện có (`fitting.json` giữ nguyên).
- Cập nhật ADR-007/008 sang trạng thái "ĐÃ CÓ SỐ LIỆU", trỏ tới 2 file mới,
  ghi rõ phát hiện 8 SKU thiếu khuôn + thu hẹp phạm vi ren kim loại còn 11 SKU.
  Đồng bộ `docs/CHANGELOG.md`.
- KHÔNG sửa prototype hay `fitting.json` gốc — 2 file mới là dữ liệu "nháp Pha
  2", chưa chính thức vào schema (còn thiếu baseline khóa giá + tồn kho ban đầu
  cho ren kim loại, và giá khuôn mới nếu công ty mua thêm cho 8 SKU kia).

## Còn treo / việc phiên sau
- ADR-008: `baselinePriceVnd` + `thresholdPct` ban đầu cho khóa giá ren kim
  loại, và tồn kho ban đầu (đợt nhập/số lượng/giá) — chưa có lịch sử nhập nên
  chưa tính được bình quân gia quyền thật.
- ADR-007: giá khuôn mới khi công ty thực mua thêm cho 8 SKU chưa sản xuất.
- Khi sang Pha 2 (schema): quyết định `MoldAsset.cost` cho phép `null` hay tách
  hẳn SKU chưa có khuôn khỏi danh mục sản phẩm đang bán (khác ý nghĩa business).
- Khi đủ dữ liệu, có thể cân nhắc cập nhật prototype Pha 1 để demo 2 dòng chi
  phí mới (nhưng cần thêm UI mới — tab tồn kho ren kim loại, bảng khuôn có giá
  — chưa làm trong phiên này, chưa được yêu cầu).

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 11 cùng ngày — Tồn kho ban đầu ren kim loại + phát hiện granularity đúng (theo PT size)
Pha hiện tại: 1 (Prototype) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới — làm rõ 1 chi tiết kỹ thuật quan trọng của
  ADR-008 (granularity đúng của vật tư ren kim loại), không đổi nguyên tắc.

## Đã làm
- User gửi 10 dòng số lượng (đơn vị Cái) không kèm tên rõ ràng là gì — dùng
  AskUserQuestion xác nhận đây là TỒN KHO HIỆN CÓ, cùng giá đã có trong
  `Gia_phu_kien_ren.pdf`, thay vì tự suy đoán ý nghĩa con số.
- Trước khi tin cấu trúc, đối chiếu lại giá đã lưu: phát hiện SKU "Nối ren
  trong 20xPT15" và "25xPT15" có giá GIỐNG HỆT (16.200đ) — bằng chứng ren kim
  loại thực chất là **10 loại vật tư theo (renType, ptSize)**, không phải 11
  loại theo từng SKU đầy đủ (2 SKU nhựa khác nhau dùng chung 1 loại ren).
  10 dòng user gửi khớp CHÍNH XÁC với 10 loại này (6 "trong" + 4 "ngoài").
- Cập nhật `tests/fixtures/metal-insert.json`: thêm `insertCatalog` (10 dòng —
  renType, ptSize, unitPriceVnd, inventoryQtyOnHand, inventoryValueVnd,
  weightedAvgPriceVnd) và `skuToInsertMap` (map 11 SKU → 10 loại ren), giữ
  nguyên `metalInsertSkus` cũ (đủ cho tính giá thành/SKU, nhưng KHÔNG phải cấp
  đúng cho tồn kho). Tổng: 29.000 cái, 1.016.300.000đ.
- Cập nhật `docs/decisions/ADR-008-...md`: thêm phát hiện granularity (sửa lại
  diễn giải mục 3 — `insertQtyPerUnit=1` xác nhận cho cả 11 SKU, nhưng tồn
  kho/mua hàng phải quản lý ở cấp 10 loại, không tách theo SKU), thêm mục "Dữ
  liệu thật — Tồn kho ban đầu". Trạng thái ADR-008 nay chỉ còn thiếu
  baseline/ngưỡng khóa giá ban đầu.
- Đồng bộ `docs/CHANGELOG.md` (đoạn "Cập nhật lần 3").

## Còn treo / việc phiên sau
- ADR-008: `baselinePriceVnd` + `thresholdPct` ban đầu cho khóa giá ren kim
  loại — có thể mặc định = giá hiện hành cho lần chốt đầu, cần user xác nhận.
- ADR-007: giá khuôn mới khi công ty thực mua thêm cho 8 SKU chưa sản xuất.
- Khi 7 SKU Cút/Tê ren trong vào sản xuất thật, cần đơn giá + tồn kho ren cho
  chúng lúc đó.
- Pha 2 (khi lên schema): quyết định model tồn kho ren kim loại nên gắn theo
  `insertCatalog` (10 dòng, đúng nghiệp vụ mua/tồn) hay theo SKU — khuyến nghị
  dùng `insertCatalog` làm nguồn chân lý tồn kho, `skuToInsertMap` chỉ để tra
  cứu giá khi tính giá thành từng SKU.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.
