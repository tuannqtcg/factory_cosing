# SESSION 2026-07-06 — Duyệt UI đóng Pha 1, mở cổng sang Pha 2
Pha hiện tại: 2 (Flow + Zod schema + API contract) | Agent: Claude Code

## Đã quyết (nếu là quyết định kiến trúc → tạo ADR, ghi số ADR ở đây)
- User **DUYỆT UI chính thức** cho Pha 1 (Prototype) — cổng theo AGENTS.md
  ("brief → prototype → ⛔ duyệt UI → schema...") đã mở. Không phải ADR kiến trúc
  (không đổi nghiệp vụ), chỉ là quyết định quy trình nhưng bắt buộc ghi lại vì
  đây là điểm chuyển pha.
- `prototype/blazemaster-costing-app.dc.html` (bản cuối cùng của Phiên 1-14 hôm
  qua) trở thành nguồn tham chiếu UI/UX đóng băng cho Pha 3 — đổi thiết kế màn
  hình ở Pha 2/3 phải quay lại Pha 1 và ghi ADR mới, không tự ý sửa trong lúc
  code.

## Đã làm
- Cập nhật `docs/CONTEXT_PASTE.md`: đổi khối "TRẠNG THÁI HIỆN TẠI" từ Pha 1 →
  Pha 2, ghi rõ ngày duyệt và việc tiếp theo (flow + schema Zod bằng skill
  `schema-design`).

## Còn treo / việc phiên sau
- Bắt đầu Pha 2: dựng Zod schema + API contract cho toàn bộ nghiệp vụ đã chốt ở
  điểm 1-10 trong CONTEXT_PASTE.md (cost driver kép, giá vốn kép, khóa bảng giá,
  thang giá 5 bậc, hoạch định 2 chiều + phân tầng vai, khấu hao khuôn theo thời
  gian, ren kim loại mua ngoài, danh mục quản lý thu hẹp 8 SKU) — dùng skill
  `schema-design`, không phát minh field ngoài những gì đã có trong ADR/fixture.
- ADR-007: vẫn thiếu giá khuôn mới khi mua thêm cho 8 SKU chưa sản xuất — không
  chặn Pha 2 (đã có `MoldAsset.cost` cho phép rỗng theo quyết định trước), nhưng
  cần theo dõi khi có số liệu.
- ADR-008: khi 7 SKU Cút/Tê ren trong vào sản xuất thật, cần đơn giá + tồn kho +
  baseline khóa giá cho loại ren mới của chúng.
- Sau khi có schema nháp: cần ⛔ đóng băng schema (cổng thứ 2) trước khi viết
  code Pha 3 — nhắc user xác nhận rõ ràng như đã làm với cổng Pha 1.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 2 cùng ngày — Viết schema nháp Pha 2 (5 file contract)

Pha hiện tại: 2 (Flow + Zod schema + API contract) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới (không tạo ADR) — hiện thực hóa các ADR đã
  chấp nhận (001-008) thành schema cụ thể, đúng vai trò của Pha 2.
- **Quyết định thiết kế trong lúc viết schema** (ghi lại vì ảnh hưởng cấu trúc):
  1. Tách rõ INPUT (cấu hình lưu trữ: `baseline`, `thresholdPct`, `Resource`,
     `Product`, `CostPool`) khỏi OUTPUT (giá trị tính ra mỗi lần forward:
     `replacement`, `pricingPrice`, MHR, thang giá, CVP...) — khớp đúng kiến
     trúc `(ScenarioInput) → ScenarioOutput` ở PROJECT_SPEC §3.
  2. `managementStatus` (active/pending_mold — ADR-007 quyết định bổ sung)
     THIẾT KẾ LÀ TRƯỜNG TÍNH RA từ `moldAssets`, KHÔNG lưu trên `Product` — để
     tránh lỗi đồng bộ tay giống `EXCLUDED_SKUS` ở prototype Pha 1.
  3. Firestore: tách VẬT LÝ 4 doc con (`outputs/internal`, `outputs/priceList`,
     `outputs/plan`, `outputs/targetCosting`) thay vì 1 doc lọc field — vì rules
     Firestore không lọc field trong cùng 1 document.
  4. Engine chạy trong Cloud Function (không chạy thẳng ở client) — nếu chạy ở
     browser của vai `sales`, `ScenarioInput` đầy đủ (gồm giá vốn) phải tải
     xuống trước khi tính, vi phạm luật "sales không đọc được cost" ngay ở tầng
     network dù UI có ẩn số.

## Đã làm
- Đọc lại toàn bộ ADR 001-008, `PROJECT_SPEC.md`, `GLOSSARY.md`, tất cả fixture
  (`assumptions/pipe/fitting/dashboard/price-list/mold-assets/metal-insert.json`),
  `docs/BUSINESS_MODEL.md` §1-8, skill `inverse-solver`, và phần cấu hình
  vai/khóa tham số trong `prototype/blazemaster-costing-app.dc.html` để đảm bảo
  schema khớp 100% số liệu/quy tắc đã chốt — không phát minh field mới ngoài
  ADR/fixture.
- Viết `docs/contracts/resource.md`: `ResourceSchema` discriminated union
  (`continuous_kg` | `machine_hour`) + `MoldAssetSchema` (ADR-007).
- Viết `docs/contracts/product.md`: `ProductSchema` discriminated union
  (`pipe` | `fitting`) + BOM ren kim loại optional (ADR-008) + hàm
  `managementStatusOf()` tính từ `moldAssets`.
- Viết `docs/contracts/cost-pool.md`: `SharedFixedCosts`, `NonProductionCosts`,
  `CurrencyParams`, `MarkupChain`.
- Viết `docs/contracts/pricing-chain.md`: `PriceLockPolicy` generic (factory
  function, dùng chung USD/VND) tách input/output; `CompoundInventory` +
  `MetalInsertCatalog` (dual costing ADR-002/008); thang giá 5 bậc; chuỗi
  markup SKU. **Phát hiện và ghi rõ cảnh báo**: `thresholdPct` lệch đơn vị giữa
  2 fixture hiện có (`assumptions.json` = thập phân 0.03, `metal-insert.json`
  = số nguyên 5) — chốt dùng thập phân cho schema, cần chuyển đổi khi migrate.
- Viết `docs/contracts/scenario.md`: `ScenarioInputSchema`/`ScenarioOutputSchema`
  tổng hợp 4 file kia; `PlanInput`/`PlanResult` (T1, dạng đóng); solver contract
  + `TargetProfitRequest` (T2, dạng đóng CVP) + `TargetPriceRequest` (T3, qua
  solver, gồm giá thâm nhập); bảng Firestore doc split (6 collection/doc path)
  + bảng phân quyền tổng hợp 4 vai × 6 vùng dữ liệu.
- Cập nhật `docs/CONTEXT_PASTE.md`: ghi rõ schema nháp đã viết xong, CHƯA đóng
  băng (chờ duyệt cổng thứ 2), nêu phát hiện lệch đơn vị `thresholdPct`.

## Còn treo / việc phiên sau
- **Chờ user duyệt đóng băng schema (cổng thứ 2)** — sau khi duyệt mới được
  viết code Pha 3 (`src/schemas/*.ts` thật từ 5 file `.md` này).
- Khi migrate: chuẩn hóa `thresholdPct` trong `metal-insert.json` sang thập
  phân (xem cảnh báo `pricing-chain.md`).
- Cơ chế cấp/thu hồi Firebase Auth custom claim `role` chưa thiết kế — để dành
  security-review Pha 4.
- Cloud Function tính `ScenarioOutput` + ghi 4 doc con — chưa có code, mới là
  thiết kế/hợp đồng.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 3 cùng ngày — Duyệt đóng băng schema, mở Pha 3 (M1: scaffold + src/schemas)
Pha hiện tại: 3 (Code) — M1/12 xong | Agent: Claude Code

## Đã quyết
- User duyệt schema Pha 2 ("thực hiện theo đề xuất và triển khai") → 5 file
  `docs/contracts/*.md` chính thức **ĐÓNG BĂNG** (cổng thứ 2, AGENTS.md).
- User yêu cầu: **chia Pha 3 thành nhiều phiên/milestone nhỏ** để không tốn
  tool call và có thể dừng khi sắp hết token (trả lời AskUserQuestion về phạm
  vi "triển khai" bằng yêu cầu này thay vì chọn 1 trong 3 phương án phạm vi đề
  xuất) — không phải ADR, là quyết định vận hành phiên làm việc.

## Đã làm
- Viết `docs/PHASE3_PLAN.md`: lộ trình 12 milestone (M1 scaffold/schema → M2-M9
  từng phần engine → M10 solver → M11 parity suite đầy đủ → M12 UI thật/Firestore,
  chỉ làm nếu mở rộng phạm vi), mỗi milestone tự chứa, có bảng trạng thái để
  phiên sau đọc 1 file là biết chính xác việc tiếp theo.
- **M1 hoàn tất**:
  - `package.json` (zod, typescript, vitest) + `tsconfig.json` (strict).
  - `src/schemas/{resource,product,cost-pool,pricing-chain,scenario}.ts` — code
    Zod thật, giữ NGUYÊN cấu trúc đã đóng băng ở `docs/contracts/*.md`, không
    phát minh field mới.
  - `tests/unit/schemas.test.ts` — 10 test: parse toàn bộ 66 `moldAssets`, 91
    SKU phụ kiện, 8 SKU ống, `CostPool` từ `assumptions.json` thật; test
    `managementStatusOf()` với 1 SKU có khuôn (active) + 1 SKU trong
    `skusWithoutMold` (pending_mold); và 2 test xác nhận phát hiện lệch đơn vị
    `thresholdPct` — chứng minh Zod CHỦ ĐỘNG TỪ CHỐI giá trị số nguyên % (5)
    chưa chuẩn hóa, chỉ chấp nhận sau khi chia 100.
  - `npm run typecheck` sạch, `npm test` 10/10 xanh.
- Cập nhật `docs/CONTEXT_PASTE.md` (Pha 2→3, trỏ PHASE3_PLAN.md, ghi M1 xong),
  `docs/CHANGELOG.md` (mục v1.8).

## Còn treo / việc phiên sau
- **M2 tiếp theo**: `src/engine/pipe.ts` — công suất + chi phí SX Ống tại công
  suất bình thường (BUSINESS_MODEL §2.1-2.2), verify khớp tuyệt đối
  `tests/fixtures/pipe.json.costAtNormalCapacity` (`fullCostPerKg =
  106204.729733113`). Xem chi tiết đầy đủ M2..M12 ở `docs/PHASE3_PLAN.md`.
- Node/npm audit báo 5 lỗ hổng (3 moderate, 1 high, 1 critical) — toàn bộ nằm
  trong chuỗi transitive `vitest→vite→esbuild` (rủi ro dev-server, không phải
  runtime production), fix cần nâng vitest lên v4 (breaking change) — CHƯA làm
  vì ngoài phạm vi M1, cân nhắc lại khi milestone sau cần API vitest mới.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 4 cùng ngày — Pha 3 M2: engine Ống (src/engine/pipe.ts)
Pha hiện tại: 3 (Code) — M2/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới.
- **Sửa lỗi thiếu sót schema** (không phải ADR): `ContinuousKgResourceSchema` +
  `MachineHourResourceSchema` (viết ở M1) thiếu 12 field mà công thức chi phí
  gia công §2.2/§3.3 luôn cần (lương, điện, nước, bao bì, năng suất quy đổi giờ
  máy→kg). Phát hiện ngay khi bắt tay viết `pipe.ts` (không tính được
  `laborPerYear` nếu thiếu `avgSalaryMonthly`). Đã bổ sung vào
  `src/schemas/resource.ts` + `docs/contracts/resource.md` (có ghi chú rõ đây
  là sửa lỗi dịch thiếu ADR-001, không phải đổi nghiệp vụ) + cập nhật
  `tests/unit/schemas.test.ts` cho khớp.

## Đã làm
- Viết `src/engine/cost-pool.ts`: `sharedFixedCostsTotalPerYear()` — helper
  dùng chung cho cả Ống (M2) và Phụ kiện (M3, sẽ viết sau), verify công thức
  khớp `assumptions.json.sharedFixedCosts.totalPerYear = 1.253.500.000`.
- Viết `src/engine/pipe.ts`: `calculatePipeCapacity()` (§2.1) +
  `calculatePipeCostAtNormalCapacity()` (§2.2). 2 tham số CHƯA nối dây thật, ghi
  rõ trong comment đầu file: `otherLineEstimatedProductionKgYear` (cross-ref
  sang Phụ kiện cho `sharedCostAllocationRatio` — chưa có `fitting.ts` tới M3)
  và `compoundPricingPriceUsdPerKg` (output ADR-004 price-lock — chưa có engine
  tới M5); cả 2 nhận trực tiếp làm input tạm thời, KHÔNG import module chưa tồn
  tại (giữ `pipe.ts` độc lập/pure).
- Viết `tests/parity/pipe.test.ts` (skill `excel-parity-testing`): đối chiếu
  toàn bộ `pipe.json.capacity` + `pipe.json.costAtNormalCapacity` — khớp tuyệt
  đối `fullCostPerKg = 106.204,729733113` và `vfPricePerKg =
  132.755,912166391` (số vàng bậc 3 và bậc 5 thang giá).
- `npm run typecheck` sạch, `npm test` 14/14 xanh (10 schema cũ + 4 parity mới).
- Cập nhật `docs/PHASE3_PLAN.md` (M2 `[x]`, thêm mục "Nhật ký milestone đã
  xong" + hướng dẫn chi tiết M3), `docs/CONTEXT_PASTE.md`, `docs/CHANGELOG.md`
  (mục v1.9).

## Còn treo / việc phiên sau
- **M3 tiếp theo**: `src/engine/fitting.ts` — công suất ép phun + MHR (§3.1-3.3,
  ADR-001), CÙNG PATTERN đã dùng ở `pipe.ts`. Lưu ý: khấu hao khuôn ĐỘNG theo
  `asOfYear` (ADR-007) là M4 riêng — M3 có thể tạm cộng thẳng
  `Σ moldAsset.costVnd/usefulLifeYears` không lọc năm (vẫn ra đúng số vàng hiện
  tại vì `purchaseYear=2026` toàn bộ 66 khuôn). Chi tiết đầy đủ ở
  `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 5 cùng ngày — Pha 3 M3: engine Phụ kiện (src/engine/fitting.ts)
Pha hiện tại: 3 (Code) — M3/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới.
- **Sửa lỗi thiếu sót schema** (không phải ADR): `MachineHourResourceSchema`
  thiếu `depreciationYears` (khấu hao MÁY ép phun — khái niệm KHÁC
  `MoldAsset.usefulLifeYears` là khấu hao KHUÔN, dù cùng =5 trong dữ liệu hiện
  tại). Bổ sung vào `src/schemas/resource.ts` + `docs/contracts/resource.md`.
- **Quyết định thiết kế trong lúc code**: tách `machineDepreciationPerYear` và
  `moldDepreciationPerYear` thành 2 field riêng thay vì gộp `machineMoldDepreciation`
  như Excel — để M4 (khấu hao khuôn động theo `asOfYear`, ADR-007) chỉ cần sửa
  đúng 1 field, không đụng vào phần khấu hao máy.

## Đã làm
- Viết `src/engine/fitting.ts`: `calculateFittingCapacity()` (§3.2) +
  `calculateFittingCostAtNormalCapacity()` (§3.3). Verify tay từng dòng công
  thức trước khi code (batchesPerYear, totalMachines, designMachineHours3Shift,
  normalMachineHoursUtilized, estimatedProductionKgYear, rồi machineDepreciation
  + moldDepreciation + labor + electricity + water + sharedCostAllocated →
  mhrPerMachineHour) — tất cả khớp số vàng trước khi viết test.
  `moldDepreciationPerYear` hiện cộng Σ toàn bộ 66 `moldAsset` KHÔNG lọc theo
  `asOfYear` (đúng vì tất cả `purchaseYear=2026`, năm đầu khấu hao) — M4 sẽ
  thêm điều kiện lọc.
- Viết `tests/parity/fitting.test.ts`: khớp tuyệt đối
  `mhrPerMachineHour = 1.344.175,79463858`, cùng toàn bộ field trung gian
  (`sharedCostAllocationRatio=0,124026...`, `totalProcessingCostPerYear`...) và
  3 số quy-kg tham chiếu (`processingCostPerKgRef`/`fullCostPerKgRef`/`vfPricePerKgRef`).
- `npm run typecheck` sạch, `npm test` 18/18 xanh (10 schema + 4 parity pipe +
  4 parity fitting).
- Cập nhật `docs/PHASE3_PLAN.md` (M3 `[x]`, thêm hướng dẫn chi tiết M4 — chỉ
  cần sửa 1 điều kiện lọc trong vòng lặp `moldDepreciationPerYear`, không đổi
  công thức), `docs/CONTEXT_PASTE.md`, `docs/CHANGELOG.md` (mục v1.10).

## Còn treo / việc phiên sau
- **M4 tiếp theo**: thêm `asOfYear` vào input + điều kiện lọc trong
  `calculateFittingCostAtNormalCapacity()` (chỉ cộng khuôn còn trong thời gian
  khấu hao) + viết test với `asOfYear` giả định xa hơn 2026 để CHỨNG MINH logic
  lọc chạy đúng (hiện chưa có test nào phân biệt được vì `asOfYear=2026` luôn
  rơi vào năm đầu của mọi khuôn). Chi tiết đầy đủ ở `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 6 cùng ngày — Pha 3 M4: khấu hao khuôn động theo asOfYear (ADR-007)
Pha hiện tại: 3 (Code) — M4/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới — hiện thực hóa đúng ADR-007 đã chấp nhận.

## Đã làm
- Viết `src/engine/mold-depreciation.ts`: `isMoldAssetStillDepreciating(asset,
  asOfYear)` (điều kiện `0 ≤ asOfYear−purchaseYear < usefulLifeYears`, có xử lý
  cả trường hợp `asOfYear < purchaseYear` — khuôn chưa tồn tại) +
  `moldDepreciationPerYear(moldAssets, asOfYear)`.
- Nối vào `src/engine/fitting.ts`: thêm `asOfYear` vào
  `FittingCostAtNormalCapacityInputs`, thay vòng lặp reduce inline (M3) bằng
  gọi hàm mới — không đổi công thức nào khác, chỉ thêm điều kiện lọc đúng như
  kế hoạch M3 đã ghi.
- Viết `tests/unit/mold-depreciation.test.ts` (10 test): 5 test đơn vị cho
  `isMoldAssetStillDepreciating` (năm đầu/năm cuối/đúng mốc hết hạn/quá hạn
  nhiều năm/trước năm mua); 2 test cho `moldDepreciationPerYear` dùng DỮ LIỆU
  TỔNG HỢP (3 khuôn mua 3 năm khác nhau, đời sống khác nhau) để chứng minh lọc
  CHỌN LỌC đúng — dữ liệu thật (66 khuôn cùng `purchaseYear=2026`) không đủ
  phân biệt logic lọc có chạy đúng hay không (luôn ra tất-cả-hoặc-không); 3 test
  trên `mold-assets.json` thật tại `asOfYear` 2026 (khớp năm gốc, Σ=1.308.570.000),
  2030 (còn hạn, vẫn khớp), 2031 (hết hạn cả 66 khuôn → về 0, không throw).
- Cập nhật `tests/parity/fitting.test.ts` (thêm `asOfYear: 2026` vào input) —
  vẫn khớp đúng `mhrPerMachineHour` cũ.
- `npm run typecheck` sạch, `npm test` 28/28 xanh.
- Cập nhật `docs/PHASE3_PLAN.md` (M4 `[x]`, hướng dẫn chi tiết M5 — nối dây 2
  tham số "tạm thời" từ M2/M3 vào output price-lock thật), `docs/CONTEXT_PASTE.md`,
  `docs/CHANGELOG.md` (mục v1.11).

## Còn treo / việc phiên sau
- **M5 tiếp theo**: `src/engine/price-lock.ts` (ADR-004, 5 kịch bản nghiệm thu
  trong `price-lock-scenarios.json`) + `src/engine/dual-costing.ts` (ADR-002) +
  NỐI DÂY `compoundPricingPriceUsdPerKg` ở `pipe.ts`/`fitting.ts` (hiện nhận
  trực tiếp làm input) vào output thật của `evaluatePriceLock()`. Chi tiết đầy
  đủ ở `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 7 cùng ngày — Pha 3 M5: khóa bảng giá (ADR-004) + giá vốn kép (ADR-002)
Pha hiện tại: 3 (Code) — M5/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới — hiện thực hóa đúng ADR-002/ADR-004 đã
  chấp nhận từ trước.

## Đã làm
- Viết `src/engine/price-lock.ts`: `evaluatePriceLock({baseline, thresholdPct,
  replacement, lastLotPrice}) → {deviationPct, isLocked, pricingPrice,
  stalenessWarning}` — 1 hàm DÙNG CHUNG cho compound (USD) và ren kim loại
  (VND, sẽ dùng ở M6), vì công thức ADR-004 không phụ thuộc đơn vị tiền, chỉ tỷ
  lệ. Test `tests/unit/price-lock.test.ts` chạy đủ 5 kịch bản
  `price-lock-scenarios.json` (khóa/mở khóa 2 chiều + cảnh báo staleness).
- Viết `src/engine/dual-costing.ts`: `weightedAvgUsdPerKg()`,
  `totalInventoryKg()`, `holdingGainLossVnd()`, `provisionWarning()`. Test
  `tests/unit/dual-costing.test.ts` khớp tuyệt đối kịch bản "kho 2 đợt" trong
  skill `excel-parity-testing` (100t@3,03 + 50t@3,5, tái tạo 3,5 → bình quân
  3,1867, lãi giữ kho = 1.332.685.000đ) + 2 test biên (không lô nào → null,
  tái tạo < bình quân → cảnh báo VAS 02).
- **Nối dây** (`tests/parity/price-lock-integration.test.ts`): với MỖI kịch
  bản ADR-004, gọi `evaluatePriceLock()` rồi feed `pricingPrice` thẳng vào
  `calculatePipeCostAtNormalCapacity()` (M2, KHÔNG sửa signature) — verify
  `fullCostPerKg` khớp cả 5 kịch bản, kể cả 2 kịch bản MỞ KHÓA quan trọng nhất
  (replacement 3,50 → 121.012; replacement 2,80 → 98.958) — đây là bằng chứng
  mạnh nhất cho tới giờ rằng cơ chế khóa giá hoạt động đúng xuyên suốt chuỗi
  tính giá, không chỉ đúng ở hàm `evaluatePriceLock()` đơn lẻ.
- `npm run typecheck` sạch, `npm test` 43/43 xanh (28 cũ + 5 price-lock + 5
  dual-costing + 5 tích hợp).
- Cập nhật `docs/PHASE3_PLAN.md` (M5 `[x]`, hướng dẫn chi tiết M6 — dòng ren
  kim loại ADR-008, tái dùng `evaluatePriceLock`/`holdingGainLossVnd` đã có,
  nhắc lại cảnh báo chuẩn hóa `thresholdPct`), `docs/CONTEXT_PASTE.md`,
  `docs/CHANGELOG.md` (mục v1.12).

## Còn treo / việc phiên sau
- **M6 tiếp theo**: `src/engine/metal-insert.ts` (ADR-008) — công thức
  `materialCostPerUnit` mở rộng cho 11 SKU họ "Nối ren trong/ngoài", tái dùng
  `evaluatePriceLock()` (đã có) với `thresholdPct` CHUẨN HÓA từ
  `metal-insert.json` (chia 100). Fixture gốc (`fitting.json.skus`) có
  `brassInsertCost=0` cho toàn bộ 91 SKU (Excel chưa tích hợp ADR-008) — M6
  không có "số vàng Excel" để đối chiếu trực tiếp phần này, phải tự verify
  bằng cách tính tay + so khớp hợp lý. Chi tiết đầy đủ ở `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 8 cùng ngày — Pha 3 M6: dòng vật liệu ren kim loại (ADR-008)
Pha hiện tại: 3 (Code) — M6/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới.
- User xác nhận hiểu đúng nghiệp vụ trước khi code (hỏi lại 1 câu chốt logic):
  ren kim loại là 1 con ren đồng thau ép vào đúng đầu ren của thân nhựa CPVC —
  đúng cỡ ren (PT size), không phải theo SKU. Xác nhận khớp thiết kế đã có ở
  `docs/contracts/pricing-chain.md`/`product.md` (Pha 2), không cần sửa gì.

## Đã làm
- Viết `src/engine/metal-insert.ts`: `materialCostPerUnitWithInsert()` (gập
  chi phí ren vào `materialCostPerUnit`, thay cho hằng số tĩnh `brassInsertCost`
  cộng riêng ở `breakEvenPerUnit` như thiết kế cũ), `weightedAvgInsertPriceVnd()`,
  `metalInsertHoldingGainLossVnd()` (bản KHÔNG quy đổi ngoại tệ — quyết định
  viết hàm riêng thay vì gọi `dual-costing.ts:holdingGainLossVnd()` với tham số
  giả rate=0/1 để vô hiệu hóa quy đổi, vì hack đó làm code khó đọc/dễ hiểu sai).
  `evaluatePriceLock()` (M5) DÙNG LẠI NGUYÊN cho cả 2 policy (compound USD, ren
  kim loại VND) — không viết price-lock riêng.
- **PHÁT HIỆN QUAN TRỌNG khi chuẩn bị test**: kiểm tra lại
  `fitting.json.skus[].brassInsertCost` trước khi viết test (đúng thói quen
  "verify trước khi tin số liệu") — phát hiện field này KHÔNG phải 0 cho toàn
  bộ 91 SKU như `BUSINESS_MODEL.md`/`ADR-008` ghi nhận trước đó, mà 18 SKU họ
  "ren" đã có giá trị thật (khớp tuyệt đối `metal-insert.json`) — có thể do
  fixture được cập nhật ở Phiên 10-13 (nạp số liệu ren thật) nhưng phần mô tả
  trong BUSINESS_MODEL/ADR-008 không được đồng bộ lại câu chữ. Tận dụng phát
  hiện này: dùng `materialCostPerUnit + brassInsertCost` (fixture) làm SỐ VÀNG
  TỰ-ĐỐI-CHIẾU thay vì phải tự tính tay không kiểm chứng được — khớp tuyệt đối
  cả 11/11 SKU ren có khuôn thật.
- Viết `tests/parity/metal-insert.test.ts` (14 test): 11 test theo từng SKU họ
  ren (khớp `materialCostPerUnitWithInsert()` với số vàng tự-đối-chiếu) + 3 test
  dual-costing (`weightedAvgInsertPriceVnd` khớp fixture, `holdingGainLoss=0` ở
  trạng thái hiện tại, xác nhận KHÔNG quy đổi ngoại tệ).
- `npm run typecheck` sạch, `npm test` 57/57 xanh.
- Cập nhật `docs/PHASE3_PLAN.md` (M6 `[x]`, ghi rõ phát hiện brassInsertCost,
  hướng dẫn chi tiết M7 — thang giá 5 bậc + chuỗi markup, cảnh báo lại 2 lỗi
  công thức cũ ở bậc 2/4), rút gọn khối tóm tắt M1-M6 trong
  `docs/CONTEXT_PASTE.md` (đang phình to, trỏ sang PHASE3_PLAN.md thay vì lặp
  lại chi tiết), `docs/CHANGELOG.md` (mục v1.13).

## Còn treo / việc phiên sau
- **7 SKU Cút/Tê ren trong** (`pending_mold`): `brassInsertCost` trong fixture
  là số kế thừa từ Excel gốc theo pattern PT-size, CHƯA xác nhận theo ADR-008
  "stillOpen" — không dùng làm chính thức, nhưng vô hại vì SKU đã bị ẩn khỏi
  danh mục vận hành. Khi 7 SKU này vào sản xuất thật, xác nhận lại giá.
- **M7 tiếp theo**: `src/engine/price-ladder.ts` — thang giá 5 bậc + chuỗi
  markup 99 dòng (8 ống + 91 phụ kiện), gom toàn bộ M2-M6. Chi tiết đầy đủ ở
  `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 9 cùng ngày — Pha 3 M7+M8: thang giá 5 bậc + CVP (đảo thứ tự làm M8 trước)
Pha hiện tại: 3 (Code) — M7,M8/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới.
- User yêu cầu KIỂM TRA lại kỹ công thức bậc 2/4 (đã cảnh báo 2 lỗi cũ ở
  prototype Pha 1) TRƯỚC khi cho phép code tiếp — tuân thủ đúng yêu cầu, tính
  tay đối chiếu `dashboard.json` bằng script Python độc lập trước khi viết bất
  kỳ dòng code nào.
- **Quyết định kỹ thuật phát sinh khi verify**: đảo thứ tự M7 (price-ladder) và
  M8 (CVP) — phát hiện bậc 1 thang giá (`variableCostFloor`) CHÍNH LÀ
  `cvp.variableCostPerKg`, nên làm CVP trước để price-ladder tái dùng, tránh
  tính trùng công thức.

## Đã làm
- Verify tay bằng script Python độc lập (không phải TypeScript, để tránh thiên
  vị nếu công thức TS sai giống hệt kiểu sai cũ): tính lại bậc 2 (CHỈ
  compliance+rent qua `sharedCostAllocationRatio`, loại hẳn phần khấu hao
  lab/UL) và bậc 4 (revenueShare = capacity×vfPrice mỗi dòng / tổng 2 dòng,
  KHÔNG chia theo kg) cho cả Ống và Phụ kiện — khớp tuyệt đối cả 4 con số với
  `dashboard.json` TRƯỚC khi viết `src/engine/*.ts`.
- Viết `src/engine/cvp.ts`: `calculatePipeCvp()`/`calculateFittingCvp()`, nhận
  thẳng output đã có sẵn từ `pipe.ts`/`fitting.ts` (M2/M3) làm input (không
  tính lại `materialPerKgFinished`, `vfPricePerKg`... từ đầu). Test
  `tests/parity/cvp.test.ts` (8 test) khớp tuyệt đối `{pipe,fitting}.json.cvp`.
- Viết `src/engine/price-ladder.ts`: `calculatePipePriceLadder5Tier()`/
  `calculateFittingPriceLadder5Tier()` (đúng công thức đã verify tay ở trên —
  bậc 2 và bậc 4 tách biệt, không lặp lại 2 bug cũ), `calculatePipeSkuPriceChain()`/
  `calculateFittingSkuPriceChain()` (chuỗi markup + ROUNDUP hàng trăm cho giá
  niêm yết). Thiết kế bỏ HẲN tham số `brassInsertCost` cộng riêng ở
  `calculateFittingSkuPriceChain()` — vì ADR-008 (M6) đã gập chi phí ren vào
  `materialCostPerUnit` ngay từ đầu, giữ tham số cũ sẽ tạo 2 đường cộng trùng.
- Viết `tests/parity/price-ladder.test.ts` (101 test): 2 test thang giá 5 bậc
  (khớp `dashboard.json` cho cả Ống và Phụ kiện) + 8 test bảng giá Ống theo DN
  (khớp `pipe.json.priceLadderByDN`) + 91 test bảng giá SKU Phụ kiện (khớp
  `fitting.json.skus`, trong đó 11 SKU họ ren gọi `materialCostPerUnitWithInsert()`
  từ M6 trước khi tính chuỗi giá). Phát hiện + xử lý 2 vấn đề khi chạy test lần
  đầu: (1) sai số float khi so `toBe` cho `listPriceWithVat` (phép nhân
  `×(1+vatOutputRate)` không tròn số) — đổi sang `toBeCloseTo`, KHÔNG phải lỗi
  công thức; (2) 7 SKU `pending_mold` (Cút/Tê ren trong) có `brassInsertCost`
  trong fixture nhưng đây là số CHƯA XÁC NHẬN (ADR-008 "stillOpen", phát hiện ở
  M6) — sửa test dùng `mold-assets.json.skusWithoutMold` để KHÔNG cộng số này
  khi so sánh, đúng theo thiết kế engine (chỉ dùng số liệu đã xác nhận).
- `npm run typecheck` sạch, `npm test` 162/162 xanh.
- Cập nhật `docs/PHASE3_PLAN.md` (M7 VÀ M8 `[x]`, ghi rõ lý do đảo thứ tự,
  hướng dẫn chi tiết M9 — Plan_SX, cảnh báo KHÔNG có số vàng Excel cho phần
  này), `docs/CONTEXT_PASTE.md`, `docs/CHANGELOG.md` (mục v1.14).

## Còn treo / việc phiên sau
- **M9 tiếp theo**: `src/engine/plan.ts` (Plan_SX, T1 — tầng vận hành, dạng
  đóng, BUSINESS_MODEL §6). KHÁC mọi milestone trước: sheet Excel gốc là
  template input=0, KHÔNG có số vàng thật — phải viết test bằng input mẫu tự
  chọn, tính tay verify, ghi rõ đây không phải parity Excel thật. Chi tiết đầy
  đủ ở `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 10 cùng ngày — Pha 3 M9: Plan_SX (T1, BUSINESS_MODEL §6)
Pha hiện tại: 3 (Code) — M9/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới.
- **Sửa lỗi thiếu sót schema** (không phải ADR): `PlanInputSchema` (đóng băng
  Pha 2) thiếu `periodMonths` (hệ số kỳ, §6.2 luôn cần) và
  `currentLaborHeadcount` (§6.5 cần, nhân công hiện có không nằm ở đâu trong
  schema). Bổ sung cả 2 vào `src/schemas/scenario.ts` + `docs/contracts/scenario.md`.
- **2 giả định thiết kế** (không có Excel xác nhận vì Plan_SX gốc là template
  input=0, ghi rõ trong comment `plan.ts`): số máy cần thêm khi thiếu cả 3 ca
  coi công suất 3-ca hiện tại là 1 đơn vị công suất (ROUNDUP số đơn vị cần
  thêm); `moldSetCountBySizeDN` nhận làm cross-ref input thay vì tự suy từ
  `moldAssets`+`products` bên trong `plan.ts` (join phức tạp, để tầng
  orchestration cấp — giữ `plan.ts` thuần).

## Đã làm
- Đọc lại `docs/BUSINESS_MODEL.md` §6 (6 quy tắc) trước khi code.
- Viết `src/engine/plan.ts`: `calculatePlan()` — quy đổi kế hoạch Ống (mét→kg
  thành phẩm→kg nạp máy→giờ máy đùn) và Phụ kiện (số lượng→giờ máy qua
  `calculateMachineHoursPerUnit()` tái dùng từ `price-ladder.ts`→kg thành
  phẩm), đánh giá bậc ca (1/2/3, so với giờ khả dụng theo `periodMonths/12`),
  ràng buộc khuôn theo size DN (chỉ Phụ kiện), nguyên liệu+ngoại tệ cần (RAW,
  không qua price-lock — khác MỌI hàm trước dùng `pricingPrice`), nhân công
  cần tuyển (MAX(0, ...)), chi phí/kg thực tế so công suất nhàn rỗi (chỉ Ống,
  null nếu chưa có kế hoạch — tránh chia 0).
- **Verify TRƯỚC khi viết assertion** (đúng thói quen đã lặp lại suốt phiên):
  viết script Python độc lập tính tay 2 kịch bản mẫu (A: kế hoạch vừa công
  suất — Ống DN50 50.000m + Phụ kiện Tê đều 20 × 20.000 cái/quý; B: Phụ kiện
  1.000.000 cái — cố tình vượt xa công suất) TRƯỚC khi viết `tests/parity/plan.test.ts`,
  đối chiếu số Python với số TypeScript sau khi code — khớp tuyệt đối cả 2
  kịch bản (bao gồm nhánh `insufficient`/`extraMachinesNeeded=1` và cảnh báo
  khuôn `extraMoldSetsNeeded=3`).
- Ghi rõ trong comment đầu file `plan.ts` VÀ đầu file test: đây là input mẫu
  tính tay, KHÔNG phải số vàng Excel — khác biệt quan trọng so với mọi test
  parity trước đó trong repo, để phiên sau không nhầm lẫn khi đọc lại.
- `npm run typecheck` sạch, `npm test` 171/171 xanh.
- Cập nhật `docs/PHASE3_PLAN.md` (M9 `[x]`, hướng dẫn chi tiết M10 — inverse
  solver T2/T3), `docs/CONTEXT_PASTE.md`, `docs/CHANGELOG.md` (mục v1.15).

## Còn treo / việc phiên sau
- **M10 tiếp theo**: `src/engine/solver.ts` (ADR-005/006, skill
  `inverse-solver`) — bisection thuần cho T3, dạng đóng cho T2. Milestone cuối
  của engine lõi (M11 sau đó là gom parity suite tổng hợp, M12 là UI thật/
  Firestore, ngoài phạm vi hiện tại trừ khi user mở rộng). Chi tiết đầy đủ ở
  `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.


---

# Phiên 11 cùng ngày — Đóng gói tri thức cuối ngày cho phiên mới
Pha hiện tại: 3 (Code) — 9/12 milestone xong (M1-M9) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới — phiên rà soát/chốt sổ theo yêu cầu user,
  cùng thông lệ đã thiết lập ở kit v1.5 (2026-07-05, Phiên 14).

## Đã làm
- Rà soát tính nhất quán 4 file điều hướng: `docs/CONTEXT_PASTE.md` (thêm dòng
  tóm tắt tiến độ "9/12 milestone xong, tiếp theo M10" ngay đầu khối trạng
  thái), `docs/PHASE3_PLAN.md` (bảng M1-M9 đã đúng `[x]`, con trỏ M10 chính
  xác — không cần sửa), `docs/GLOSSARY.md` (không có thuật ngữ UI mới phát
  sinh ở M1-M9 — field mới như `periodMonths`/`moldSetCountBySizeDN` là nội bộ
  engine, chưa lên UI nên chưa cần thuật ngữ tiếng Việt), `AGENTS.md` (thứ tự
  đọc file đầu phiên vẫn đúng vì `CONTEXT_PASTE.md` tự trỏ sang
  `PHASE3_PLAN.md`, không cần sửa).
- Xác nhận `git status` sạch, mọi commit đã push lên `claude/nifty-dirac-wuamy6`
  (không có gì dở dang giữa các milestone).
- Chạy lại `npm run typecheck` + `npm test` lần cuối trong ngày — 171/171 xanh.
- Ghi `docs/CHANGELOG.md` (mục v1.16) tổng hợp bảng rà soát.

## Còn treo / việc phiên sau
- **M10 tiếp theo**: `src/engine/solver.ts` (inverse solver T2/T3, ADR-005/006)
  — milestone cuối của engine lõi trước M11 (gom parity suite tổng hợp) và M12
  (UI thật/Firestore, ngoài phạm vi hiện tại trừ khi user mở rộng). Đọc
  `docs/PHASE3_PLAN.md` mục "Việc tiếp theo ngay khi phiên sau vào" để có đầy
  đủ chi tiết công thức T2 (dạng đóng)/T3 (bisection) + test bắt buộc.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 12 — M10: Inverse solver (T2/T3)
Pha hiện tại: 3 (Code) — 10/12 milestone xong (M1-M10) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới nghiệp vụ.
- **Sửa lỗi thiếu sót schema** (theo đúng quy trình ADR-009 đã đặt ra — ghi
  thẳng vào bảng ADR-009 thay vì chỉ code comment): `SolveParams` (`scenario.ts`,
  đóng băng Pha 2) thiếu field `baseInput: ScenarioInput` — `forwardFn` cần 1
  input gốc để `solve()` clone rồi set giá trị dò vào theo `freeVarPath`, bản
  đóng băng chỉ liệt kê forwardFn/freeVarPath/targetSelector/target/bounds/tol.
  Bổ sung vào `src/schemas/scenario.ts` + `docs/contracts/scenario.md` + bảng
  ADR-009 (dòng #4).
- **Quyết định thiết kế nhỏ** (không phải kiến trúc nghiệp vụ, ghi comment đầu
  `solver.ts`): viết `solve()`/`solveDiscrete()` GENERIC `<TInput, TOutput>`
  thay vì khoá cứng `ScenarioInput`/`ScenarioOutput`, vì chưa có hàm
  orchestration `calculateScenario()` nối toàn bộ pipe/fitting/cvp/price-ladder/
  price-lock/dual-costing/metal-insert thành 1 `ScenarioOutput` hoàn chỉnh
  (thuộc phạm vi M11/M12) — solve() hoạt động với BẤT KỲ input/output pure
  function nào, đúng tinh thần mọi module engine khác trong repo (pipe.ts
  không phụ thuộc ScenarioInput).

## Đã làm
- Đọc `docs/PHASE3_PLAN.md` (mục "Việc tiếp theo ngay") + skill `inverse-solver`
  + `docs/contracts/scenario.md` §4 trước khi code.
- Viết `src/engine/solver.ts`:
  - `solve()` — bisection thuần cho biến LIÊN TỤC: kiểm tra `f(lo)`/`f(hi)` kẹp
    target trước khi chia đôi (luật #3 skill) → không kẹp thì trả
    `{feasible:false, reason, achievableRange}`; forward-verify nghiệm cuối
    (luật #4). Hỗ trợ cả hàm tăng dần lẫn giảm dần theo biến dò.
  - `solveDiscrete()` — quét rời rạc cho biến NGUYÊN (`shifts` 1-3), KHÔNG
    bisection liên tục (luật #5).
  - `solveTargetProfit()` — T2 dạng đóng (`Q = (fixedCostPerYear +
    targetProfitVnd) / contributionMarginPerKg`), tái dùng thẳng số ra từ
    `cvp.ts` (M8), KHÔNG tính lại — đúng skill mục 2 (đây CHÍNH LÀ forward CVP,
    không phải ngoại lệ luật cấm công thức ngược).
- Test:
  - `tests/unit/solver.test.ts` (5 test) — cơ chế bisection/quét rời rạc/
    infeasible trên hàm số học đơn giản, độc lập nghiệp vụ.
  - `tests/parity/solver.test.ts` (5 test) — trên forward function THẬT
    (`pipe.ts` + `price-ladder.ts`) + fixture v3.4:
    - Round-trip: bắt đầu XA giá gốc (compound=0), `solve()` hội tụ đúng
      `compoundReplacementPriceUsdPerKg=3.03` và `|residual| < 1e-6`.
    - **Case chuẩn T3 (skill inverse-solver)**: "giá niêm yết DN50 mục tiêu
      260.000đ/m — huy động phụ kiện không đổi, hỏi giá compound tối đa được
      phép" → nghiệm ≈2,48 USD/kg, forward-verify khớp TUYỆT ĐỐI
      `listPriceBeforeVat=260000` (đã tính tay bằng Python trước để xác nhận
      hàm bậc-thang do `roundUpToHundred` vẫn hội tụ đúng vì đơn điệu không
      giảm — không phải lỗi công thức).
    - Case infeasible: mục tiêu 10.000đ/m (dưới cả `listPriceBeforeVat` khi
      giá compound=0, tức dưới sàn biến phí bậc 1) → `feasible:false` kèm
      `achievableRange`, không trả số bừa.
    - T2: `solveTargetProfit(targetProfitVnd=0)` khớp TUYỆT ĐỐI
      `pipe.json.cvp.breakEvenKgYear=107039.627408882` (Q hòa vốn không lợi
      nhuận chính là breakEvenKgYear — cùng công thức) + case vượt công suất
      3 ca → `feasibleWithinNormalCapacity=false`.
- `npm run typecheck` sạch (phải sửa `noUncheckedIndexedAccess` ở
  `setAtPath()` — `keys[i]` là `string | undefined`), `npm test` 185/185 xanh.
- Cập nhật `docs/PHASE3_PLAN.md` (M10 `[x]`, nhật ký milestone, con trỏ M11),
  `docs/CONTEXT_PASTE.md` (10/12, tên branch `claude/pensive-gates-4e0jmo`),
  `docs/decisions/ADR-009-schema-completion-fields.md` (dòng #4).

## Còn treo / việc phiên sau
- **M11 tiếp theo**: gom M2-M9 thành 1 bộ test parity Excel đầy đủ 372
  assertion trong `tests/parity/` (đối chiếu skill `excel-parity-testing`) —
  KHÔNG viết engine mới, chỉ tổng hợp/audit lại toàn bộ `tests/fixtures/*.json`
  trong 1 chỗ.
- M12 (UI thật + Firestore) — CHỈ làm khi user xác nhận mở rộng phạm vi ngoài
  "chỉ engine".
- Orchestrator `calculateScenario(input: ScenarioInput): ScenarioOutput` nối
  toàn bộ engine CHƯA tồn tại — cần thiết cho M12 (và để `solve()` dùng đúng
  kiểu cụ thể `ScenarioInput`/`ScenarioOutput` thay vì generic tạm thời).

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 13 — M11 (audit fixture coverage) + đổi quy trình push
Pha hiện tại: 3 (Code) — 11/12 milestone xong (M1-M11), ENGINE LÕI HOÀN THÀNH | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới.
- **Thay đổi quy trình git theo yêu cầu user**: từ nay push thẳng vào nhánh
  mặc định của repo (`claude/project-knowledge-setup-2au4hr` — repo này KHÔNG
  có `main`, nhánh này đóng vai trò main vì là HEAD branch của remote), KHÔNG
  tạo Pull Request nữa. Đã push fast-forward commit M10 (523048c) + M11 vào
  nhánh này trong phiên này.
- **Chốt phạm vi "làm hết Pha 3"** (user yêu cầu mơ hồ, đã hỏi lại 1 câu theo
  đúng luật CLAUDE.md): user chọn CHỈ làm M11 (audit/gom test parity), KHÔNG
  làm M12 (UI thật + Firestore) trong phiên này — M12 vẫn giữ nguyên điều
  kiện gate "chỉ làm khi user xác nhận mở rộng phạm vi" từ `PHASE3_PLAN.md`.

## Đã làm
- Xác nhận nhánh mặc định remote qua `git remote show origin` (`HEAD branch:
  claude/project-knowledge-setup-2au4hr`) — KHÔNG có `refs/heads/main` nào
  trong repo. Fast-forward push `claude/pensive-gates-4e0jmo` (chứa M10) vào
  nhánh này.
- Rà soát TOÀN BỘ `tests/fixtures/*.json` xem file nào chưa có test nào tham
  chiếu (`grep -rl` từng tên file qua `tests/`) — phát hiện `price-list.json`
  (bảng phẳng 99 dòng, sheet "PriceList", README.md đã mô tả) là fixture DUY
  NHẤT không có test nào chạm tới; mọi test khác (M2-M10) đối chiếu qua
  `pipe.json`/`fitting.json` trung gian, chưa từng khớp trực tiếp với bảng giá
  CUỐI CÙNG này — đúng đúng fixture mà AGENTS.md luật #5 gọi tên làm số vàng
  ("8 giá ống + 91 SKU").
- Verify TRƯỚC khi viết test (đúng thói quen cả phiên): viết script Python đối
  chiếu tay thứ tự 99 dòng `price-list.json` với `pipe.json.priceLadderByDN`
  (8 dòng đầu) + `fitting.json.skus` (91 dòng sau) theo VỊ TRÍ — khớp 100% cả
  tên sản phẩm/size lẫn `priceBeforeVat`, kể cả 8 SKU `pending_mold`.
- Viết `tests/parity/price-list-snapshot.test.ts` (93 test): đối chiếu ĐỘC LẬP
  `listPriceBeforeVat`/`listPriceWithVat` tính từ engine với `price-list.json`
  cho 8 dòng Ống + 83/91 dòng Phụ kiện — 8 SKU `pending_mold` CỐ TÌNH bỏ qua so
  giá trị (chỉ giữ test khớp tên+size), vì `brassInsertCost` của nhóm này là số
  CHƯA XÁC NHẬN (ADR-008 "stillOpen"), engine không cộng nên lệch fixture theo
  đúng thiết kế đã áp dụng từ M7 (`price-ladder.test.ts`).
  Không phát hiện lệch nào ngoài dự kiến — `npm test` 278/278 xanh (185 → 278,
  +93 test mới), `npm run typecheck` sạch.
- Xác nhận: sau M11, MỌI fixture trong `tests/fixtures/*.json` đều có ít nhất
  1 test tham chiếu (không còn "hố đen" chưa kiểm chứng).
- Ghi chú trong `PHASE3_PLAN.md`: con số "372 assertion" ở AGENTS.md là ước
  lượng đặt ra từ Pha 0 (trước khi có engine thật) — tổng `expect()` thực thi
  hiện tại (tính cả vòng lặp 8+91 dòng) đã vượt xa mốc này; mục tiêu thật của
  M11 là "phủ hết mọi fixture vàng", không phải khớp đúng 1 con số cụ thể.
- Cập nhật `docs/PHASE3_PLAN.md` (M11 `[x]`, nhật ký, con trỏ M12 — CHỜ user
  xác nhận), `docs/CONTEXT_PASTE.md` (11/12, engine lõi hoàn thành, ghi rõ quy
  trình push mới + tên nhánh mặc định).
- Push fast-forward trực tiếp vào `claude/project-knowledge-setup-2au4hr`,
  KHÔNG tạo Pull Request (theo quyết định mới ở trên).

## Còn treo / việc phiên sau
- **M12 (UI thật + Firestore)** — CHỈ bắt đầu khi user xác nhận mở rộng phạm
  vi rõ ràng (KHÔNG tự suy diễn từ "làm hết Pha 3" — đã hỏi lại và user chọn
  dừng ở M11). Khi bắt đầu: đọc `prototype/blazemaster-costing-app.dc.html` +
  `docs/contracts/scenario.md` §5-6; cần viết thêm orchestrator
  `calculateScenario(input: ScenarioInput): ScenarioOutput` (chưa tồn tại) làm
  cầu nối UI ↔ engine, và lúc đó `solver.ts` nên được gọi lại với kiểu cụ thể
  `ScenarioInput`/`ScenarioOutput` thay vì generic tạm thời.
- Từ phiên sau: mọi lần push coi nhánh `claude/project-knowledge-setup-2au4hr`
  là đích chính (không tạo nhánh `claude/<tên-ngẫu-nhiên>` riêng rồi mở PR nữa,
  trừ khi môi trường phiên bắt buộc tạo nhánh làm việc riêng trước — lúc đó vẫn
  merge/push thẳng vào nhánh mặc định khi xong thay vì mở PR).

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 14 — Artifact "chi phí sản xuất/kg" (khảo sát, KHÔNG đổi code repo)
Pha hiện tại: 3 (Code) — vẫn 11/12 milestone (M1-M11), M12 vẫn CHỜ xác nhận | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc/schema nào. Phiên này thuần khảo sát + dựng 1
  Artifact minh hoạ (Claude Artifact, KHÔNG phải file trong repo) theo yêu cầu
  user "cần xem ngay chi phí sản xuất/kg dạng dễ nhìn kiểu Excel" trước khi bàn
  tiếp M12.

## Đã làm
- Viết script `tsx` tạm (`/tmp/.../scratchpad/dump-cost.mjs`, KHÔNG commit vào
  repo) chạy TRỰC TIẾP `src/engine/{pipe,fitting,cvp,price-ladder}.ts` với
  đúng fixture v3.4 → xuất số thật (không gõ tay) để nhúng vào Artifact.
- Dựng Artifact HTML kiểu "sổ Excel" (2 cột Ống/Phụ kiện, data-bar kiểu
  conditional formatting, thang giá 5 bậc màu theo đúng quy ước đã có ở
  `prototype/blazemaster-costing-app.dc.html`, hỗ trợ light/dark).
- User phản hồi 2 vòng, đã chỉnh theo:
  1. "Chi phí sản xuất/kg" phải là **chưa gồm nguyên liệu** — tách headline
     chính thành `unitProcessingCostPerKg`/`processingCostPerKgRef` (loại
     nguyên liệu + bao bì), thêm thanh tỷ lệ Nguyên vật liệu/Chi phí sản xuất
     + gom bảng breakdown thành 2 khối rõ ràng. "Giá thành đầy đủ" (có nguyên
     liệu) lùi xuống làm số phụ.
  2. User nghi ngờ số Phụ kiện (30.138 đ/kg quy đổi) quá lớn — khảo sát và
     XÁC NHẬN KHÔNG PHẢI LỖI: (a) 66 khuôn trị giá 6,54 tỷ đồng, khấu hao 5
     năm/khuôn = 1,31 tỷ/năm — verify bằng script Python tính tay từ
     `mold-assets.json`, khớp đúng `moldDepreciationPerYear` engine; (b) dây
     ép phun đang lấy "công suất bình thường" = 1 ca × 60% huy động (1.968 giờ
     máy/năm) — THẤP hơn Ống (3 ca, 4.920 giờ/năm) ~7 lần, đúng theo
     `BUSINESS_MODEL.md` §3.3 và đúng số vàng AGENTS.md đã ghi ("MHR =
     1.344.176, 1 ca × 60%") — không phải giá trị tự chọn/sai; (c) dẫn
     `fitting.json.mhrByCapacityTier` (bảng 6 kịch bản huy động — CHƯA có test
     engine riêng, phát hiện qua `grep`) cho thấy MHR giảm ~66% nếu chạy 3 ca
     85%; (d) minh hoạ bằng 1 SKU thật (Tê đều 20, 55g/cái) để chỉ rõ
     "30.138 đ/kg quy đổi" KHÔNG dùng để suy ra giá 1 CÁI bằng phép nhân trọng
     lượng — SKU thật định giá qua MHR × giờ máy/đơn vị (giá trị khác hẳn phép
     nhân đơn giản).
- **Phát hiện (chưa xử lý, không phải milestone)**: `fitting.json.mhrByCapacityTier`
  (bảng độ nhạy 6 kịch bản ca/huy động, nguồn Excel thật) là fixture DUY NHẤT
  còn lại KHÔNG có test engine đối chiếu — khác diện với `price-list.json` đã
  xử lý ở M11 vì đây là bảng "what-if" ngoài phạm vi costing chuẩn (M1-M11 chỉ
  làm đúng 1 kịch bản chuẩn 1ca×60%), không tính vào nợ kỹ thuật của M11.
- KHÔNG có thay đổi nào trong repo (`git status` sạch trước/sau phiên) — toàn
  bộ script + Artifact nằm ở scratchpad phiên, không commit.

## Còn treo / việc phiên sau
- User đã đồng ý số liệu đúng, còn treo 1 việc nhỏ CHƯA làm (do user nói "ok
  phiên kế tiếp" trước khi tôi kịp làm): **thêm ghi chú giải thích "vì sao
  30.138đ/kg cao" (kèm rút gọn bảng độ nhạy 6 kịch bản) trực tiếp vào Artifact**
  — nếu phiên sau user nhắc lại, làm ngay không cần hỏi lại phạm vi.
- M12 (UI thật + Firestore) vẫn CHỜ xác nhận rõ ràng — chưa đổi gì so với
  Phiên 13.
- Nếu sau này UI thật (M12) cần hiển thị/đổi kịch bản huy động ca cho Phụ
  kiện (giống bảng độ nhạy 6 kịch bản), lúc đó cần viết test engine đối chiếu
  `mhrByCapacityTier` (hiện chưa có) trước khi tin số.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 15 — User xác nhận mở rộng phạm vi: bắt đầu M12 (UI thật + Firestore)
Pha hiện tại: 3 (Code) — engine lõi M1-M11 xong, M12 ĐANG LÀM (M12.1, M12.2 xong) | Agent: Claude Code

## Đã quyết
- **User xác nhận mở rộng phạm vi** ("hoàn thiện nốt pha 3 đi còn m12 UI thật
  +firestore") — gỡ gate "M12 chờ xác nhận" đã đặt ở Phiên 13. M12 tự nó quá
  lớn cho 1 phiên (frontend thật + Firebase Auth/Firestore/Rules/Cloud
  Functions + nhiều màn hình) nên áp dụng ĐÚNG kỷ luật đã dùng cho M1-M11:
  viết `docs/M12_PLAN.md` chia M12 thành M12.1-M12.10, mỗi milestone tự chứa.
- **Hỏi lại 1 câu chốt phạm vi hạ tầng Firebase** (theo CLAUDE.md: yêu cầu mơ
  hồ → hỏi lại, không tự suy diễn): repo chưa có project Firebase thật (không
  `.firebaserc`/`firebase.json`). User chọn **dùng Firebase Local Emulator
  Suite trước** — code đầy đủ Auth/Firestore/Rules/Cloud Functions, test bằng
  emulator; khi có project thật chỉ cần dán config vào `.env`, không sửa code.
- Không có ADR nghiệp vụ mới — 2 bổ sung field (xem "Đã làm") là sửa lỗi
  thiếu sót theo đúng quy trình ADR-009 đã đặt ra, không phải quyết định kiến
  trúc/nghiệp vụ mới.

## Đã làm
- Viết `docs/M12_PLAN.md` (bảng M12.1-M12.10 + quyết định hạ tầng Emulator +
  "cách phiên mới bắt đầu"), trỏ từ `PHASE3_PLAN.md`.
- **M12.1 — Orchestrator `src/engine/scenario.ts`**: `calculateScenario()`
  nối pipe/fitting/cvp/price-ladder/price-lock/dual-costing/metal-insert theo
  đúng thứ tự phụ thuộc chéo 2 dòng SP. Đọc kỹ toàn bộ schema liên quan trước
  khi viết (resource.ts, cost-pool.ts, pricing-chain.ts, product.ts) để không
  đoán field.
  - Bổ sung field thiếu ở schema đã đóng băng (ghi ADR-009 dòng #5):
    `CompoundInventorySchema.replacementPriceUsdPerKg`,
    `MetalInsertCatalogEntrySchema.replacementPriceVnd` — `evaluatePriceLock()`
    cần "giá thị trường hiện hành" làm input nhưng schema gốc chỉ có lịch sử
    mua (`lots`) + chính sách khóa (`priceLock.baseline/thresholdPct`).
  - `bookCostPerKg` (ADR-002, sổ sách) tái dùng `landedCostPerKgVnd()` với
    `weightedAvgUsdPerKg` thay `pricingPrice` — CHƯA có số vàng Excel cho
    trường hợp lệch giá thật (kịch bản "kho 2 đợt" trong skill
    excel-parity-testing chỉ verify `holdingGainLossVnd`, không verify
    `bookCostPerKg`) — verify bằng tự-đối-chiếu (weightedAvg=pricingPrice ở
    kịch bản mặc định → phải khớp tuyệt đối `fullCostPerKg`).
  - **Phát hiện + sửa 2 lỗi có thật** khi lần đầu gọi
    `ScenarioOutputSchema.parse()` trên toàn bộ 99 SKU (chưa ai làm ở quy mô
    này trước M12): (1) `listPriceWithVat` là float do sai số nhân dấu phẩy
    động, vi phạm `z.number().int()` đã đóng băng — sửa `price-ladder.ts`
    làm tròn `Math.round()` (đ VND không có đơn vị lẻ, đúng ý nghĩa nghiệp
    vụ, không đổi số khớp fixture); (2) `tests/fixtures/mold-assets.json`
    gõ SAI ký tự cho "Cút 90°"/"Cút 45°" (dấu ĐỘ `°` U+00B0) thay vì đúng ký
    tự `fitting.json` dùng ("Cút 90º"/"Cút 45º", dấu CHỈ SỐ THỨ TỰ NAM TÍNH
    `º` U+00BA — 2 ký tự trông giống hệt nhau khi đọc bằng mắt) — khiến
    `managementStatusOf()` (viết từ M1, chưa từng được gọi thật tới M12)
    phân loại nhầm 11 SKU "Cút 90/45" thành `pending_mold`. Sửa 22 chỗ trong
    `mold-assets.json` (label + producesSkus.productName, 11 khuôn) về đúng
    ký tự nguồn `fitting.json` — xác nhận bằng script Python đối chiếu set
    tên sản phẩm 2 file TRƯỚC khi sửa, để chắc chắn không còn lệch nào khác.
  - Test `tests/parity/scenario.test.ts` (8 test): đối chiếu output với
    `dashboard.json`/`pipe.json`/`fitting.json`/`price-list.json` qua ĐÚNG 1
    cửa ngõ, xác nhận toàn bộ 91+8 SKU phân loại active/pending_mold đúng.
- **M12.2 — Scaffold frontend thật**: cài `react@18`/`react-dom@18` (ghim
  đúng bản 18 theo AGENTS.md — npm mặc định kéo về React 19, phải ghim lại),
  `vite@5` + `@vitejs/plugin-react@4` (ghim bản tương thích vite@5 vì
  vitest@2 đang dùng — bản mới nhất của plugin-react đòi vite@8, xung đột),
  `tailwindcss@3`, `recharts@3`. Thêm `vite.config.ts`, `tailwind.config.ts`,
  `postcss.config.js`, `index.html`, `src/main.tsx`, `src/App.tsx` (placeholder
  trung thực hiển thị đúng trạng thái M12.1-M12.10, KHÔNG giả lập dữ liệu —
  phân biệt rõ với `prototype/*.dc.html` là mockup Pha 1), `src/index.css`
  (Tailwind directives). Sửa `tsconfig.json` thêm `lib: DOM/DOM.Iterable` +
  `jsx: react-jsx` (dùng CHUNG 1 tsconfig cho engine Node-only lẫn frontend —
  không tách 2 file, vì engine không đụng DOM nên an toàn). Thêm script
  `dev`/`build`/`preview` vào `package.json` (trước đây `dev` là placeholder
  báo lỗi cố ý).
  **Verify thật** (đúng skill `run`/`verify`, không chỉ tin build xanh): chạy
  `npm run build` (thành công, Tailwind sinh CSS thật ~7KB) + khởi động
  `npm run dev`, dùng Playwright chụp màn hình thật ở `127.0.0.1:5183` — xác
  nhận React render đúng, Tailwind áp dụng đúng style, sau đó tắt dev server.
- `npm test` 286/286 xanh (278→286, +8 test scenario.ts), `npm run typecheck`
  sạch, `npm run build` chạy được.
- Cập nhật `docs/PHASE3_PLAN.md` (M12 → "ĐANG LÀM", trỏ `M12_PLAN.md`),
  `docs/CONTEXT_PASTE.md`, `docs/decisions/ADR-009-schema-completion-fields.md`
  (dòng #5).

## Còn treo / việc phiên sau
- **M12.3 tiếp theo**: Firebase Emulator Suite — `firebase.json` +
  `firestore.rules` (bảng phân quyền `scenario.md` §5-6, 4 vai × 6 vùng dữ
  liệu — sales KHÔNG BAO GIỜ thấy giá vốn) + `firestore.indexes.json` + rules
  unit test bằng `@firebase/rules-unit-testing`. Đọc `docs/M12_PLAN.md` mục
  "Cách phiên mới bắt đầu" trước khi code.
- M12.4-M12.10 (Cloud Function, 5 màn hình theo vai, security review) —
  chưa bắt đầu, xem bảng trạng thái đầy đủ ở `docs/M12_PLAN.md`.
- Khi có Firebase project thật: chỉ cần dán config vào `.env` (đã gitignore),
  không sửa code (thiết kế Emulator-first từ đầu đã tính việc này).

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 16 — Đóng gói tri thức cho phiên mới (checkpoint M12.3)
Pha hiện tại: 3 (Code) — M12.1+M12.2 xong, tiếp theo M12.3 | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới — phiên rà soát/chốt sổ theo yêu cầu user
  ("lưu lại tri thức đánh dấu các mission xong để phiên kế tiếp khởi tạo mới
  sẽ hiểu và làm tiếp"), cùng thông lệ "kit vX" đã thiết lập ở v1.5/v1.16.

## Đã làm
- Rà soát toàn bộ chuỗi file điều hướng đầu phiên: `docs/M12_PLAN.md`,
  `docs/CONTEXT_PASTE.md`, `docs/PHASE3_PLAN.md`, `docs/GLOSSARY.md`,
  `AGENTS.md`.
- **Phát hiện + vá 1 lỗ hổng quy trình**: `docs/M12_PLAN.md` (file DUY NHẤT
  phiên sau đọc để biết chính xác việc tiếp theo, theo đúng thiết kế đã lập ở
  Phiên 15) bị bỏ sót cập nhật — bảng trạng thái vẫn ghi M12.2 là `[ ]` dù đã
  làm xong ở Phiên 15 (CONTEXT_PASTE.md/session log ghi đúng, nhưng file theo
  dõi chính thức thì không) — đã tick `[x]` + bổ sung nhật ký M12.2 đầy đủ.
- Viết thêm mục "Việc tiếp theo ngay khi phiên sau vào" cho M12.3 trong
  `docs/M12_PLAN.md` — 6 bước cụ thể (cài `firebase-tools`/`@firebase/
  rules-unit-testing`, viết `firebase.json`/`firestore.rules` dịch đúng bảng
  phân quyền `scenario.md` §5-6, test bắt buộc mỗi vai×vùng dữ liệu, đặc biệt
  `sales` phải bị từ chối đọc `outputs/internal`/`outputs/targetCosting`) —
  để phiên sau không phải tự suy luận lại từ đầu, giống mức chi tiết
  `PHASE3_PLAN.md` đã làm cho M10 trước khi code.
- Xác nhận `docs/CONTEXT_PASTE.md`/`docs/PHASE3_PLAN.md` đã đúng từ Phiên 15,
  không cần sửa. `docs/GLOSSARY.md` không cần thuật ngữ mới (field
  `replacementPriceUsdPerKg`/`replacementPriceVnd` đã có sẵn `replacementCost`
  — Giá tái tạo — bao quát đúng khái niệm, giống cách v1.16 từng kết luận với
  `periodMonths`).
- Ghi `docs/CHANGELOG.md` v1.17-v1.20 (backfill M10/M11/M12.1+M12.2 chưa có
  entry, + v1.20 là checkpoint rà soát của chính phiên này).
- Chạy lại `npm run typecheck` + `npm test` + `npm run build` lần cuối — sạch
  cả 3, 286/286 xanh.

## Còn treo / việc phiên sau
- **M12.3 tiếp theo**: Firebase Emulator Suite + Security Rules — đọc
  `docs/M12_PLAN.md` mục "Việc tiếp theo ngay khi phiên sau vào" (đã viết chi
  tiết 6 bước trong phiên này), KHÔNG cần đọc lại session log để suy luận.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 17 — M12.3: Firebase Emulator Suite + Firestore Security Rules
Pha hiện tại: 3 (Code) — M12.1-M12.3 xong, tiếp theo M12.4 | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc/nghiệp vụ mới — hiện thực hóa đúng bảng phân
  quyền đã đóng băng ở `docs/contracts/scenario.md` §5-6.
- **Quyết định phạm vi field-lock (ghi rõ để không bị hiểu nhầm là thiếu sót)**:
  `scenarios/{id}.update` cho vai `pricing` CHỈ khóa đúng field liệt kê ở
  `resource.md`/`cost-pool.md` (đúng câu chữ scenario.md §5 dòng 1 trỏ tới 2
  nguồn này) + `pricing-chain.md` (`inventory.{pipe,fitting}.priceLock.thresholdPct`
  — không phải mảng nên khóa trực tiếp được). **CHƯA khóa** `products[]`
  (product.md: "chỉ admin ghi") và `inventory.metalInsert[].priceLock.thresholdPct`
  (mảng, rules không có vòng lặp để khóa field TỪNG phần tử mà không chặn luôn
  phần pricing được phép sửa như `lots`) — cần ADR/cập nhật `scenario.md` §5
  nếu muốn mở rộng, không tự quyết ở milestone hẹp này (AGENTS.md luật #5).

## Đã làm
- Cài `firebase-tools@15`, `firebase@12`, `@firebase/rules-unit-testing@5`
  (devDependency) — môi trường có sẵn Java 21 nên Firestore Emulator chạy
  thật, không cần mock.
- `firebase.json`: emulator `firestore` (8080) + `auth` (9099) + `ui`, chưa có
  `hosting`/`functions` (để dành M12.4). `firestore.indexes.json` rỗng.
- `firestore.rules`: dịch bảng `scenario.md` §5 (doc split) + §6 (4 vai × 6
  vùng dữ liệu) — role đọc từ `request.auth.token.role`; 1 khối `match` riêng
  cho mỗi doc path (`scenarios/{id}`, `outputs/internal`, `outputs/priceList`,
  `outputs/plan`, `planInputs/{period}`, `outputs/targetCosting`,
  `moldAssets/{moldId}`); mọi `outputs/*` chặn client ghi trực tiếp (đúng thiết
  kế "Cloud Function/Admin SDK ghi, bỏ qua rules"); mặc định từ chối path lạ.
  Hàm `scenarioLockedFieldsUnchanged()` field-lock cho pricing (chi tiết phạm
  vi ở trên).
- `tests/rules/firestore.rules.test.ts` (33 test, `@firebase/rules-unit-testing`)
  + `vitest.config.ts` (loại `tests/rules/**` khỏi `npm test` — trước đây chưa
  có file config này) + `vitest.rules.config.ts` (include riêng thư mục đó) +
  script `npm run test:rules` (`firebase emulators:exec --only firestore,auth
  "vitest run --config vitest.rules.config.ts"`).
- **Verify THẬT**: `npm run test:rules` chạy trên Firestore Emulator sống (tự
  tải `cloud-firestore-emulator-v1.21.0.jar` lần đầu) — 33/33 pass, bao phủ
  đúng yêu cầu bắt buộc (mỗi vai × mỗi vùng dữ liệu, đặc biệt `sales` bị từ
  chối đọc `outputs/internal` và `production` bị từ chối đọc
  `outputs/internal`/`outputs/targetCosting`/`scenarios/{id}`). `npm test`
  vẫn 286/286 (không đụng, `tests/rules` bị loại đúng ý). `npm run typecheck`
  sạch.
- `.gitignore` thêm artifact emulator (`firebase-debug.log`,
  `firestore-debug.log`, `ui-debug.log`, `.firebase/`).
- Cập nhật `docs/M12_PLAN.md` (M12.3 `[x]`, nhật ký chi tiết + con trỏ M12.4),
  `docs/CONTEXT_PASTE.md`. Sửa luôn 2 chỗ ghi CỨNG tên nhánh cũ
  `claude/project-knowledge-setup-2au4hr` (M12_PLAN.md bước 3 +
  CONTEXT_PASTE.md) thành mô tả chung "branch làm việc do hạ tầng phiên chỉ
  định" — phiên này chạy trên branch khác (`claude/firebase-emulator-security-
  rules-5eij5z`) do hạ tầng phiên cấp, tên cứng cũ đã sai/lỗi thời.

## Còn treo / việc phiên sau
- **M12.4 tiếp theo**: Cloud Function `onScenarioWrite` — trigger `onWrite`
  trên `scenarios/{id}`, chạy `calculateScenario()` (đã có, M12.1) bằng Admin
  SDK, ghi tách 4 doc con `outputs/*` đúng ranh giới đã khóa ở `firestore.rules`
  (M12.3). Cần thêm thư mục `functions/` (Node runtime riêng) + khai báo
  `functions` block trong `firebase.json`. Xem chi tiết ở `docs/M12_PLAN.md`
  mục "Việc tiếp theo ngay khi phiên sau vào".
- Field-lock còn treo cho `products[]` (product.md) và
  `inventory.metalInsert[].priceLock.thresholdPct` (pricing-chain.md) — xem
  "Đã quyết" ở trên, cần ADR/cập nhật scenario.md §5 nếu muốn khóa, không phải
  bug của M12.3.
- Cơ chế cấp/thu hồi Firebase Auth custom claim `role` vẫn chưa thiết kế — để
  dành security-review Pha 4 (M12.10), rules M12.3 giả định claim đã tồn tại.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 18 — M12.4: Cloud Function `onScenarioWrite` (lõi), ADR-010 tách phạm vi
Pha hiện tại: 3 (Code) — M12.1-M12.4 xong, tiếp theo M12.4b | Agent: Claude Code

## Đã quyết
- **ADR-010** (mới): tách M12.4 (dự kiến ban đầu "ghi tách 4 doc con" trong 1
  Cloud Function) thành 3 phần độc lập sau khi phát hiện 2 khoảng trống thiết
  kế ngay khi bắt tay code — không tự phát minh cách lấp, dừng lại ghi ADR:
  1. `outputs/plan` cần `calculatePlan()` (M9) nhưng `calculateScenario()`
     (M12.1) CHƯA orchestrate Plan_SX — thiếu hàm dẫn xuất
     `moldSetCountBySizeDN` từ `moldAssets`+`products` (comment gốc ở
     `plan.ts` từ M9 đã nói "để tầng orchestration cấp" nhưng tầng đó chưa
     từng làm việc này tới giờ).
  2. `outputs/targetCosting` T3 cần chọn 1 SKU cụ thể để dò giá nhưng
     `TargetPriceRequestSchema` (đóng băng Pha 2) không có trường này.
  → M12.4 (phần này) chỉ làm `onScenarioWrite` (outputs/internal+priceList —
  KHÔNG có khoảng trống). M12.4b (outputs/plan) và M12.4c (outputs/targetCosting)
  hoãn, lý do + hướng giải quyết ghi đầy đủ trong ADR-010.
- Không có quyết định nghiệp vụ nào khác.

## Đã làm
- Viết `docs/decisions/ADR-010-cloud-function-boundaries.md`.
- Scaffold `functions/` (Cloud Functions TS project riêng `package.json`):
  `firebase-functions@7` + `firebase-admin@13` (ghim `^13` — peer dep của
  `firebase-functions@7` chưa hỗ trợ `admin@14` mới nhất). `functions/tsconfig.json`
  dùng cùng kiểu module với root (`ESNext`/`Bundler`) để tái dùng thẳng
  `src/engine`/`src/schemas` qua `../../src/...` — verify tsc tự suy `rootDir`
  ra gốc repo, output lồng đúng (`functions/lib/functions/src/*.js` +
  `functions/lib/src/**/*.js`, cùng độ lệch tương đối nên import path compiled
  tự khớp) bằng cách chạy thử `node --experimental-vm-modules -e "import(...)"`
  trên file compiled, không chỉ tin `tsc` không báo lỗi.
- `firebase.json`: thêm block `functions` (source `functions/`, predeploy
  `npm run build`) + emulator `functions` port 5001. Thêm `.firebaserc`
  (project `demo-costing-app`) — bắt buộc để Firestore+Functions Emulator
  chung 1 project ID khi chạy `firebase emulators:exec` (khác `tests/rules/`
  — `@firebase/rules-unit-testing` không cần khớp project CLI).
- `functions/src/index.ts`: `onScenarioWrite` (`onDocumentWritten`
  `scenarios/{scenarioId}`) — parse `ScenarioInputSchema` → `calculateScenario()`
  (TÁI DÙNG NGUYÊN, M12.1) → parse `ScenarioOutputSchema` → ghi
  `outputs/internal` (đầy đủ) + `outputs/priceList` (lược còn
  `productKey`/`managementStatus` + 4 field giá cuối + `priceLadder`); doc bị
  xóa → dọn cả 2 `outputs/*`.
- **Refactor (không đổi hành vi)**: tách `tests/helpers/scenario-fixture.ts`
  (dựng `ScenarioInput` thật từ fixture v3.4) ra khỏi
  `tests/parity/scenario.test.ts` để dùng lại ở test Cloud Function — verify
  `npm test` vẫn 286/286 sau khi tách.
- `tests/functions/on-scenario-write.test.ts` (2 test, tích hợp thật): ghi
  `scenarios/{id}` bằng `firebase-admin` thẳng lên Firestore Emulator, đợi
  Cloud Function tự chạy trên Functions Emulator, xác nhận `outputs/internal`
  CÓ field giá vốn (`materialCostPerUnit`) còn `outputs/priceList` KHÔNG CÓ —
  bằng chứng ranh giới sales-safe THẬT, không chỉ đọc code đoán đúng. Test
  xóa doc → xác nhận 2 `outputs/*` bị dọn. Script `npm run test:functions`
  (build `functions/` trước + `firebase emulators:exec --only
  firestore,functions,auth`), `vitest.functions.config.ts` riêng, loại
  `tests/functions/**` khỏi `npm test` thường.
- **Verify THẬT**: `npm run test:functions` 2/2 pass trên emulator sống (lần
  chạy đầu tiên đã pass, không cần sửa lỗi runtime). `npm test` 286/286 (kể cả
  sau refactor). `npm run typecheck` root sạch (sửa 1 lỗi
  `noUncheckedIndexedAccess` ở chính test mới). `cd functions && npm run
  typecheck` sạch riêng. Chạy lại `npm run test:rules` (M12.3) — vẫn 33/33,
  xác nhận `.firebaserc` mới không phá test cũ.
- Cập nhật `docs/M12_PLAN.md` (M12.4 `[x]`, tách dòng M12.4b/M12.4c mới +
  hướng dẫn chi tiết M12.4b, nhật ký), `docs/CONTEXT_PASTE.md`,
  `docs/CHANGELOG.md` (v1.22).

## Còn treo / việc phiên sau
- **M12.4b tiếp theo**: Cloud Function `onPlanInputWrite` — xem
  `docs/M12_PLAN.md` mục "Việc tiếp theo ngay khi phiên sau vào" (4 bước cụ
  thể: viết `deriveMoldSetCountBySizeDN()`, nối vào `functions/src/index.ts`,
  test tích hợp, cập nhật bảng).
- **M12.4c** (sau M12.4b): HTTPS Callable `computeTargetCosting` — T2 làm
  ngay được, T3 cần ADR riêng sửa `TargetPriceRequestSchema` thêm trường chọn
  SKU trước (xem ADR-010 mục 3).
- Field-lock còn treo cho `products[]`/`metalInsert[].thresholdPct` (từ Phiên
  17, chưa đổi) + cơ chế cấp/thu hồi Auth custom claim `role` (để dành
  security-review Pha 4, M12.10) — không đổi so với trước.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 19 — Đóng gói tri thức cho phiên mới (checkpoint sau M12.4)
Pha hiện tại: 3 (Code) — M12.1-M12.4 xong, tiếp theo M12.4b | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc/nghiệp vụ mới — phiên rà soát/chốt sổ theo yêu
  cầu user ("lưu tri thức và cập nhật các nhiệm vụ đã xong để đóng session này
  lại và tối mở session mới và làm tiếp"), cùng thông lệ "kit vX" đã thiết lập
  ở v1.5/v1.16/v1.20.

## Đã làm
- Rà soát toàn bộ chuỗi file điều hướng đầu phiên: `AGENTS.md`,
  `docs/CONTEXT_PASTE.md`, `docs/M12_PLAN.md`, `docs/PHASE3_PLAN.md`,
  `docs/GLOSSARY.md`, `docs/PROJECT_SPEC.md`.
- **Phát hiện + vá 2 chỗ lỗi thời** (không phải bug code, chỉ tài liệu chưa
  theo kịp): (1) `docs/CONTEXT_PASTE.md` dòng liệt kê "ADR đã CHẤP NHẬN" vẫn
  dừng ở "001-006 (đầy đủ), 007 và 008" — thiếu ADR-009 (Phiên 12) và ADR-010
  (Phiên 18, phiên này) — đã cập nhật đủ; (2) `docs/PROJECT_SPEC.md` §3 (Kiến
  trúc) chưa có dòng nào nhắc `functions/`/Cloud Function dù đây là quyết
  định kiến trúc đã ghi ADR-010 — thêm 1 dòng ngắn ("Cloud Function chạy
  `calculateScenario()` server-side... client KHÔNG BAO GIỜ tính ScenarioOutput
  phía trình duyệt") khớp văn phong các dòng khác trong §3.
- Xác nhận không cần sửa: `docs/M12_PLAN.md` (bảng M12.1-M12.4 `[x]`, M12.4b
  con trỏ đúng, nhật ký đầy đủ — đã làm đúng ngay lúc code ở Phiên 17-18,
  không bị bỏ sót như lỗ hổng Phiên 15→16 trước đây); `docs/PHASE3_PLAN.md`
  (M12 vẫn trỏ đúng `M12_PLAN.md`); `docs/GLOSSARY.md` (không có thuật ngữ UI
  mới — `moldSetCountBySizeDN`, `onScenarioWrite`, tên Cloud Function... đều
  là nội bộ engine/hạ tầng, chưa lên UI, cùng cách xử lý đã áp dụng cho
  `periodMonths` ở Phiên 11); `AGENTS.md` (thứ tự đọc file đầu phiên vẫn đúng).
- Xác nhận `git status` sạch, mọi commit (M12.3 + M12.4, 2 commit) đã push lên
  `claude/firebase-emulator-security-rules-5eij5z` — branch do hạ tầng phiên
  này cấp (KHÁC branch các phiên trước, xem ghi chú sửa lỗi thời ở Phiên 17).
- Chạy lại `npm test` + `npm run typecheck` + `npm run build` lần cuối trong
  phiên — 286/286 xanh, sạch, build được (KHÔNG chạy lại `test:rules`/
  `test:functions` ở checkpoint này — 2 lệnh đó cần khởi động Firebase
  Emulator tốn thời gian hơn, đã verify thật ngay lúc code M12.3/M12.4 trong
  cùng phiên, không cần lặp lại chỉ để chốt sổ).
- Ghi `docs/CHANGELOG.md` (mục v1.23) tổng hợp bảng rà soát.

## Còn treo / việc phiên sau
- **M12.4b tiếp theo**: Cloud Function `onPlanInputWrite` — đọc
  `docs/M12_PLAN.md` mục "Việc tiếp theo ngay khi phiên sau vào" (đã viết chi
  tiết 4 bước ở Phiên 18), KHÔNG cần đọc lại session log để suy luận.
- Sau M12.4b: M12.4c (Target Costing callable, T3 cần ADR riêng — xem ADR-010).
- M12.5-M12.10 (5 màn hình + security review) — chưa bắt đầu, xem bảng trạng
  thái đầy đủ ở `docs/M12_PLAN.md`.
- Field-lock còn treo (`products[]`/`metalInsert[].thresholdPct`) + cơ chế Auth
  custom claim `role` — không đổi so với Phiên 17/18, để dành M12.10.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 19b — Merge PR #2 (M12.3-M12.4) vào nhánh mặc định
Pha hiện tại: 3 (Code) — M12.1-M12.4 xong, tiếp theo M12.4b | Agent: Claude Code

## Đã quyết
- Hạ tầng phiên trước tự tạo PR #2 (`claude/firebase-emulator-security-rules-
  5eij5z` → `claude/project-knowledge-setup-2au4hr`) từ giao diện Claude Code
  — KHÔNG phải do agent chủ động tạo (luật CLAUDE.md "không tự tạo PR" vẫn giữ
  nguyên, đây là ngoại lệ do hạ tầng). User yêu cầu rõ ràng: merge PR này,
  kiểm tra lại, rồi push tri thức đầy đủ cho phiên sau.

## Đã làm
- Kiểm tra PR #2: `mergeable_state: clean`, không có check CI nào cấu hình
  (repo chưa có GitHub Actions — `total_count: 0` ở combined status, KHÔNG
  phải CI đang fail).
- Merge PR #2 vào `claude/project-knowledge-setup-2au4hr` (merge commit
  `fc6d5fc`, giữ nguyên 3 commit gốc — không squash, đúng văn phong mỗi commit
  1 milestone chi tiết đã dùng suốt dự án).
- **Verify lại TOÀN BỘ trên nhánh mặc định SAU merge** (không chỉ tin merge
  thành công): checkout `claude/project-knowledge-setup-2au4hr` local, chạy
  `npm test` (286/286), `npm run typecheck` (root + `functions/`, sạch cả 2),
  `npm run build` (thành công), `npm run test:rules` (33/33 trên emulator
  thật), `npm run test:functions` (2/2 trên Firestore+Functions Emulator thật)
  — xác nhận kết quả merge không có xung đột/lỗi ẩn nào so với lúc verify
  trước khi tạo PR.

## Còn treo / việc phiên sau
- Branch `claude/firebase-emulator-security-rules-5eij5z` đã MERGE — theo quy
  ước hạ tầng, nếu có việc mới trên tên branch đó thì phải khôi phục lại từ
  `claude/project-knowledge-setup-2au4hr` mới nhất (KHÔNG lắp thêm commit lên
  lịch sử đã merge). Việc tiếp theo (**M12.4b**) làm trên
  `claude/project-knowledge-setup-2au4hr` (nhánh mặc định thật của repo) hoặc
  branch mới do hạ tầng phiên sau cấp — xem `docs/M12_PLAN.md` mục "Việc tiếp
  theo ngay khi phiên sau vào".

## Vi phạm constitution phát hiện được (nếu có)
- Không có.
