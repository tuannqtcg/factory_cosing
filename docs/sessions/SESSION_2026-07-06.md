# SESSION 2026-07-06 — Duyệt UI đóng Pha 1, mở cổng sang Pha 2
Pha hiện tại: 2 (Flow + Zod schema + API contract) | Agent: Claude Code

## Đã quyết (nếu là quyết định kiến trúc → tạo ADR, ghi số ADR ở đây)
- User **DUYỆT UI chính thức** cho Pha 1 (Prototype) — cổng theo AGENTS.md
  ("brief → prototype → ⛔ duyệt UI → schema...") đã mở. Không phải ADR kiến trúc
  (không đổi nghiệp vụ), chỉ là quyết định quy trình nhưng bắt buộc ghi lại vì
  đây là điểm chuyển pha.
- `prototype/blazemaster-costing-app.dc.html` (bản cuối cùng của Phiên 1-14 hôm
  qua) trở thành nguồn tham chiếu UI/UX đóng băng cho Pha 3 — đổi thiết kế màn
  hình ở Pha 2/3 phải quay lại Pha 1 và ghi ADR mới, không tự ý sửa trong lúc
  code.

## Đã làm
- Cập nhật `docs/CONTEXT_PASTE.md`: đổi khối "TRẠNG THÁI HIỆN TẠI" từ Pha 1 →
  Pha 2, ghi rõ ngày duyệt và việc tiếp theo (flow + schema Zod bằng skill
  `schema-design`).

## Còn treo / việc phiên sau
- Bắt đầu Pha 2: dựng Zod schema + API contract cho toàn bộ nghiệp vụ đã chốt ở
  điểm 1-10 trong CONTEXT_PASTE.md (cost driver kép, giá vốn kép, khóa bảng giá,
  thang giá 5 bậc, hoạch định 2 chiều + phân tầng vai, khấu hao khuôn theo thời
  gian, ren kim loại mua ngoài, danh mục quản lý thu hẹp 8 SKU) — dùng skill
  `schema-design`, không phát minh field ngoài những gì đã có trong ADR/fixture.
- ADR-007: vẫn thiếu giá khuôn mới khi mua thêm cho 8 SKU chưa sản xuất — không
  chặn Pha 2 (đã có `MoldAsset.cost` cho phép rỗng theo quyết định trước), nhưng
  cần theo dõi khi có số liệu.
- ADR-008: khi 7 SKU Cút/Tê ren trong vào sản xuất thật, cần đơn giá + tồn kho +
  baseline khóa giá cho loại ren mới của chúng.
- Sau khi có schema nháp: cần ⛔ đóng băng schema (cổng thứ 2) trước khi viết
  code Pha 3 — nhắc user xác nhận rõ ràng như đã làm với cổng Pha 1.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 2 cùng ngày — Viết schema nháp Pha 2 (5 file contract)

Pha hiện tại: 2 (Flow + Zod schema + API contract) | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới (không tạo ADR) — hiện thực hóa các ADR đã
  chấp nhận (001-008) thành schema cụ thể, đúng vai trò của Pha 2.
- **Quyết định thiết kế trong lúc viết schema** (ghi lại vì ảnh hưởng cấu trúc):
  1. Tách rõ INPUT (cấu hình lưu trữ: `baseline`, `thresholdPct`, `Resource`,
     `Product`, `CostPool`) khỏi OUTPUT (giá trị tính ra mỗi lần forward:
     `replacement`, `pricingPrice`, MHR, thang giá, CVP...) — khớp đúng kiến
     trúc `(ScenarioInput) → ScenarioOutput` ở PROJECT_SPEC §3.
  2. `managementStatus` (active/pending_mold — ADR-007 quyết định bổ sung)
     THIẾT KẾ LÀ TRƯỜNG TÍNH RA từ `moldAssets`, KHÔNG lưu trên `Product` — để
     tránh lỗi đồng bộ tay giống `EXCLUDED_SKUS` ở prototype Pha 1.
  3. Firestore: tách VẬT LÝ 4 doc con (`outputs/internal`, `outputs/priceList`,
     `outputs/plan`, `outputs/targetCosting`) thay vì 1 doc lọc field — vì rules
     Firestore không lọc field trong cùng 1 document.
  4. Engine chạy trong Cloud Function (không chạy thẳng ở client) — nếu chạy ở
     browser của vai `sales`, `ScenarioInput` đầy đủ (gồm giá vốn) phải tải
     xuống trước khi tính, vi phạm luật "sales không đọc được cost" ngay ở tầng
     network dù UI có ẩn số.

## Đã làm
- Đọc lại toàn bộ ADR 001-008, `PROJECT_SPEC.md`, `GLOSSARY.md`, tất cả fixture
  (`assumptions/pipe/fitting/dashboard/price-list/mold-assets/metal-insert.json`),
  `docs/BUSINESS_MODEL.md` §1-8, skill `inverse-solver`, và phần cấu hình
  vai/khóa tham số trong `prototype/blazemaster-costing-app.dc.html` để đảm bảo
  schema khớp 100% số liệu/quy tắc đã chốt — không phát minh field mới ngoài
  ADR/fixture.
- Viết `docs/contracts/resource.md`: `ResourceSchema` discriminated union
  (`continuous_kg` | `machine_hour`) + `MoldAssetSchema` (ADR-007).
- Viết `docs/contracts/product.md`: `ProductSchema` discriminated union
  (`pipe` | `fitting`) + BOM ren kim loại optional (ADR-008) + hàm
  `managementStatusOf()` tính từ `moldAssets`.
- Viết `docs/contracts/cost-pool.md`: `SharedFixedCosts`, `NonProductionCosts`,
  `CurrencyParams`, `MarkupChain`.
- Viết `docs/contracts/pricing-chain.md`: `PriceLockPolicy` generic (factory
  function, dùng chung USD/VND) tách input/output; `CompoundInventory` +
  `MetalInsertCatalog` (dual costing ADR-002/008); thang giá 5 bậc; chuỗi
  markup SKU. **Phát hiện và ghi rõ cảnh báo**: `thresholdPct` lệch đơn vị giữa
  2 fixture hiện có (`assumptions.json` = thập phân 0.03, `metal-insert.json`
  = số nguyên 5) — chốt dùng thập phân cho schema, cần chuyển đổi khi migrate.
- Viết `docs/contracts/scenario.md`: `ScenarioInputSchema`/`ScenarioOutputSchema`
  tổng hợp 4 file kia; `PlanInput`/`PlanResult` (T1, dạng đóng); solver contract
  + `TargetProfitRequest` (T2, dạng đóng CVP) + `TargetPriceRequest` (T3, qua
  solver, gồm giá thâm nhập); bảng Firestore doc split (6 collection/doc path)
  + bảng phân quyền tổng hợp 4 vai × 6 vùng dữ liệu.
- Cập nhật `docs/CONTEXT_PASTE.md`: ghi rõ schema nháp đã viết xong, CHƯA đóng
  băng (chờ duyệt cổng thứ 2), nêu phát hiện lệch đơn vị `thresholdPct`.

## Còn treo / việc phiên sau
- **Chờ user duyệt đóng băng schema (cổng thứ 2)** — sau khi duyệt mới được
  viết code Pha 3 (`src/schemas/*.ts` thật từ 5 file `.md` này).
- Khi migrate: chuẩn hóa `thresholdPct` trong `metal-insert.json` sang thập
  phân (xem cảnh báo `pricing-chain.md`).
- Cơ chế cấp/thu hồi Firebase Auth custom claim `role` chưa thiết kế — để dành
  security-review Pha 4.
- Cloud Function tính `ScenarioOutput` + ghi 4 doc con — chưa có code, mới là
  thiết kế/hợp đồng.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 3 cùng ngày — Duyệt đóng băng schema, mở Pha 3 (M1: scaffold + src/schemas)
Pha hiện tại: 3 (Code) — M1/12 xong | Agent: Claude Code

## Đã quyết
- User duyệt schema Pha 2 ("thực hiện theo đề xuất và triển khai") → 5 file
  `docs/contracts/*.md` chính thức **ĐÓNG BĂNG** (cổng thứ 2, AGENTS.md).
- User yêu cầu: **chia Pha 3 thành nhiều phiên/milestone nhỏ** để không tốn
  tool call và có thể dừng khi sắp hết token (trả lời AskUserQuestion về phạm
  vi "triển khai" bằng yêu cầu này thay vì chọn 1 trong 3 phương án phạm vi đề
  xuất) — không phải ADR, là quyết định vận hành phiên làm việc.

## Đã làm
- Viết `docs/PHASE3_PLAN.md`: lộ trình 12 milestone (M1 scaffold/schema → M2-M9
  từng phần engine → M10 solver → M11 parity suite đầy đủ → M12 UI thật/Firestore,
  chỉ làm nếu mở rộng phạm vi), mỗi milestone tự chứa, có bảng trạng thái để
  phiên sau đọc 1 file là biết chính xác việc tiếp theo.
- **M1 hoàn tất**:
  - `package.json` (zod, typescript, vitest) + `tsconfig.json` (strict).
  - `src/schemas/{resource,product,cost-pool,pricing-chain,scenario}.ts` — code
    Zod thật, giữ NGUYÊN cấu trúc đã đóng băng ở `docs/contracts/*.md`, không
    phát minh field mới.
  - `tests/unit/schemas.test.ts` — 10 test: parse toàn bộ 66 `moldAssets`, 91
    SKU phụ kiện, 8 SKU ống, `CostPool` từ `assumptions.json` thật; test
    `managementStatusOf()` với 1 SKU có khuôn (active) + 1 SKU trong
    `skusWithoutMold` (pending_mold); và 2 test xác nhận phát hiện lệch đơn vị
    `thresholdPct` — chứng minh Zod CHỦ ĐỘNG TỪ CHỐI giá trị số nguyên % (5)
    chưa chuẩn hóa, chỉ chấp nhận sau khi chia 100.
  - `npm run typecheck` sạch, `npm test` 10/10 xanh.
- Cập nhật `docs/CONTEXT_PASTE.md` (Pha 2→3, trỏ PHASE3_PLAN.md, ghi M1 xong),
  `docs/CHANGELOG.md` (mục v1.8).

## Còn treo / việc phiên sau
- **M2 tiếp theo**: `src/engine/pipe.ts` — công suất + chi phí SX Ống tại công
  suất bình thường (BUSINESS_MODEL §2.1-2.2), verify khớp tuyệt đối
  `tests/fixtures/pipe.json.costAtNormalCapacity` (`fullCostPerKg =
  106204.729733113`). Xem chi tiết đầy đủ M2..M12 ở `docs/PHASE3_PLAN.md`.
- Node/npm audit báo 5 lỗ hổng (3 moderate, 1 high, 1 critical) — toàn bộ nằm
  trong chuỗi transitive `vitest→vite→esbuild` (rủi ro dev-server, không phải
  runtime production), fix cần nâng vitest lên v4 (breaking change) — CHƯA làm
  vì ngoài phạm vi M1, cân nhắc lại khi milestone sau cần API vitest mới.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 4 cùng ngày — Pha 3 M2: engine Ống (src/engine/pipe.ts)
Pha hiện tại: 3 (Code) — M2/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới.
- **Sửa lỗi thiếu sót schema** (không phải ADR): `ContinuousKgResourceSchema` +
  `MachineHourResourceSchema` (viết ở M1) thiếu 12 field mà công thức chi phí
  gia công §2.2/§3.3 luôn cần (lương, điện, nước, bao bì, năng suất quy đổi giờ
  máy→kg). Phát hiện ngay khi bắt tay viết `pipe.ts` (không tính được
  `laborPerYear` nếu thiếu `avgSalaryMonthly`). Đã bổ sung vào
  `src/schemas/resource.ts` + `docs/contracts/resource.md` (có ghi chú rõ đây
  là sửa lỗi dịch thiếu ADR-001, không phải đổi nghiệp vụ) + cập nhật
  `tests/unit/schemas.test.ts` cho khớp.

## Đã làm
- Viết `src/engine/cost-pool.ts`: `sharedFixedCostsTotalPerYear()` — helper
  dùng chung cho cả Ống (M2) và Phụ kiện (M3, sẽ viết sau), verify công thức
  khớp `assumptions.json.sharedFixedCosts.totalPerYear = 1.253.500.000`.
- Viết `src/engine/pipe.ts`: `calculatePipeCapacity()` (§2.1) +
  `calculatePipeCostAtNormalCapacity()` (§2.2). 2 tham số CHƯA nối dây thật, ghi
  rõ trong comment đầu file: `otherLineEstimatedProductionKgYear` (cross-ref
  sang Phụ kiện cho `sharedCostAllocationRatio` — chưa có `fitting.ts` tới M3)
  và `compoundPricingPriceUsdPerKg` (output ADR-004 price-lock — chưa có engine
  tới M5); cả 2 nhận trực tiếp làm input tạm thời, KHÔNG import module chưa tồn
  tại (giữ `pipe.ts` độc lập/pure).
- Viết `tests/parity/pipe.test.ts` (skill `excel-parity-testing`): đối chiếu
  toàn bộ `pipe.json.capacity` + `pipe.json.costAtNormalCapacity` — khớp tuyệt
  đối `fullCostPerKg = 106.204,729733113` và `vfPricePerKg =
  132.755,912166391` (số vàng bậc 3 và bậc 5 thang giá).
- `npm run typecheck` sạch, `npm test` 14/14 xanh (10 schema cũ + 4 parity mới).
- Cập nhật `docs/PHASE3_PLAN.md` (M2 `[x]`, thêm mục "Nhật ký milestone đã
  xong" + hướng dẫn chi tiết M3), `docs/CONTEXT_PASTE.md`, `docs/CHANGELOG.md`
  (mục v1.9).

## Còn treo / việc phiên sau
- **M3 tiếp theo**: `src/engine/fitting.ts` — công suất ép phun + MHR (§3.1-3.3,
  ADR-001), CÙNG PATTERN đã dùng ở `pipe.ts`. Lưu ý: khấu hao khuôn ĐỘNG theo
  `asOfYear` (ADR-007) là M4 riêng — M3 có thể tạm cộng thẳng
  `Σ moldAsset.costVnd/usefulLifeYears` không lọc năm (vẫn ra đúng số vàng hiện
  tại vì `purchaseYear=2026` toàn bộ 66 khuôn). Chi tiết đầy đủ ở
  `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 5 cùng ngày — Pha 3 M3: engine Phụ kiện (src/engine/fitting.ts)
Pha hiện tại: 3 (Code) — M3/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới.
- **Sửa lỗi thiếu sót schema** (không phải ADR): `MachineHourResourceSchema`
  thiếu `depreciationYears` (khấu hao MÁY ép phun — khái niệm KHÁC
  `MoldAsset.usefulLifeYears` là khấu hao KHUÔN, dù cùng =5 trong dữ liệu hiện
  tại). Bổ sung vào `src/schemas/resource.ts` + `docs/contracts/resource.md`.
- **Quyết định thiết kế trong lúc code**: tách `machineDepreciationPerYear` và
  `moldDepreciationPerYear` thành 2 field riêng thay vì gộp `machineMoldDepreciation`
  như Excel — để M4 (khấu hao khuôn động theo `asOfYear`, ADR-007) chỉ cần sửa
  đúng 1 field, không đụng vào phần khấu hao máy.

## Đã làm
- Viết `src/engine/fitting.ts`: `calculateFittingCapacity()` (§3.2) +
  `calculateFittingCostAtNormalCapacity()` (§3.3). Verify tay từng dòng công
  thức trước khi code (batchesPerYear, totalMachines, designMachineHours3Shift,
  normalMachineHoursUtilized, estimatedProductionKgYear, rồi machineDepreciation
  + moldDepreciation + labor + electricity + water + sharedCostAllocated →
  mhrPerMachineHour) — tất cả khớp số vàng trước khi viết test.
  `moldDepreciationPerYear` hiện cộng Σ toàn bộ 66 `moldAsset` KHÔNG lọc theo
  `asOfYear` (đúng vì tất cả `purchaseYear=2026`, năm đầu khấu hao) — M4 sẽ
  thêm điều kiện lọc.
- Viết `tests/parity/fitting.test.ts`: khớp tuyệt đối
  `mhrPerMachineHour = 1.344.175,79463858`, cùng toàn bộ field trung gian
  (`sharedCostAllocationRatio=0,124026...`, `totalProcessingCostPerYear`...) và
  3 số quy-kg tham chiếu (`processingCostPerKgRef`/`fullCostPerKgRef`/`vfPricePerKgRef`).
- `npm run typecheck` sạch, `npm test` 18/18 xanh (10 schema + 4 parity pipe +
  4 parity fitting).
- Cập nhật `docs/PHASE3_PLAN.md` (M3 `[x]`, thêm hướng dẫn chi tiết M4 — chỉ
  cần sửa 1 điều kiện lọc trong vòng lặp `moldDepreciationPerYear`, không đổi
  công thức), `docs/CONTEXT_PASTE.md`, `docs/CHANGELOG.md` (mục v1.10).

## Còn treo / việc phiên sau
- **M4 tiếp theo**: thêm `asOfYear` vào input + điều kiện lọc trong
  `calculateFittingCostAtNormalCapacity()` (chỉ cộng khuôn còn trong thời gian
  khấu hao) + viết test với `asOfYear` giả định xa hơn 2026 để CHỨNG MINH logic
  lọc chạy đúng (hiện chưa có test nào phân biệt được vì `asOfYear=2026` luôn
  rơi vào năm đầu của mọi khuôn). Chi tiết đầy đủ ở `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.

---

# Phiên 6 cùng ngày — Pha 3 M4: khấu hao khuôn động theo asOfYear (ADR-007)
Pha hiện tại: 3 (Code) — M4/12 xong | Agent: Claude Code

## Đã quyết
- Không có quyết định kiến trúc mới — hiện thực hóa đúng ADR-007 đã chấp nhận.

## Đã làm
- Viết `src/engine/mold-depreciation.ts`: `isMoldAssetStillDepreciating(asset,
  asOfYear)` (điều kiện `0 ≤ asOfYear−purchaseYear < usefulLifeYears`, có xử lý
  cả trường hợp `asOfYear < purchaseYear` — khuôn chưa tồn tại) +
  `moldDepreciationPerYear(moldAssets, asOfYear)`.
- Nối vào `src/engine/fitting.ts`: thêm `asOfYear` vào
  `FittingCostAtNormalCapacityInputs`, thay vòng lặp reduce inline (M3) bằng
  gọi hàm mới — không đổi công thức nào khác, chỉ thêm điều kiện lọc đúng như
  kế hoạch M3 đã ghi.
- Viết `tests/unit/mold-depreciation.test.ts` (10 test): 5 test đơn vị cho
  `isMoldAssetStillDepreciating` (năm đầu/năm cuối/đúng mốc hết hạn/quá hạn
  nhiều năm/trước năm mua); 2 test cho `moldDepreciationPerYear` dùng DỮ LIỆU
  TỔNG HỢP (3 khuôn mua 3 năm khác nhau, đời sống khác nhau) để chứng minh lọc
  CHỌN LỌC đúng — dữ liệu thật (66 khuôn cùng `purchaseYear=2026`) không đủ
  phân biệt logic lọc có chạy đúng hay không (luôn ra tất-cả-hoặc-không); 3 test
  trên `mold-assets.json` thật tại `asOfYear` 2026 (khớp năm gốc, Σ=1.308.570.000),
  2030 (còn hạn, vẫn khớp), 2031 (hết hạn cả 66 khuôn → về 0, không throw).
- Cập nhật `tests/parity/fitting.test.ts` (thêm `asOfYear: 2026` vào input) —
  vẫn khớp đúng `mhrPerMachineHour` cũ.
- `npm run typecheck` sạch, `npm test` 28/28 xanh.
- Cập nhật `docs/PHASE3_PLAN.md` (M4 `[x]`, hướng dẫn chi tiết M5 — nối dây 2
  tham số "tạm thời" từ M2/M3 vào output price-lock thật), `docs/CONTEXT_PASTE.md`,
  `docs/CHANGELOG.md` (mục v1.11).

## Còn treo / việc phiên sau
- **M5 tiếp theo**: `src/engine/price-lock.ts` (ADR-004, 5 kịch bản nghiệm thu
  trong `price-lock-scenarios.json`) + `src/engine/dual-costing.ts` (ADR-002) +
  NỐI DÂY `compoundPricingPriceUsdPerKg` ở `pipe.ts`/`fitting.ts` (hiện nhận
  trực tiếp làm input) vào output thật của `evaluatePriceLock()`. Chi tiết đầy
  đủ ở `docs/PHASE3_PLAN.md`.

## Vi phạm constitution phát hiện được (nếu có)
- Không có.
