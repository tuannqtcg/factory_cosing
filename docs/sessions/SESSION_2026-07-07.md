# SESSION 2026-07-07 — Đối chiếu Excel v3.7 (ADR-011) + khảo sát yêu cầu multi-material (Corzan)

Pha hiện tại: 3 (Code, engine lõi M1-M11 đã xong, M12 UI+Firestore đang treo) | Agent: Claude Code

## Đã quyết (ADR mới)
- **ADR-011** (`docs/decisions/ADR-011-fitting-productivity-bottom-up.md`):
  đối chiếu `BlazeMaster_Model_v3_7.xlsx` (user upload) với kiến trúc hiện tại —
  Assumptions/Ống/Plan_SX/PriceList giống hệt v3.4. CHỈ 1 thay đổi thật: Phụ
  kiện `avgProductivityKgPerMachineHour` đổi từ nhập tay cố định (44,6 kg/giờ
  máy) sang tính bottom-up từ bảng khuôn (nhóm SKU theo `moldSizeDN`, trung
  bình không trọng số qua 8 nhóm — đúng giả định %mix đều 1/8 hiện có trong
  Excel), có ghi đè tùy chọn. User chọn "ghi ADR + cập nhật engine/fixture
  theo v3.7".
- User chọn hướng xử lý yêu cầu "quản lý nguyên liệu theo sản phẩm" (Corzan
  3710 cho sản phẩm dòng Corzan, khác BlazeMaster Orange hiện tại): **viết
  design brief + prototype trước** (Pha 0/1), CHƯA đụng schema/code production
  — đúng ADR-003 (Phase 1→2 chỉ khi có ca dùng thứ 2 thật, Corzan chính là ca
  đó, nhưng vẫn phải qua cổng duyệt UI trước khi code).

## Đã làm
- Trích dữ liệu v3.7 bằng script Python/openpyxl đọc thẳng ô đã tính (không gõ
  tay, đúng skill `excel-parity-testing`), cập nhật fixture vàng:
  `pipe.json`, `fitting.json`, `dashboard.json`, `price-list.json`,
  `price-lock-scenarios.json`. Số cũ (v3.4) giữ lại ở ADR-011 + comment trong
  BUSINESS_MODEL.md/SKILL.md để đối chiếu lịch sử.
- Engine: `src/schemas/resource.ts` (`avgProductivityKgPerMachineHour` →
  `.optional()`), `src/engine/fitting.ts` (thêm
  `computeMixAvgProductivityKgPerMachineHour()`, `calculateFittingCapacity()`
  nhận thêm `fittingProducts`), `src/engine/cvp.ts` (đọc năng suất mix đã giải
  quyết từ `capacity` thay vì `resource` trực tiếp — tránh giải quyết override
  2 lần), `src/engine/scenario.ts` (lọc `products` theo `kind === 'fitting'`
  truyền vào).
- Cập nhật 5 test file (`fitting.test.ts`, `plan.test.ts`, `price-ladder.test.ts`,
  `price-list-snapshot.test.ts`, `cvp.test.ts`) để truyền `fittingProducts` vào
  `calculateFittingCapacity()`, và sửa các assertion hardcode số vàng cũ.
  `npm test` 286/286 xanh, `npm run typecheck` + `npm run build` OK.
- Cập nhật tài liệu: `docs/BUSINESS_MODEL.md` §1a/§3.2 (công thức + số mới),
  `docs/contracts/resource.md` (ghi chú field optional), skill
  `excel-parity-testing` (bảng số vàng), `docs/CONTEXT_PASTE.md` (khối TRẠNG
  THÁI HIỆN TẠI, điểm 5 + ADR-011 + việc treo).

## Phiên 2 cùng ngày — Duyệt UI multi-material, mở Pha 2, ADR-012 + contract material.md

- User **DUYỆT LAYOUT** prototype `multi-material-catalog.html` (cổng Pha 1 mở
  cho tính năng multi-material) + cung cấp giá thật: **Corzan compound ống
  3,47 USD/kg, phụ kiện 3,97 USD/kg, nhập Ấn Độ** → prototype cập nhật (2
  Material Corzan riêng, cột xuất xứ/thuế NK).
- Chốt 3 câu hỏi phạm vi (AskUserQuestion): (1) thuế NK: user yêu cầu TRA CỨU →
  compound HS 3904 nhập Ấn Độ có C/O form AI theo AIFTA (NĐ 122/2022/NĐ-CP)
  = **0%** (nguồn logistics công khai — cần forwarder xác nhận mã HS con);
  (2) **markup Corzan RIÊNG** (số % chờ user); (3) **chung line, chung MHR**.
- Viết **ADR-012** (multi-material: Material entity độc lập, materialId trên
  Product, thuế NK + markup VF theo material, migration giữ nguyên số vàng
  v3.7) — trạng thái CHẤP NHẬN kiến trúc, schema chi tiết chờ đóng băng.
- Viết contract **`docs/contracts/material.md`** (CHỜ ĐÓNG BĂNG) — schema
  MaterialSchema + bảng thay đổi lan sang product/scenario/cost-pool contract
  + kế hoạch migration + phân quyền.

## Phiên 3 cùng ngày — ĐÓNG BĂNG schema, code Pha 3 M13 (multi-material engine + Corzan)

- User: "duyệt đóng băng, markup như đề xuất" + rule danh mục SKU Corzan (cùng
  tên/size/thông số BlazeMaster; ỐNG đơn trọng ×1,1 theo size; PHỤ KIỆN giống
  hệt — chung khuôn). Tra cứu thuế NK theo yêu cầu user: HS 3904 nhập Ấn Độ
  C/O form AI (AIFTA, NĐ 122/2022) = 0% — cần forwarder xác nhận mã HS con.
- **M13.1** (commit `7a8aa95`): Material schema + engine multi-material —
  `material.ts`, `materialId` trên Product, `materials[]` trong ScenarioInput
  (bỏ `inventory.pipe|fitting`), CurrencyParams/MarkupChain gọn lại (thuế NK +
  markup VF chuyển vào Material), ScenarioOutput theo (line, materialId),
  PlanResult.materialRequirement theo materialId, PlanInput entries thêm
  `materialId` optional (khóa trùng tên/size 2 thương hiệu). Orchestrator chạy
  khóa giá/CVP/thang giá/dual-costing theo TỪNG material; quy ước material
  THAM CHIẾU (đầu tiên trong materials[] mà line dùng) cho doanh thu chéo bậc
  4 + MHR top-level — ghi vào material.md mục "Bổ sung khi code Pha 3 M13".
  Migration parity: 286/286 xanh, mọi số vàng v3.7 khớp tuyệt đối.
- **M13.2**: `tests/fixtures/corzan.json` (2 Material giá thật 3,47/3,97, thuế
  0%, kho rỗng) + `buildCorzanScenarioInput()` sinh 99 SKU Corzan theo rule
  (không chép tay) + `tests/parity/corzan.test.ts` (11 test): số tính tay độc
  lập Python (fullCost ống Corzan 114.050,99 đ/kg; DN50 niêm yết 367.000; Tê
  đều 20 → 26.400; Nối ren trong 20xPT15 → 65.400), test CÁCH LY (99 chuỗi giá
  + thang giá + kho BM giống hệt baseline từng số), managementStatus Corzan
  83 active/8 pending (chung khuôn). `npm test` 297/297 xanh.

## Phiên 4 cùng ngày — Chốt sổ 2 việc treo (user xác nhận)
- Thuế NK Corzan = **0% XÁC NHẬN** (không còn chờ forwarder) — cập nhật
  corzan.json/_meta, material.md, ADR-012, prototype banner. Vận hành: mỗi lô
  nhập vẫn cần C/O form AI hợp lệ.
- User xác nhận **KHÔNG cần Excel Corzan riêng** — logic BlazeMaster áp
  nguyên; nguồn chân lý Corzan = engine forward + rule corzan.json; đối chiếu
  độc lập bằng số tính tay ở corzan.test.ts là đủ (pattern M9 — sheet không có
  số vàng Excel).

## Còn treo / việc phiên sau
- KHÔNG còn việc treo cho ADR-012/multi-material.
- Prototype `multi-material-catalog.html` chưa cập nhật theo rule SKU ×1,1 —
  UI thật (M12) sẽ dựng từ engine, prototype chỉ còn giá trị tham chiếu layout.
- M12 (UI thật + Firestore) vẫn treo — M12.4b (onPlanInputWrite) là việc kế
  tiếp của mạch M12; M13 (multi-material) đã xong phần engine.

## Vi phạm constitution phát hiện được (nếu có)
- Không có — mọi sửa cấu trúc field (`avgProductivityKgPerMachineHour`) đã có
  ADR-011 trước khi commit, đúng luật #5 + tiền lệ ADR-009.
