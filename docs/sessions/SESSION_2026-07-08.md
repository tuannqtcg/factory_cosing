# SESSION 2026-07-08 — Pha 3 M12.4b → M12.7: 2 Cloud Function còn lại + Dashboard + Bảng Giá + Kế Hoạch SX

Pha hiện tại: 3 (Code, M12 đang làm — xong đến M12.7) | Agent: Claude Code

## Đã quyết
- KHÔNG có ADR mới. 2 hàm engine mới là bổ sung HÀM (không sửa schema nào) —
  theo đúng hệ quả ADR-010 ("ghi rõ đây là hàm ENGINE mới… không cần dòng
  ADR-009").
- Orchestration Plan_SX đặt Ở ENGINE (`calculatePlanForScenario()` trong
  `src/engine/plan-support.ts`, pure) thay vì viết thẳng trong Cloud Function
  như mô tả gốc M12_PLAN — rộng hơn mô tả 1 nấc nhưng đúng PROJECT_SPEC §3
  (engine pure dùng chung mọi nơi): test được bằng `npm test` không cần
  emulator, M12.7 (màn Kế Hoạch SX) tái dùng nguyên. Cloud Function chỉ còn
  I/O Firestore + parse Zod 2 đầu.
- Hành vi xóa: xóa `planInputs/{period}` → dọn `outputs/plan` (doc không lưu
  period nguồn nên không phân biệt được kết quả đang đứng thuộc period nào —
  dọn cho khỏi stale); xóa `scenarios/{id}` → `onScenarioWrite` giờ dọn thêm
  `outputs/plan` (trước chỉ dọn 2 doc nó tự ghi); `planInputs` là INPUT của
  vai production, không bao giờ tự xóa.

## Đã làm
- `src/engine/plan-support.ts` (mới):
  - `deriveMoldSetCountBySizeDN(moldAssets, products)` — lấp đúng khoảng
    trống #1 của ADR-010: 1 `MoldAsset` = 1 bộ khuôn, cộng 1 vào TỪNG size DN
    distinct mà khuôn ép ra (join `producesSkus` → `FittingProduct.moldSizeDN`
    qua productName+sizeLabel; SKU ngoài danh mục bỏ qua). Số kỳ vọng đếm tay
    độc lập (script Python) từ 66 khuôn mold-assets.json:
    {20:4, 25:9, 32:8, 40:10, 50:11, 65:9, 80:7, 100:8} — tổng đúng 66,
    không khuôn nào map 2 size.
  - `calculatePlanForScenario(ScenarioInput, PlanInput)` — wiring giống hệ
    quy ước `calculateScenario()` (ADR-012): material tham chiếu dòng Ống =
    material đầu tiên trong `materials[]` có SP Ống dùng; khóa giá evaluate
    TỪNG material (tái dùng `lastLotPriceOf` — export thêm từ `scenario.ts`);
    `PlanMaterialPricing[]` cấp cho MỌI material: `compoundLandedPerKgVnd` =
    `landedCostPerKgVnd(giá ĐÃ KHÓA)`, `replacementUsdPerKgRaw` = replacement
    THÔ (ADR-004 §1a). Phát hiện nhỏ: mô tả cũ trong M12_PLAN liệt kê
    `calculateFittingCostAtNormalCapacity` là cần gọi — thực tế KHÔNG cần
    (`CalculateFittingPlanInputs` không nhận cost), đã ghi lại trong nhật ký.
- `functions/src/index.ts`: thêm `onPlanInputWrite` (`onDocumentWritten`
  `scenarios/{scenarioId}/planInputs/{period}`): đọc scenario (không tồn tại
  → log + bỏ qua, v2 không retry), parse `ScenarioInputSchema` +
  `PlanInputSchema`, gọi `calculatePlanForScenario()`, parse
  `PlanResultSchema`, GHI ĐÈ `outputs/plan` (1 doc duy nhất đúng
  `scenario.md` §5/ADR-010).
- `vitest.functions.config.ts`: thêm `fileParallelism: false` — 2 file test
  functions cùng `recursiveDelete` collection `scenarios` ở afterAll, chạy
  song song mặc định của vitest sẽ xóa dữ liệu của nhau giữa chừng.
- Test mới:
  - `tests/unit/plan-support.test.ts` (12 test): fixture thật 66 khuôn; quy
    tắc đếm (nhiều SKU cùng size = 1 bộ; 1 khuôn 2 size = mỗi size 1; SKU lạ
    bỏ qua); `calculatePlanForScenario` đối chiếu số tính tay kịch bản A của
    `plan.test.ts` (baseline khóa giá "KHÓA" pricingPrice=replacement nên 2
    đường tính phải trùng — kgToBuy 73.500, landed 3,03×1,07×26.500, idle
    8.256,97 đ/kg v3.7); 2 case multi-material Corzan (chỉ định
    `materialId: corzan-fitting` tách đúng dòng NVL; bỏ trống → khớp
    BlazeMaster, tương thích plan cũ).
  - `tests/functions/on-plan-input-write.test.ts` (3 test, emulator thật):
    ghi planInput → `outputs/plan` khớp engine pure; ghi lại kế hoạch khác →
    GHI ĐÈ đúng (147.000 kg = 2× kịch bản đầu); xóa planInput → dọn
    `outputs/plan`.
- Verify đủ 5 cổng: `npm test` **309/309** (297 cũ + 12 mới), `npm run
  test:functions` **5/5** (2 cũ + 3 mới, emulator thật), `npm run test:rules`
  **33/33**, `npm run typecheck` root + functions sạch, `npm run build` OK.
- Tài liệu: bảng + nhật ký `docs/M12_PLAN.md` (M12.4b `[x]`, mục "Việc tiếp
  theo" chuyển sang M12.4c với ghi chú T2 trước/T3 cần ADR chọn SKU),
  `docs/CONTEXT_PASTE.md` khối TRẠNG THÁI HIỆN TẠI.

## Phần 2 cùng phiên — M12.4c: HTTPS Callable `computeTargetCosting` (user: "vậy làm đi")

### Đã quyết (ADR-013 — `docs/decisions/ADR-013-target-costing-callable.md`)
- `TargetPriceRequestSchema` += `productKey` {dn?/productName?/sizeLabel?/
  materialId?} (chọn SKU T3 — khoảng trống #2 ADR-010); `TargetProfitRequestSchema`
  += `materialId` optional (CVP theo (line, material) sau ADR-012). Cả 2 vào
  bảng ADR-009 dòng #6/#7 + comment trong contract `scenario.md` §4 — đúng quy
  trình "không tự thêm field ngầm".
- `bounds`/`tol` KHÔNG do client cấp: allowlist `TARGET_PRICE_FREE_VARS`
  server-side — 5 biến LIÊN TỤC v1 đúng skill inverse-solver mục 5
  (normalUtilizationFactor phụ kiện; replacement từng material — đi QUA khóa
  giá ADR-004, f bậc thang đơn điệu nên bisection vẫn đúng; markupVf từng
  material; markupTcg; listPriceMargin). Biến nguyên `shifts` HOÃN → M12.8
  (chỉ thêm entry + solveDiscrete, ghi bổ sung ADR-013, không cần ADR mới).
- Doc `outputs/targetCosting`: GHI ĐÈ 1 doc {kind, request, result} (request
  gần nhất, đối xứng outputs/plan) + callable TRẢ kết quả trực tiếp; phân biệt
  T2/T3 bằng field đặc thù của request (targetProfitVnd ⇔ targetListPriceVnd).
- Quyền: custom claim `role` ∈ {pricing, admin} (bảng scenario.md §6);
  unauthenticated/permission-denied/invalid-argument/not-found chuẩn HttpsError.

### Đã làm
- `src/engine/target-costing.ts` (mới, pure — cùng pattern plan-support M12.4b,
  M12.8 tái dùng): `computeTargetProfitForScenario` (T2) +
  `computeTargetPriceForScenario` (T3 — solve() bisection với forwardFn =
  `calculateScenario` NGUYÊN CON, lần đầu solver chạy trên forward function
  đầy đủ của app; target = `chain.listPriceBeforeVat`, tol 0,5đ < bước làm
  tròn 100đ; validate freeVarPath/productKey TRƯỚC khi bisection). Helper
  `referenceMaterialOf` export từ `scenario.ts`, plan-support refactor dùng chung.
- `functions/src/index.ts`: callable `computeTargetCosting` (onCall v2) —
  auth → parse Zod → engine → ghi đè `outputs/targetCosting` + return.
- Test: 10 unit (`tests/unit/target-costing.test.ts` — T2 khớp
  breakEvenKgYear vàng v3.7 cả 2 line + case Corzan materialId; T3 case chuẩn
  DN50 260.000đ/m forward-verify + round-trip, infeasible, allowlist chặn,
  productKey sai) + 4 tích hợp emulator
  (`tests/functions/compute-target-costing.test.ts` — Auth Emulator thật:
  custom claim → custom token → idToken, gọi đúng giao thức HTTP onCall; T2
  vai pricing, T3 vai admin + ghi đè doc, sales 403 + anonymous 401,
  freeVarPath lạ 400).
- Verify đủ 5 cổng: `npm test` **319/319**, `test:functions` **9/9**,
  `test:rules` **33/33**, typecheck root+functions, build OK.
- Tài liệu: ADR-013 (mới), ADR-009 (#6/#7), contract scenario.md §4,
  M12_PLAN (bảng + nhật ký + con trỏ → M12.5), PHASE3_PLAN (con trỏ),
  CONTEXT_PASTE.

## Phần 3 cùng phiên — M12.5: màn hình Dashboard thật (user: "tiếp tục")

### Đã quyết
- 2 khối số vàng `dashboard.json` chưa có hàm engine (`capacityLevels`,
  `investment`) → viết `src/engine/dashboard-support.ts` TRƯỚC khi dựng UI,
  công thức GIẢI MÃ từ số vàng bằng script Python độc lập (khớp tuyệt đối cả
  7 giá trị rồi mới code — đúng kỷ luật M8). Phát hiện nghiệp vụ: 3 mức công
  suất giữ phân bổ chi phí chung TẠI CS bình thường (không tính lại ratio);
  tổng vốn cố định gồm cả vốn đầu tư Lab/UL (field `labAnnualized`/
  `ulSetupAnnualized` thực chất lưu NGUYÊN GIÁ, engine chia /depreciationYears
  khi tính năm). KHÔNG thêm field vào ScenarioOutput (đóng băng) — KPI là view
  dẫn xuất, vai admin/pricing gọi engine client-side (đọc được trọn
  ScenarioInput nên không xuyên ranh giới dữ liệu).
- Sidebar "Xem Như Vai" (prototype chỉ đổi state) → bản thật = ĐĂNG NHẬP user
  demo `{role}@demo.local` với custom claim `role` trên Auth Emulator (rules
  phải chạy thật). Cơ chế dev-only; login thật cho project production thuộc
  security-review Pha 4 (scenario.md §6 "Còn treo").
- Top-down "Compound tối đa": prototype mock dùng công thức ngược closed-form
  — bản thật KHÔNG được (luật inverse-solver #1) → `solve()` bisection trên
  `calculateScenario` client-side, target = giá thành đầy đủ Ống tại material
  đang chọn.
- Nguồn dữ liệu tách theo vai đúng scenario.md §5: admin/pricing đọc
  scenario + outputs/internal (onSnapshot + validate Zod client — luật #2);
  sales CHỈ đọc outputs/priceList; production không có tab dashboard.

### Đã làm
- Engine: `src/engine/dashboard-support.ts` (`calculateDashboardKpis`) + 9
  parity test `tests/parity/dashboard-kpis.test.ts` (capacityLevels 3 mức,
  investment 4 KPI, tự nhất quán bậc 3 thang giá, Corzan không đổi KPI).
- Hạ tầng: `src/lib/firebase.ts` (emulator mặc định, `.env` → project thật),
  `scripts/seed-emulator.ts` (vite-node; 4 user + claim + scenario baseline),
  npm scripts `emulators`/`seed:emulator`, `src/vite-env.d.ts`.
- UI: `src/features/shell/AppShell.tsx` (sidebar đúng prototype, nav theo
  ROLE_TAB_ACCESS ADR-006, tab chưa dựng → placeholder trỏ milestone),
  `src/features/auth/useAuth.ts`, `src/features/dashboard/{Dashboard.tsx,
  useScenarioData.ts}`, `src/lib/format.ts` (số VN); lock bar ADR-004 + nút
  "Chốt Baseline Mới" (updateDoc), thang giá byLineMaterial + MaterialPicker
  (ADR-012), 3 mức công suất, top-down, 6 KPI card. `src/App.tsx` → AppShell.
- Verify CHẠY THẬT: `npm run emulators` (3 function nạp) + seed + `npm run
  dev` + Playwright (playwright-core cài --no-save) chụp 2 vai: pricing đủ
  I-IV số vàng v3.7 đúng trên màn hình (thang giá 100.663→132.899; 3 mức
  113.195/108.038/106.319 đ/kg; top-down 120.000đ → margin 11,4% + compound
  tối đa 3,46 USD/kg từ solver; KPI 15,97/16,37/33,78 tỷ, payback 0,82 năm);
  sales chỉ thang giá + ghi chú ADR-006 (đọc outputs/priceList qua rules
  thật). Screenshot lưu scratchpad phiên (không commit).
- `npm test` **328/328** (+9), typecheck + build OK (test:functions/rules
  không đổi — 9/9 + 33/33 đã xanh ở phần 1-2).

## Phần 4 cùng phiên — M12.6: màn Bảng Giá sales-safe (user: "tiếp tục")

### Đã quyết
- Doc `outputs/priceList` thiếu ĐVT/Quy cách mà bảng giá prototype cần (sales
  không đọc được `scenarios/{id}` để tự tra) → thêm `unit`/`spec` hiển thị
  vào từng dòng + định nghĩa `PriceListDocSchema` (doc vốn dựng ad-hoc từ
  M12.4, nay validate cả 2 đầu — luật #2). Bảng ADR-009 dòng #8 + contract
  scenario.md §5. TUYỆT ĐỐI không thêm field giá vốn.
- 2 chỗ CỐ Ý khác prototype (prototype mock sai so nguồn chân lý): ẩn 8 SKU
  chưa khuôn bằng `managementStatus` (ADR-007) thay hard-code EXCLUDED_SKUS;
  KHÔNG có dòng #100 "Dung môi 550" (PriceList Excel = 99 dòng;
  solvent550PricePerBox là vật tư phụ CostPool, không phải SKU thương mại).
- Nhãn "Có VAT (8%)": thuế suất SUY TỪ DỮ LIỆU (withVat/beforeVat − 1) — sales
  không đọc được costPool, không hard-code 8%.

### Đã làm
- `toPriceListDoc(output, products)`: zip theo index (calculateScenario giữ
  thứ tự products) + đối chiếu khóa từng dòng trước khi ghi (throw nếu lệch);
  test on-scenario-write thêm assertion unit='mét'/spec='SDR 13.5'.
- `PriceListDocSchema` (src/schemas/scenario.ts), `useScenarioData` đọc trọn
  doc priceList cho mọi vai (sales-path cũ chỉ lấy priceLadder — bỏ),
  `src/features/price-list/PriceList.tsx` (search + toggle VAT + 10 nút lọc
  + bảng 6 cột, React key gồm materialId — ADR-012), AppShell nối tab.
- Verify: `npm run test:functions` 9/9, `npm test` 328/328, typecheck
  root+functions, build OK; chạy thật emulator + seed + screenshot vai sales:
  91 SKU (99 − 8 pending_mold), lọc "Tê giảm" 18 dòng, giá v3.7 đúng (DN20
  71.600 / Tê đều 20 26.900 — prototype mock hiển thị số v3.4 cũ, bảng thật
  theo fixture).

## Phần 5 cùng phiên — M12.7: màn Kế Hoạch SX (user hỏi rõ 2 phương án rồi chốt "phương án a")

### Đã quyết (ADR-014)
- User được trình 2 phương án cấp danh mục SP cho vai production (không đọc
  được scenarios/{id}): (a) doc danh mục tối thiểu riêng; (b) nới quyền đọc
  outputs/priceList (có giá bán). **User chốt (a)** → ADR-014: doc
  `outputs/productCatalog` (`ProductCatalogDocSchema`) do onScenarioWrite ghi,
  gồm khóa SP + dữ liệu KỸ THUẬT (đơn trọng/chu kỳ/cavity — prototype đóng
  băng hiển thị các cột này; khi trình phương án từng nói "không đơn trọng",
  xem xét kỹ thì ranh giới thật cần giữ là GIÁ, đã ghi rõ trong ADR) + block
  `params` vận hành (yield, công suất, giờ khả dụng, người/ca). TUYỆT ĐỐI
  không field giá — test tích hợp kiểm cả chuỗi JSON không chứa
  price/cost/markup/usdVnd/inventory/lot. Quyền: production/admin/pricing
  đọc, sales ✗, client không ghi.
- Ranh giới engine trên UI: cột dẫn xuất từng dòng (kg TP, giờ máy, ngày SX)
  là số học thuần từ catalog; "Ca máy cần"/NVL đ/nhân công/cảnh báo khuôn CHỈ
  từ `outputs/plan` (server tính qua onPlanInputWrite) — KHÁC prototype mock
  tự tính toàn bộ, đúng nguyên tắc client không lắp công thức engine.

### Đã làm
- ADR-014 + contract scenario.md §5/§6 + `ProductCatalogDocSchema` +
  `toProductCatalogDoc()` trong onScenarioWrite (ghi + dọn khi xóa) +
  firestore.rules match mới + 4 case rules test (37/37) + assertion tích hợp
  (catalog 8 pipes/91 fittings, cấm chuỗi nhạy cảm, dọn khi xóa — 9/9).
- UI `src/features/plan/` (PlanScreen + usePlanData): hàng nhập kỳ/nhân
  công/dự phòng (3 field PlanInput ADR-009 #3 prototype không có) + bảng Ống
  nhập MÉT + bảng PK nhóm theo loại nhập SỐ CÁI (83 SKU active) + nút
  "Lưu & tính" (validate Zod client trước khi ghi — luật #2) + panel kết quả
  từ outputs/plan (ca cần, cảnh báo khuôn, NVL theo material ADR-012, tuyển
  thêm). Form tự nạp lại planInputs đã lưu. Tên nguyên liệu chỉ hiện khi
  LINE có >1 material.
- Verify: rules 37/37, functions 9/9, `npm test` 328/328, typecheck
  root+functions, build OK; chạy thật vai production với ĐÚNG kịch bản A
  plan.test.ts (DN50 50.000m + Tê đều 20 20.000 cái): preview 63.000 kg/500
  giờ/62,5 ngày; server trả 2 ca Ống + 1 ca PK + tuyển 0; NVL bm-orange-pipe
  73.500 kg / 6.314.800.275 đ / 222.705 $ — khớp tuyệt đối số tính tay test.

## Phần 6 cùng phiên — Đóng gói tri thức (kit v1.28) chuẩn bị phiên tối (user yêu cầu)

- `docs/CHANGELOG.md` thêm **v1.28** (tóm tắt 5 milestone M12.4b→M12.7 + 2
  ADR + trạng thái xanh + con trỏ phiên tối).
- Rà soát tài liệu lỗi thời khi đóng gói (cùng thông lệ kit v1.23):
  - `PROJECT_SPEC.md` §3 bullet `functions/` còn mô tả thời chỉ có 1 Cloud
    Function → cập nhật 2 trigger + 1 callable + nuance "client không tính
    ScenarioOutput để hiển thị/lưu; admin/pricing được gọi engine pure cho
    view dẫn xuất (M12.5)".
  - `CONTEXT_PASTE.md` danh sách ADR bị lệch thứ tự (013/014 chen trước
    011/012) → sắp lại 009→014.
  - Phát hiện có lợi cho M12.8: `PROJECT_SPEC.md` §2 đã dự kiến "màn hình
    Target Costing riêng" TỪ PHA 0 — prototype đóng băng thiếu tab này, vậy
    mockup M12.8 là HOÀN THIỆN thiếu sót so spec (vẫn cần user duyệt layout).
    Đã ghi vào mục "Việc tiếp theo" của M12_PLAN.
- Verify chốt sổ: `npm test` 328/328, typecheck root+functions, build OK
  (rules 37/37 + functions 9/9 đã xanh ở phần 5, không đổi gì thêm sau đó).

## Phần 7 cùng phiên — Merge lên nhánh mặc định (kit v1.29, user yêu cầu)

- Merge `claude/next-session-work-iz1i9g` (8 commit) vào
  `claude/project-knowledge-setup-2au4hr` — merge commit `be2bdd2` (--no-ff,
  merge thuần tiến vì nhánh mặc định đứng yên ở `f298f01`).
- Verify TRÊN nhánh mặc định SAU merge, đủ 5 cổng: `npm test` 328/328,
  typecheck root+functions, `npm run build` OK, `test:rules` 37/37,
  `test:functions` 9/9 (emulator thật). Biên bản: CHANGELOG v1.29 +
  CONTEXT_PASTE cập nhật dòng trạng thái merge. Nhánh làm việc đồng bộ về
  head nhánh mặc định.

## Việc treo cho phiên sau (TỐI 2026-07-08)
- **M12.8: màn Target Costing — MỞ ĐẦU PHIÊN bằng dựng mockup** (chọn SKU,
  chọn biến dò trong allowlist ADR-013, T2 lợi nhuận mục tiêu, hiển thị
  forward-verify) rồi hỏi user duyệt layout, SAU ĐÓ mới code. Spec §2 đã dự
  kiến màn riêng từ Pha 0; prototype đóng băng chưa có tab này. Backend xong
  hết (callable `computeTargetCosting` M12.4c). Biến `shifts` nếu mockup cần
  → bổ sung ADR-013 + allowlist + solveDiscrete.
- Sau đó M12.9 (form Tồn kho/Giả định/Cấu hình — admin/pricing ghi
  scenarios/{id}, trigger tự tính lại), M12.10 (security review Pha 4 + chạy
  lại toàn bộ parity + chuẩn bị merge — KẾT PHA 3).
- Khởi động môi trường phiên mới: `npm ci` (+ `cd functions && npm ci`) →
  `npm run emulators` → `npm run seed:emulator` → `npm run dev` → chọn vai
  trên sidebar (user demo đã seed).
