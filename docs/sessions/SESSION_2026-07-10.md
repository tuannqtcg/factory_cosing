# SESSION 2026-07-10 — Pha 3 M12.8 → M12.10: Target Costing, M12.9 (Cấu Hình/Tồn Kho/Tham Số), Security Review — HOÀN TẤT PHA 3

Pha hiện tại: 3 → 4 (Code xong, chuẩn bị merge) | Agent: Claude Code

Phiên bắt đầu bằng "phiên tiếp" (tiếp tục từ tối 2026-07-09, xem cuối
`SESSION_2026-07-08.md` mục "Việc treo cho phiên sau"). Chạy qua đêm nên các
mốc M12.8/M12.9 ghi ngày 2026-07-09 trong `docs/M12_PLAN.md`, phần M12.10 ghi
2026-07-10 (đúng lúc code) — 1 phiên hội thoại liên tục, không phải 2 phiên.

## Đã quyết (ADR mới)

- **ADR-015** (`docs/decisions/ADR-015-array-field-lock-rules.md`): khóa
  `thresholdPct` admin-only TRONG TỪNG phần tử `materials[]`/
  `inventory.metalInsert[]` bằng UNROLL theo index cố định trong
  `firestore.rules` (cận trên 8/10, đã biết thực tế nhỏ hơn nhiều). User được
  hỏi giữa 2 phương án (unroll thật vs defense-in-depth client-side), không tự
  tin chọn ("tôi chưa biết phải chọn cách nào cho chuẩn") — tôi khuyến nghị
  kèm lý do (mảng có bound rõ + đây là field bảo mật thật, không chỉ UX) rồi
  chốt theo khuyến nghị.
- (Không phải ADR mới, nhưng là quyết định phạm vi quan trọng) User chọn
  **"làm phần an toàn trước"** cho M12.9 khi phát hiện prototype 2 tab Tồn
  Kho/Tham Số vẽ TRƯỚC ADR-012 — chia M12.9 thành 4 sub-milestone (a rules fix
  / b Cấu Hình / c báo cáo Ống-PK / d Tồn Kho+Tham Số cần mockup riêng) thay
  vì cố làm hết 1 lượt.

## Đã làm

### M12.8 — Target Costing (T2/T3)
Mockup Pha 1 (artifact HTML/JS thuần — Artifact tool chặn CDN nên không tải
được React) → user duyệt ("layout ổn") → code thật gọi callable
`computeTargetCosting` có sẵn từ M12.4c. Vá 1 bug thật lúc verify: Firebase
Callable SDK JSON-encode `undefined` → `null`, làm field optional bị Zod
reject — chỉ đưa key vào request khi có giá trị. Verify thật: T2 khớp tuyệt
đối breakEvenKgYear vàng (108.761/18.942), T3 goal-seek + forward-verify +
case infeasible đúng `achievableRange`. Chi tiết đầy đủ: `docs/M12_PLAN.md`
mục M12.8.

### M12.9a — Vá 2 lỗ hổng `firestore.rules`
Phát hiện khi rà soát mở đầu M12.9: (1) bug đường dẫn chết
`inventory.pipe/fitting.priceLock.thresholdPct` (ADR-012 đã xóa 2 field này
khỏi document thật) khiến **MỌI lần `pricing` ghi `scenarios/{id}` bị từ
chối**, kể cả field không khóa — không bị test cũ bắt vì fixture test cũng
dùng shape lỗi thời; (2) thiếu khóa `products[]` admin-only dù `product.md`
đã đóng băng quy định này từ Pha 2. Sửa + fixture test cập nhật khớp schema
thật + test regression → 47/47.

### M12.9b — Màn Cấu Hình Nhà Máy
`src/features/config/ConfigScreen.tsx` — field-map lại đúng
`resources.pipe/fitting` + `costPool` (schema không có bucket "chung" như
mock giả định — mỗi Resource có field vận hành riêng). Khóa field client-side
đúng danh sách `resource.md`/`cost-pool.md` (19 field), thêm tab cho vai
`pricing` (trước chỉ admin thấy, sai vì pricing sửa được field không khóa).

### M12.9c — Báo cáo Ống CPVC + Phụ Kiện
`src/features/production-report/ProductionReport.tsx` (dùng chung 2 tab qua
prop `line`) — đọc-only, tái dùng `ScenarioOutput` đã có (priceLadder/cvp/
skuPriceChains), KHÔNG gọi lại cost breakdown nội bộ engine (hàm đó không
xuất field trung gian ra `ScenarioOutput`, tự gọi lại sẽ trùng lặp wiring của
`calculateScenario()`).

### M12.9d — Tồn Kho Compound + Tham Số + ADR-015
Mockup Pha 1 mới (KHÔNG porting prototype gốc) → user duyệt layout → hỏi
quyết định khóa field trong mảng (ADR-015, xem trên) → code:
`src/features/inventory/InventoryScreen.tsx` (đợt nhập theo material + ren
kim loại, thêm nguyên liệu mới — APPEND cuối mảng đúng giả định ADR-015),
`src/features/assumptions/AssumptionsScreen.tsx` (giá tái tạo/baseline/
markupVf/thuế theo material, ngưỡng khóa 🔒 admin-only khớp ADR-015 thật).
Verify thật: pricing thấy đúng 12 field khóa, admin sửa `thresholdPct` thành
công (rules unroll hoạt động đúng trên document thật, không chỉ fixture).
**Toàn bộ 9 tab của prototype đã dựng thật — hết placeholder.**

### M12.10 — Security Review (Pha 4 gate)
Dùng skill `security-review`, đối chiếu `firestore.rules` với bảng
`scenario.md` §6 từng ô một:
- Xác nhận default-deny, tách doc đúng (sales không đọc được `outputs/internal`/
  `scenarios/{id}`; `outputs/priceList` sạch — chỉ 4 field giá bán, không
  `materialCostPerUnit`/tồn kho).
- Xác nhận mọi Cloud Function (`onScenarioWrite`, `onPlanInputWrite`,
  `computeTargetCosting`) parse Zod server-side trước khi tin dữ liệu.
- Quét bí mật trong repo (`.env`, serviceAccount, private key, API key
  hardcode) — sạch, `.gitignore` đã chặn đúng.
- **Phát hiện + vá 2 contract doc bị TRÔI khỏi schema thật từ ADR-012**
  (ADR-012 §"Hệ quả" có liệt kê phải sửa nhưng chưa từng làm): `cost-pool.md`
  còn `markupVfPipe/Fitting`/`compoundImportTaxRate`/`customsLogisticsFeeRate`
  (đã bỏ từ ADR-012); `product.md` thiếu field `materialId`. Đã vá cả 2 +
  cập nhật `scenario.md` mục "Còn treo" (2/3 mục đã xong từ lâu nhưng chưa
  gạch; mục custom-claim-role viết rõ lại tình trạng — CHỈ dùng được cho
  Emulator, PHẢI thiết kế cơ chế cấp quyền thật trước khi trỏ project Firebase
  thật).
- **Bổ sung audit log cho "Chốt Baseline Mới"** (checklist bắt buộc của skill
  security-review, trước đó `updateDoc` thẳng không lưu vết) — doc mới
  `scenarios/{id}/priceLockAudit/{entryId}` APPEND ONLY (rules cấm
  update/delete kể cả admin): `PriceLockAuditEntryFieldsSchema` (schemas/
  scenario.ts) + `src/lib/priceLockAudit.ts` (helper dùng chung) + wiring vào
  `Dashboard.tsx` (`chotBaselineMoi`, ghi ngay sau updateDoc) và
  `AssumptionsScreen.tsx` (`handleSave`, diff baseline trước/sau so bản đã lưu
  gần nhất — bắt được cả trường hợp gõ tay, không chỉ bấm nút). Threading
  `user` (uid/email) từ `useAuth` xuống 2 màn qua `AppShell.tsx`. 8 test rules
  mới (tạo được/không được theo vai, đọc được/không, KHÔNG ai sửa/xóa được kể
  cả admin) → 55/55.
- Verify audit log THẬT (không chỉ fixture): emulator + seed + Playwright —
  sửa giá tái tạo material vượt ngưỡng 3% → MỞ KHÓA → bấm "Chốt Baseline Mới"
  + Lưu → đọc thẳng Firestore bằng Admin SDK xác nhận đúng 1 doc
  `{materialId, materialName, oldBaselineUsdPerKg: 3.03, newBaselineUsdPerKg: 6,
  changedByUid: demo-pricing, changedByEmail, changedByRole: pricing, at: <timestamp thật>}`.
- Đủ 5 cổng cuối cùng: `npm test` 328/328, `test:rules` 55/55, `test:functions`
  9/9, typecheck root+functions, build OK.

## Còn treo / việc phiên sau

- **Custom claim `role` cho Firebase Auth user thật** — ghi rõ trong
  `scenario.md` "Còn treo": cơ chế hiện tại (`scripts/seed-emulator.ts` gọi
  Admin SDK trực tiếp) CHỈ dùng được cho Emulator. Trước khi trỏ vào project
  Firebase thật PHẢI thiết kế 1 trong 2: Cloud Function `onCall` admin-only
  cấp/thu hồi role (có audit log riêng) hoặc quy trình vận hành thủ công
  ngoài app — chưa quyết, không phải rủi ro ngay (app hiện chỉ chạy Emulator).
- `PROJECT_SPEC.md`/`CHANGELOG.md`/`CONTEXT_PASTE.md` chưa đóng gói cho phiên
  sau (kit version) — nếu phiên sau cần, làm bước "đóng gói tri thức" trước
  khi merge lên nhánh mặc định (theo đúng thông lệ các phiên trước).
- Merge nhánh `claude/phien-tiep-ceoubx` lên nhánh mặc định — CHƯA làm trong
  phiên này (chỉ push lên nhánh làm việc), cần user xác nhận trước khi merge.
- Pha 3 (M1-M12.10) coi như HOÀN TẤT về mặt code + security review. Việc còn
  lại trước khi thật sự "xong dự án v1": merge, rồi có thể cân nhắc M12.9's
  phần chưa làm (gán `Product.materialId` cho SKU cụ thể qua UI — hiện chỉ
  làm được qua "thêm nguyên liệu mới" rỗng, chưa có màn gán) nếu nghiệp vụ
  thật cần thêm nguyên liệu và gán ngay cho SKU.

## Vi phạm constitution phát hiện được

- Không có vi phạm MỚI trong phiên này. 2 lỗ hổng rules (M12.9a) và 2 doc
  contract trôi (M12.10) đều là DEBT từ các phiên trước (ADR-012, 2026-07-07)
  chưa dọn hết — đã vá xong trong phiên này, không phải lỗi phát sinh mới.

---

# SESSION 2026-07-10 (phiên khác, cùng ngày) — Bắt đầu Pha 4: custom-claim role + Firebase project thật
Pha hiện tại: 4 (hạ tầng/security, code Pha 3 đã xong) | Agent: Claude Code

## Đã quyết
- **ADR-016**: user tạo Firebase project thật (`bmcosting-ver-2`) với Firestore
  Database ID = `manufacture` (không phải `(default)`) — chọn sửa code để trỏ
  đúng thay vì tạo lại database `(default)`. Xem
  `docs/decisions/ADR-016-named-firestore-database.md` cho đầy đủ lý do +
  cách verify.
- Việc "thiết kế custom claim `role` cho user thật" (còn treo từ M12.10,
  `scenario.md`) CHƯA bắt đầu code trong phiên này — user chọn hướng (a) Cloud
  Function onCall admin-only + audit log, chưa cần UI riêng — nhưng phiên rẽ
  sang xử lý ADR-016 trước (điều kiện tiên quyết: code phải nói chuyện đúng
  database mới code tiếp phần cấp quyền được). **Việc phiên sau**: viết
  `setUserRole` Cloud Function theo đúng hướng đã chọn.

## Đã làm
- Hướng dẫn user các bước tạo Firebase project thật (Console: tạo project →
  Blaze plan → Firestore → Auth Email/Password → Web app config → service
  account key) — việc ngoài code, user tự làm.
- Phát hiện + sửa toàn bộ chỗ hard-code giả định "chỉ có database `(default)`"
  khi user báo đã tạo database tên `manufacture`:
  - `functions/src/index.ts`: `defineString('FIRESTORE_DATABASE_ID', {default: '(default)'})`,
    dùng cho CẢ trigger (`database` option của `onDocumentWritten`) LẪN
    `getFirestore(id)` — nếu chỉ đổi 1 trong 2 chỗ, trigger sẽ lắng nghe nhầm
    database và không bao giờ chạy trên project thật (rủi ro đã ghi trong ADR-016).
  - `src/lib/firebase.ts`: `VITE_FIREBASE_DATABASE_ID` (optional), chỉ áp dụng
    khi KHÔNG chạy Emulator.
  - `firebase.json`: `firestore.database = "manufacture"` (xác nhận bằng đọc
    source `firebase-tools/lib/firestore/fsConfig.js` — field này chỉ ảnh
    hưởng lệnh deploy, không ảnh hưởng Emulator).
  - `.firebaserc`: thêm alias `"production": "bmcosting-ver-2"`.
  - `functions/.env.bmcosting-ver-2` (mới, KHÔNG phải secret, commit được):
    `FIRESTORE_DATABASE_ID=manufacture`.
  - `functions/.env.demo-costing-app` (mới): `FIRESTORE_DATABASE_ID=(default)`
    — **bắt buộc phải có**, nếu không `firebase emulators:exec` (chạy
    non-interactive, không có TTY) sẽ TREO VÔ HẠN chờ CLI hỏi giá trị tham số
    mới (`? Enter a string value for FIRESTORE_DATABASE_ID:`) — phát hiện
    được bằng cách chạy thật `npm run test:functions`, không đoán.
  - `.gitignore`: thêm 3 ngoại lệ (`!functions/.env.bmcosting-ver-2`,
    `!functions/.env.demo-costing-app`, `!.env.example`) vì rule chung
    `.env.*` chặn nhầm — cả 3 file đều không chứa secret.
  - `.env.example` (mới): liệt kê tên biến `VITE_FIREBASE_*` cần điền.
- **Verify THẬT bằng Emulator** (không chỉ đọc doc Firebase, xem ADR-016 mục
  "Rủi ro kỹ thuật"): script admin SDK độc lập xác nhận Firestore Emulator cô
  lập đúng 2 database tên khác nhau (ghi 1 bên không thấy ở bên kia) trước khi
  quyết định cách sửa.
- Đủ cổng: `npm test` 328/328, `npm run test:functions` 9/9, `npm run
  test:rules` 55/55, typecheck root + functions sạch, `npm run build` OK.

## Còn treo / việc phiên sau
- Deploy thật lên `bmcosting-ver-2` (`firebase deploy --project bmcosting-ver-2
  --only firestore:rules,firestore:indexes,functions`) CHƯA verify trong
  phiên này — không có quyền `firebase login` vào tài khoản Google của user.
  User cần tự deploy rồi xác nhận `outputs/internal` xuất hiện đúng trong
  database `manufacture` (không phải `(default)`) trên Console.
- `setUserRole` Cloud Function (custom claim `role`, hướng (a) đã chọn) —
  chưa viết, là việc chính của phiên sau, sau khi user xác nhận deploy OK.
- Root `.env`/service account key thật — user tự quản lý, KHÔNG được tạo/lưu
  trong phiên (chỉ hướng dẫn cách lấy).

## Vi phạm constitution phát hiện được
- Không có vi phạm nào bị phát hiện. Rủi ro "trigger im lặng không chạy" ở
  trên là rủi ro NGĂN CHẶN được trước khi xảy ra (chưa deploy thật), không
  phải lỗi đã xảy ra.

---

# SESSION 2026-07-10 (phiên khác, cùng ngày, tiếp) — Thử deploy thật + ADR-017 setUserRole

## Đã quyết
- **ADR-017** (`docs/decisions/ADR-017-custom-claim-role-management.md`):
  Cloud Function `setUserRole` (onCall, admin-only) + audit log
  `roleAudit/{entryId}` (top-level, append-only) + script
  `scripts/bootstrap-admin.ts` cho admin đầu tiên — đúng hướng (a) user chọn
  ở phiên trước, chưa cần UI riêng.
- User quyết định thêm: **giữ project Firebase dùng CHUNG**
  (`bmcosting-ver-2` có 9 database: `financial-suite`, `landingpage`,
  `sso-tcg`, `ai-studio-*`...) thay vì tách project riêng cho costing app —
  chấp nhận rủi ro vì đã cô lập bằng database riêng `manufacture` (ADR-016).

## Đã làm
- User upload trực tiếp Service Account key thật (`.json`) để tôi thử deploy
  hộ. Dùng `GOOGLE_APPLICATION_CREDENTIALS` trỏ vào key, xác nhận xác thực
  OK + liệt kê đúng project/database. Phát hiện quan trọng: service account
  mặc định ("Generate new private key") **KHÔNG có quyền deploy** — thiếu cả
  `cloudfunctions.functions.list` lẫn quyền `firebaserules.googleapis.com`
  (chỉ có `firebase.projects.get`) — không thể deploy rules HAY functions
  bằng key này. Đã thử 1 cách hợp lệ để vượt qua bước preflight-check API
  enablement (cache client-side xác nhận `firestore.googleapis.com` đã bật —
  sự thật đã verify qua `firestore:databases:list` thành công), nhưng bước
  ghi thật (`firebaserules.googleapis.com:test`) vẫn 403 — đây là giới hạn
  IAM thật, KHÔNG cố lách tiếp.
  **Xử lý an toàn**: dừng lại, KHÔNG dùng `--force`/thử cách vòng khác, báo
  rõ cho user, xóa file key khỏi container ngay sau khi dùng xong (đã hứa
  trước khi user upload), khuyến nghị user thu hồi key trên Console.
- User tự **Publish `firestore.rules` thủ công qua Console** (đúng database
  `manufacture`) — tôi gửi file qua SendUserFile thay vì dán inline.
- Cloud Functions: giải thích rõ vì sao KHÔNG thể "làm thủ công qua Console"
  cho codebase này (3 function import trực tiếp ~15 file `src/engine`/
  `src/schemas`, Console Inline Editor chỉ hợp cho hàm độc lập vài chục dòng)
  — vấn đề gốc là quyền IAM chứ không phải CLI-vs-Console, nên hướng đúng vẫn
  là user tự `firebase login` (tài khoản cá nhân, đủ quyền) rồi
  `firebase deploy --only functions`.
- Trong lúc chờ user deploy, viết luôn code cho ADR-017 (không cần chờ
  functions hiện có deploy xong mới viết thêm function mới — code xong sẽ đi
  cùng 1 lần deploy):
  - `src/schemas/role-management.ts`: `AppRoleSchema`, `SetUserRoleRequestSchema`,
    `SetUserRoleResultSchema`, `RoleAuditEntryFieldsSchema`.
  - `functions/src/index.ts`: export `setUserRole` — check admin, đọc
    `oldRole` từ claim hiện tại (an toàn nếu invalid/chưa có), giữ nguyên
    custom claim khác khi set, ghi `roleAudit/{entryId}`.
  - `firestore.rules`: collection `roleAudit` (chỉ admin đọc, không ai ghi
    trực tiếp).
  - `scripts/bootstrap-admin.ts`: cấp admin đầu tiên bằng Admin SDK trực tiếp
    (guard chặn chạy nhầm vào Emulator).
  - Test: `tests/functions/set-user-role.test.ts` (4 test, Auth+Firestore+
    Functions Emulator thật — set claim thật rồi đọc lại độc lập bằng
    `auth.getUser()`, không chỉ tin response) + 3 test rules mới
    (`tests/rules/firestore.rules.test.ts`) cho `roleAudit`.
  - Cập nhật `docs/contracts/scenario.md` "Còn treo" — đánh dấu XONG, trỏ
    ADR-017.
- Đủ 5 cổng: `npm test` 328/328, `test:functions` 13/13 (+4), `test:rules`
  58/58 (+3), typecheck root+functions, build OK.

## Còn treo / việc phiên sau
- User cần tự `firebase login` + `firebase use production` +
  `firebase deploy --only functions` (rules đã publish thủ công xong).
- Sau khi deploy: chạy `scripts/bootstrap-admin.ts` để cấp admin đầu tiên
  trên user Auth thật, rồi verify `setUserRole` hoạt động đúng trên project
  thật (không chỉ Emulator) — cần `GOOGLE_APPLICATION_CREDENTIALS` trỏ 1
  service account key MỚI (khuyến nghị thu hồi key cũ đã dùng trong phiên
  này vì đã hiện trong lịch sử chat, dù không đủ quyền gây hại).
- Chưa dựng UI cấp role (cố ý, xem ADR-017 mục 4) — làm sau nếu nhu cầu thật
  xuất hiện.

## Vi phạm constitution phát hiện được
- Không có vi phạm mới. Việc thử 1 cách vượt qua preflight-check API
  enablement (client-side cache) KHÔNG phải lách bảo mật — chỉ xác nhận lại
  1 sự thật (API đã bật, verify độc lập qua lệnh khác) để bỏ qua 1 bước kiểm
  tra dư thừa mà chính service account thiếu quyền gọi; khi gặp giới hạn IAM
  THẬT (firebaserules API) ở bước có ý nghĩa (ghi rules), đã dừng ngay, không
  tìm cách lách tiếp.
