# SESSION 2026-07-10 — Pha 3 M12.8 → M12.10: Target Costing, M12.9 (Cấu Hình/Tồn Kho/Tham Số), Security Review — HOÀN TẤT PHA 3

Pha hiện tại: 3 → 4 (Code xong, chuẩn bị merge) | Agent: Claude Code

Phiên bắt đầu bằng "phiên tiếp" (tiếp tục từ tối 2026-07-09, xem cuối
`SESSION_2026-07-08.md` mục "Việc treo cho phiên sau"). Chạy qua đêm nên các
mốc M12.8/M12.9 ghi ngày 2026-07-09 trong `docs/M12_PLAN.md`, phần M12.10 ghi
2026-07-10 (đúng lúc code) — 1 phiên hội thoại liên tục, không phải 2 phiên.

## Đã quyết (ADR mới)

- **ADR-015** (`docs/decisions/ADR-015-array-field-lock-rules.md`): khóa
  `thresholdPct` admin-only TRONG TỪNG phần tử `materials[]`/
  `inventory.metalInsert[]` bằng UNROLL theo index cố định trong
  `firestore.rules` (cận trên 8/10, đã biết thực tế nhỏ hơn nhiều). User được
  hỏi giữa 2 phương án (unroll thật vs defense-in-depth client-side), không tự
  tin chọn ("tôi chưa biết phải chọn cách nào cho chuẩn") — tôi khuyến nghị
  kèm lý do (mảng có bound rõ + đây là field bảo mật thật, không chỉ UX) rồi
  chốt theo khuyến nghị.
- (Không phải ADR mới, nhưng là quyết định phạm vi quan trọng) User chọn
  **"làm phần an toàn trước"** cho M12.9 khi phát hiện prototype 2 tab Tồn
  Kho/Tham Số vẽ TRƯỚC ADR-012 — chia M12.9 thành 4 sub-milestone (a rules fix
  / b Cấu Hình / c báo cáo Ống-PK / d Tồn Kho+Tham Số cần mockup riêng) thay
  vì cố làm hết 1 lượt.

## Đã làm

### M12.8 — Target Costing (T2/T3)
Mockup Pha 1 (artifact HTML/JS thuần — Artifact tool chặn CDN nên không tải
được React) → user duyệt ("layout ổn") → code thật gọi callable
`computeTargetCosting` có sẵn từ M12.4c. Vá 1 bug thật lúc verify: Firebase
Callable SDK JSON-encode `undefined` → `null`, làm field optional bị Zod
reject — chỉ đưa key vào request khi có giá trị. Verify thật: T2 khớp tuyệt
đối breakEvenKgYear vàng (108.761/18.942), T3 goal-seek + forward-verify +
case infeasible đúng `achievableRange`. Chi tiết đầy đủ: `docs/M12_PLAN.md`
mục M12.8.

### M12.9a — Vá 2 lỗ hổng `firestore.rules`
Phát hiện khi rà soát mở đầu M12.9: (1) bug đường dẫn chết
`inventory.pipe/fitting.priceLock.thresholdPct` (ADR-012 đã xóa 2 field này
khỏi document thật) khiến **MỌI lần `pricing` ghi `scenarios/{id}` bị từ
chối**, kể cả field không khóa — không bị test cũ bắt vì fixture test cũng
dùng shape lỗi thời; (2) thiếu khóa `products[]` admin-only dù `product.md`
đã đóng băng quy định này từ Pha 2. Sửa + fixture test cập nhật khớp schema
thật + test regression → 47/47.

### M12.9b — Màn Cấu Hình Nhà Máy
`src/features/config/ConfigScreen.tsx` — field-map lại đúng
`resources.pipe/fitting` + `costPool` (schema không có bucket "chung" như
mock giả định — mỗi Resource có field vận hành riêng). Khóa field client-side
đúng danh sách `resource.md`/`cost-pool.md` (19 field), thêm tab cho vai
`pricing` (trước chỉ admin thấy, sai vì pricing sửa được field không khóa).

### M12.9c — Báo cáo Ống CPVC + Phụ Kiện
`src/features/production-report/ProductionReport.tsx` (dùng chung 2 tab qua
prop `line`) — đọc-only, tái dùng `ScenarioOutput` đã có (priceLadder/cvp/
skuPriceChains), KHÔNG gọi lại cost breakdown nội bộ engine (hàm đó không
xuất field trung gian ra `ScenarioOutput`, tự gọi lại sẽ trùng lặp wiring của
`calculateScenario()`).

### M12.9d — Tồn Kho Compound + Tham Số + ADR-015
Mockup Pha 1 mới (KHÔNG porting prototype gốc) → user duyệt layout → hỏi
quyết định khóa field trong mảng (ADR-015, xem trên) → code:
`src/features/inventory/InventoryScreen.tsx` (đợt nhập theo material + ren
kim loại, thêm nguyên liệu mới — APPEND cuối mảng đúng giả định ADR-015),
`src/features/assumptions/AssumptionsScreen.tsx` (giá tái tạo/baseline/
markupVf/thuế theo material, ngưỡng khóa 🔒 admin-only khớp ADR-015 thật).
Verify thật: pricing thấy đúng 12 field khóa, admin sửa `thresholdPct` thành
công (rules unroll hoạt động đúng trên document thật, không chỉ fixture).
**Toàn bộ 9 tab của prototype đã dựng thật — hết placeholder.**

### M12.10 — Security Review (Pha 4 gate)
Dùng skill `security-review`, đối chiếu `firestore.rules` với bảng
`scenario.md` §6 từng ô một:
- Xác nhận default-deny, tách doc đúng (sales không đọc được `outputs/internal`/
  `scenarios/{id}`; `outputs/priceList` sạch — chỉ 4 field giá bán, không
  `materialCostPerUnit`/tồn kho).
- Xác nhận mọi Cloud Function (`onScenarioWrite`, `onPlanInputWrite`,
  `computeTargetCosting`) parse Zod server-side trước khi tin dữ liệu.
- Quét bí mật trong repo (`.env`, serviceAccount, private key, API key
  hardcode) — sạch, `.gitignore` đã chặn đúng.
- **Phát hiện + vá 2 contract doc bị TRÔI khỏi schema thật từ ADR-012**
  (ADR-012 §"Hệ quả" có liệt kê phải sửa nhưng chưa từng làm): `cost-pool.md`
  còn `markupVfPipe/Fitting`/`compoundImportTaxRate`/`customsLogisticsFeeRate`
  (đã bỏ từ ADR-012); `product.md` thiếu field `materialId`. Đã vá cả 2 +
  cập nhật `scenario.md` mục "Còn treo" (2/3 mục đã xong từ lâu nhưng chưa
  gạch; mục custom-claim-role viết rõ lại tình trạng — CHỈ dùng được cho
  Emulator, PHẢI thiết kế cơ chế cấp quyền thật trước khi trỏ project Firebase
  thật).
- **Bổ sung audit log cho "Chốt Baseline Mới"** (checklist bắt buộc của skill
  security-review, trước đó `updateDoc` thẳng không lưu vết) — doc mới
  `scenarios/{id}/priceLockAudit/{entryId}` APPEND ONLY (rules cấm
  update/delete kể cả admin): `PriceLockAuditEntryFieldsSchema` (schemas/
  scenario.ts) + `src/lib/priceLockAudit.ts` (helper dùng chung) + wiring vào
  `Dashboard.tsx` (`chotBaselineMoi`, ghi ngay sau updateDoc) và
  `AssumptionsScreen.tsx` (`handleSave`, diff baseline trước/sau so bản đã lưu
  gần nhất — bắt được cả trường hợp gõ tay, không chỉ bấm nút). Threading
  `user` (uid/email) từ `useAuth` xuống 2 màn qua `AppShell.tsx`. 8 test rules
  mới (tạo được/không được theo vai, đọc được/không, KHÔNG ai sửa/xóa được kể
  cả admin) → 55/55.
- Verify audit log THẬT (không chỉ fixture): emulator + seed + Playwright —
  sửa giá tái tạo material vượt ngưỡng 3% → MỞ KHÓA → bấm "Chốt Baseline Mới"
  + Lưu → đọc thẳng Firestore bằng Admin SDK xác nhận đúng 1 doc
  `{materialId, materialName, oldBaselineUsdPerKg: 3.03, newBaselineUsdPerKg: 6,
  changedByUid: demo-pricing, changedByEmail, changedByRole: pricing, at: <timestamp thật>}`.
- Đủ 5 cổng cuối cùng: `npm test` 328/328, `test:rules` 55/55, `test:functions`
  9/9, typecheck root+functions, build OK.

## Còn treo / việc phiên sau

- **Custom claim `role` cho Firebase Auth user thật** — ghi rõ trong
  `scenario.md` "Còn treo": cơ chế hiện tại (`scripts/seed-emulator.ts` gọi
  Admin SDK trực tiếp) CHỈ dùng được cho Emulator. Trước khi trỏ vào project
  Firebase thật PHẢI thiết kế 1 trong 2: Cloud Function `onCall` admin-only
  cấp/thu hồi role (có audit log riêng) hoặc quy trình vận hành thủ công
  ngoài app — chưa quyết, không phải rủi ro ngay (app hiện chỉ chạy Emulator).
- `PROJECT_SPEC.md`/`CHANGELOG.md`/`CONTEXT_PASTE.md` chưa đóng gói cho phiên
  sau (kit version) — nếu phiên sau cần, làm bước "đóng gói tri thức" trước
  khi merge lên nhánh mặc định (theo đúng thông lệ các phiên trước).
- Merge nhánh `claude/phien-tiep-ceoubx` lên nhánh mặc định — CHƯA làm trong
  phiên này (chỉ push lên nhánh làm việc), cần user xác nhận trước khi merge.
- Pha 3 (M1-M12.10) coi như HOÀN TẤT về mặt code + security review. Việc còn
  lại trước khi thật sự "xong dự án v1": merge, rồi có thể cân nhắc M12.9's
  phần chưa làm (gán `Product.materialId` cho SKU cụ thể qua UI — hiện chỉ
  làm được qua "thêm nguyên liệu mới" rỗng, chưa có màn gán) nếu nghiệp vụ
  thật cần thêm nguyên liệu và gán ngay cho SKU.

## Vi phạm constitution phát hiện được

- Không có vi phạm MỚI trong phiên này. 2 lỗ hổng rules (M12.9a) và 2 doc
  contract trôi (M12.10) đều là DEBT từ các phiên trước (ADR-012, 2026-07-07)
  chưa dọn hết — đã vá xong trong phiên này, không phải lỗi phát sinh mới.
