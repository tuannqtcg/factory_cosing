# SESSION 2026-07-13 — Firebase Auth Seeding & Progressive Disclosure Dashboard UX
Pha hiện tại: 4 | Agent: Gemini

## Đã quyết
- **ADR-018 (UX Redesign)**: Áp dụng nguyên lý **Progressive Disclosure** cho màn hình Dashboard. Thay vì show toàn bộ 4 khối thông tin lớn cùng lúc gây quá tải nhận thức, chia thành 4 Tab chuyên sâu:
  1. Tổng Quan (CEO/BOD view: 4 chỉ số nhịp đập)
  2. Sản xuất (Mô phỏng công suất)
  3. Đầu tư & Tài chính (Phân bổ tài sản chung, ROI, CVP)
  4. Chiến lược giá (Top-down, Thang giá 5 bậc)
- **Làm rõ ranh giới Sản xuất (Ống vs Phụ kiện)**: Khẳng định hệ thống mô phỏng 3 mức công suất hiện tại chỉ thiết kế riêng cho **Ống (đùn liên tục)**. Phụ kiện tính bằng giờ máy (ép phun) nên độc lập hoàn toàn. Giao diện được đổi nhãn để tránh nhầm lẫn.

## Đã làm
- **Firebase Auth (Real)**: Viết script `scripts/create-demo-users.ts` để tạo 4 tài khoản thật (demo) trên Firebase Auth Production của dự án `bmcosting-ver-2`, đồng thời gán custom claims (`role`) chính xác (admin, pricing, sales, production). Điều này sửa lỗi "Không đăng nhập được user demo" khi chuyển qua Database thật.
- **Tái cấu trúc UI Dashboard (Phase 1 & Phase 2)**:
  - Tách thành phần `Dashboard.tsx` thành giao diện Tab.
  - Sửa lỗi cú pháp Vite (missing `</div>`) sau khi di chuyển các khối JSX lớn.
  - Sửa lỗi `NaN` cho các hiển thị Giá thành / Phí gia công của Phụ kiện (sử dụng đúng `fullCostPerKgRef` và `processingCostPerKgRef`).
  - Đưa **Global Material Selector** lên Header để áp dụng chọn vật liệu (BlazeMaster / Corzan) cho toàn bộ Dashboard đồng nhất.
  - Thêm biểu đồ **Progress Bar** hiển thị phần trăm huy động công suất (theo ca) trong tab Sản xuất.
  - Thêm **Biểu đồ điểm hòa vốn CVP** (Recharts) trong tab Đầu tư & Tài chính.

## Còn treo / việc phiên sau (Phase 3)
- **Kiểm thử & Phân quyền (Security UI)**: Đăng nhập thử nghiệm 4 vai trò thật (Admin, Pricing, Sales, Production) và đảm bảo các Rules đã lập (Sales không thấy chi phí, Production chỉ tập trung kế hoạch) được giao diện khóa hiển thị triệt để và an toàn.

## Vi phạm constitution phát hiện được
- Không có. Logic tính toán hoàn toàn giữ nguyên, chỉ tái cấu trúc giao diện theo góc nhìn người dùng.
