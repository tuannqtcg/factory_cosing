# SESSION 2026-07-15 — Làm xanh lại suite parity sau "big update" v3.7 (ADR-019)
Pha hiện tại: 4 | Agent: Claude Code

## Đã quyết
- **ADR-019**: Giữ nguyên bộ tham số Ống v3.7 user nhập ở commit `7d134f7`
  (3 máy đùn, khấu hao máy đùn 10 năm, khuôn kéo/cắt 3,7265 tỷ khấu hao riêng
  3 năm, bảo trì 120tr) + công thức tách khấu hao trong `pipe.ts`; tái lập số
  vàng dẫn xuất từ engine forward SAU KHI hand-verify độc lập chuỗi chi phí.
  Khi có Excel v3.7 chính thức sẽ trích lại bằng cached-value.

## Đã làm
- Chẩn đoán: commit `7d134f7` đổi input `tests/fixtures/pipe.json` + engine
  nhưng không cập nhật số vàng → 47 assertion parity đỏ trên 12 file (suite
  xanh 328/328 tại commit trước đó `014bf42` — đã kiểm chứng bằng worktree).
- Hand-verify: khấu hao mới 2.326.037.866,67 đ/năm → định phí Ống
  4.989.390.480,69 đ/năm (+42,31%) → fullCost 108.711,86 đ/kg, VF 135.889,83,
  hòa vốn 141.637,79 kg, chi phí nhàn rỗi kịch bản A 11.750,73 đ/kg — tất cả
  khớp engine tới từng đồng (tỉ lệ thuận định phí).
- Cập nhật fixtures: `pipe.json` (costAtNormalCapacity/priceLadderByDN/
  capacityTiers/cvp), `dashboard.json` (thang 5 bậc cả 2 dòng — bậc 4 Phụ kiện
  đổi theo do hòa vốn toàn DN tham chiếu chéo; investment: tổng vốn 26,72 tỷ,
  payback 1,24 năm), `price-list.json` (8 dòng Ống), `price-lock-scenarios.json`
  (BE 5 kịch bản ADR-004).
- Cập nhật expectation inline: `pipe.test.ts`, `corzan.test.ts` (Ống Corzan
  fullCost 116.443,97; DN50 niêm yết 374.700), `plan.test.ts`,
  `plan-support.test.ts`.
- `npm test` 328/328 xanh; `npm run typecheck` xanh.
- ROADMAP: chuyển Firebase thật + Custom Claims (ADR-016/017, đã xong từ trước)
  lên mục hoàn thành; việc treo Security UI đưa lên đầu danh sách việc tiếp.

## Còn treo / việc phiên sau
- **Kiểm thử & Phân quyền (Security UI)** — nguyên trạng từ phiên 2026-07-13:
  đăng nhập thử 4 vai thật, xác nhận UI khóa hiển thị/nút đúng vai; chạy
  `npm run test:rules` + `npm run test:functions` (cần Firebase emulator,
  môi trường này chưa chạy được).
- Khi có Excel v3.7 chính thức: trích lại fixture (ghi chú trong ADR-019).

## Vi phạm constitution phát hiện được
- Commit `7d134f7` được đẩy lên khi suite đang đỏ (vi phạm luật "npm test phải
  xanh trước mọi commit") và đổi công thức engine không kèm ADR — phiên này
  vá lại bằng ADR-019 + tái lập số vàng.
