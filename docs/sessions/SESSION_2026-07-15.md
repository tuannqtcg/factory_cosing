# SESSION 2026-07-15 — Trợ Lý CEO (Pha 0-1): giá X → giá bán → hiệu quả năm + nút AI tư vấn
Pha hiện tại: 1 (prototype, CHỜ DUYỆT UI) | Agent: Claude Code

## Đã quyết (nếu là quyết định kiến trúc → tạo ADR, ghi số ADR ở đây)
- Yêu cầu user (CEO — đàm phán hợp đồng, đặt margin): "giá nguyên liệu X → giá
  bán bao nhiêu; chạy 3 ca số đợt tối đa/năm → hiệu suất bao nhiêu; nút AI tư
  vấn sau khi chạy số". Là MÀN HÌNH MỚI (tầng chiến lược ADR-006) → theo đúng
  quy trình 4 pha: làm brief + prototype trước, KHÔNG code production.
- AI tư vấn: Pha 1 mock rule-based tại chỗ; kiến trúc thật (callable Cloud
  Function `adviseScenario` gọi Claude API server-side, không key ở client,
  pattern ADR-010/013) sẽ ra ADR RIÊNG ở Pha 2 sau khi UI được duyệt — chưa
  tạo ADR ở phiên này (chưa phải quyết định chốt).

## Đã làm
- `docs/briefs/BRIEF-2026-07-15-ceo-planner.md` — brief Pha 0 (vấn đề, phạm vi,
  ngoài phạm vi, ràng buộc Pha 3).
- `prototype/ceo-planner.html` — prototype tự chạy (không build/API/Firebase):
  Bước 1 nhập giá compound ống/PK + margin (markup trên giá thành | margin trên
  giá bán) + bậc ca/huy động + preset "Chuẩn Excel v3.4"/"Chạy tối đa 3 ca";
  Bước 2 giá bán đề xuất, thang giá 5 bậc có vị trí "GIÁ CỦA BẠN" + cảnh báo
  theo bậc, KPI năm (sản lượng, hòa vốn %, lãi dòng), khối toàn nhà máy
  (doanh thu, EBIT, EBIT/DT, payback dòng tiền), bảng giá ống theo DN; Bước 3
  nút 🤖 AI tư vấn (mock, loading giả, có ghi chú MOCK). Đủ 4 trạng thái
  rỗng/tải/dữ liệu/lỗi; vi-VN; tabular-nums; responsive.
- KIỂM CHỨNG PARITY bằng script Node chạy đúng phần công thức nhúng: khớp
  tuyệt đối fixture v3.4 — thang 5 bậc ống (100.663,42 / 104.369,04 /
  106.318,88 / 111.070,84 / 132.898,60), PK (127.939,82 / 136.430,81 /
  180.604,12 / 189.644,95 / 252.845,77), EBIT 16.370.785.391, payback 0,8161,
  BE 17,54% / 42,16%, đủ 6 bậc `mhrByCapacityTier`.
- Phát hiện quy ước Excel khi quét bậc ca (đã ghi vào brief + comment
  prototype): lương theo ca, điện/nước theo giờ máy, khấu hao + PHÂN BỔ CHI PHÍ
  CHUNG GIỮ NGUYÊN tại công suất chuẩn (không phân bổ lại) — nếu Pha 3 tự phân
  bổ lại sẽ lệch fixture (584.345 vs 607.914 ở 3 ca × 60%).
- Cập nhật `docs/ROADMAP.md` (mục 3 mới) + `prototype/README.md`.
- Xuất bản artifact để user bấm thử prototype.

## Còn treo / việc phiên sau
- ⛔ CỔNG PHA 1: chờ user duyệt layout/UX prototype → mới sang Pha 2.
- Pha 2: ADR `adviseScenario` (dữ liệu nào được gửi cho AI — chỉ output đã
  tính, không dữ liệu thô; vai nào được bấm; audit log) + Zod contract.
- Pha 3: `src/features/ceo-planner/`, thay toàn bộ công thức nhúng bằng
  `src/engine/*`; thêm parity test cho quy ước "không phân bổ lại" nêu trên.
- Câu hỏi mở cho user: margin mặc định hiểu theo markup trên giá thành (như
  Excel 25%/40%) hay margin trên giá bán? Prototype cho chọn cả 2, cần chốt
  mặc định khi duyệt.

## Vi phạm constitution phát hiện được (nếu có)
- Không. (Prototype nhúng công thức thuần là được phép ở Pha 1 theo skill
  `prototype`, đã đánh dấu rõ "sẽ thay bằng engine".)
