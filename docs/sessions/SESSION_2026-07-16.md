# SESSION 2026-07-16 — Ổn định parity v3.7 + hợp nhất một view CEO
Pha hiện tại: 4 | Agent: Claude Code

## Đã quyết (nếu là quyết định kiến trúc → tạo ADR, ghi số ADR ở đây)
- **ADR-019** (phiên này xác nhận đã có): tái lập số vàng parity theo bộ tham số
  Ống v3.7 sau commit "big update" (tách khấu hao khuôn kéo/cắt `moldDepreciationYears`,
  3 máy đùn, phí bảo trì mới). Suite về xanh 328/328.
- **ADR-020**: Hợp nhất UI thành **một view CEO duy nhất** — bỏ role switcher
  ("Xem Như Vai") + bỏ lọc tab theo vai (`ROLE_TAB_ACCESS`). Backend theo vai
  (firestore.rules, custom claim, ADR-006/017) GIỮ NGUYÊN, chỉ gộp trải nghiệm
  client. Theo yêu cầu người dùng: "không cần xem theo role nữa, một view CEO là
  đủ; cần cấu hình tham số nào thì vào chi tiết sửa sau".

## Đã làm
- **Xác minh trạng thái repo**: bộ test parity 328/328 xanh, ADR-019 đã commit và
  đồng bộ origin (việc sửa test đỏ từ "big update" đã hoàn tất trước phiên này).
- **Refactor `AppShell.tsx` (ADR-020)**:
  - Bỏ `ROLE_DEFS`, menu "Chọn Vai", state `isRoleMenuOpen`, lọc tab theo vai.
  - Sidebar hiện TOÀN BỘ màn, chia 2 nhóm theo mục đích: **ĐIỀU HÀNH** (Tổng Quan,
    Bảng Giá, Kế Hoạch SX, Phân Tích Định Giá) và **CẤU HÌNH & DỮ LIỆU** (Tồn Kho,
    Danh Mục SP, Cấu Hình, Tham Số, Ống, Phụ Kiện).
  - Tự đăng nhập vai `admin` khi tải (`useEffect` gọi `switchRole('admin')` khi
    signed-out); thất bại → hiện nút "Thử lại". Truyền cố định `role='admin'` xuống
    mọi màn nên `canEdit`/`canSeeCostDetail` tự bật hết — không phải gỡ gating từng
    component (giữ để đảo ngược).
  - Footer sidebar đổi từ badge vai + dropdown → nhãn tĩnh "GÓC NHÌN: ĐIỀU HÀNH (CEO)".
- **Nghiệm thu**: `npm run typecheck` xanh, `vitest run` 328/328, `vite build` OK
  (674 modules; cảnh báo chunk-size là sẵn có).
- **Verify thật trên emulator (Playwright, Chromium có sẵn)**: dựng emulator Suite
  (firestore/functions/auth) + seed baseline → chụp view CEO. Xác nhận: tự đăng nhập
  `admin@demo.local`, sidebar 2 nhóm hiển thị đủ 10 màn, Dashboard render đủ 6 thẻ
  KPI (Doanh thu 95,60 tỷ · EBIT 16,74 tỷ · Hòa vốn 37,29 tỷ · Thu hồi vốn 1,2 năm),
  drill sang Cấu Hình Nhà Máy sửa được (nút LƯU & CẬP NHẬT bật).
- **Vá bug chặn dashboard (phát hiện khi verify, KHÔNG do ADR-020)**: Cloud Function
  `onScenarioWrite` văng `Cannot use "undefined" as a Firestore value` ở field
  `skuPriceChains.*.materialDesignationCode` — do "big update" thêm 2 mã optional
  (ADR-013) rồi set thẳng `undefined` khi material thiếu mã; Firestore từ chối ghi
  `undefined` → outputs/internal + priceList KHÔNG BAO GIỜ được ghi → UI kẹt
  "Đang tải…". Bộ test parity không bắt được vì gọi `PriceListDocSchema.parse` trực
  tiếp, không ghi Firestore. Sửa `functions/src/index.ts::toPriceListDoc`: chỉ đính
  2 field khi có giá trị (conditional spread) — đúng nghĩa `.optional()`. Sau vá,
  onScenarioWrite chạy sạch, dashboard hiện đủ số liệu.

## Trợ Lý CEO — DUYỆT Pha 1 + LÀM Pha 2 (ADR-021, ADR-022)
- **Bối cảnh**: user chỉ ra màn "Phiếu giá" họ mô tả nằm ở nhánh
  `claude/production-financial-planner-vjrjb0` (prototype `ceo-planner.html`,
  7 vòng duyệt Pha 1). Render kiểm tra trên Chromium: chạy đủ Bước 1→2→3, không
  lỗi. User **DUYỆT cổng Pha 1**.
- **Import** prototype + brief vào nhánh đang làm (frozen Pha 1 artifacts).
- **Pha 2 — đóng băng hợp đồng**:
  - `src/schemas/ceo-planner.ts`: `CeoPlannerRequest/Result`, `CeoLineResult`,
    `CeoLadder`(5 bậc), `CeoFactorySummary` (payback nullable, không Infinity),
    bảng DN + 83 SKU, `CeoAdviceRequest/Result`. Export qua `schemas/index.ts`.
  - `docs/contracts/ceo-planner.md`: endpoint (planner = engine client-side;
    adviseScenario = callable), phân quyền admin/pricing, ràng buộc parity.
  - **ADR-021** (planner): schema; "chọn dòng 1 nơi" là ràng buộc UI không phải
    schema; thuê mặt bằng là INPUT của màn (thay `annualLandRent` khi tính, KHÔNG
    sửa baseline → parity giữ); tính bằng engine pure client-side; quyền admin/pricing.
  - **ADR-022** (adviseScenario): callable gọi Claude API server-side, key qua
    Firebase Secret (không ở client — AGENTS.md #3), chỉ gửi OUTPUT đã tính, fallback
    mock rule-based, audit log `adviceAudit`.
  - Test smoke `tests/unit/ceo-planner-schema.test.ts` (8 assertion). Suite 336/336.
- **Thuế TNDN 20% — user XÁC NHẬN** (2026-07-16), cập nhật vào ADR-021/contract/brief.

## Trợ Lý CEO — LÀM Pha 3 (code thật, ADR-021/022)
- **Engine `src/engine/ceo-planner.ts`**: áp override CEO (giá compound, ca, huy động,
  thuê mặt bằng, tỷ giá) lên ScenarioInput rồi gọi LẠI `calculateScenario` +
  `calculateDashboardKpis` — KHÔNG chép công thức. Bảng DN/SKU back-out fullCost =
  vfPrice/(1+markupVf) từ skuPriceChains. `CIT_RATE=0.20`.
- **Parity `tests/parity/ceo-planner.test.ts`** (7 test): tại preset "Chuẩn Excel v3.4",
  planner khớp TUYỆT ĐỐI output orchestrator (chứng minh tái dùng, không trôi số).
  Ghi nhận: phụ kiện 252.845,8 = gold brief; Ống 135.889,8 (v3.7 sau ADR-019 — gold
  brief 132.898,6 là số tiền-v3.7). Payback 1,24 năm (v3.7 vốn đầu tư Ống lớn hơn).
- **Màn `src/features/ceo-planner/CeoPlannerScreen.tsx`**: 3 bước (nhập → chạy số
  client-side → AI), gắn vào view CEO (AppShell, nhóm ĐIỀU HÀNH, tab "Trợ Lý CEO").
- **Callable `adviseScenario`** (functions/index.ts): auth admin/pricing → Claude API
  server-side (key qua Secret `ANTHROPIC_API_KEY`, model `claude-opus-4-8` qua param)
  → fallback `generateCeoAdviceMock` khi chưa cấu hình/lỗi → audit `adviceAudit`.
  Thêm dep `@anthropic-ai/sdk` (server-only).
- **Verify thật trên emulator + Playwright**: màn render đủ số liệu (giá 135.890/252.846,
  LN trước thuế 16,7 tỷ, thu hồi vốn 1,2 năm — khớp parity), nút AI hiện panel (qua
  mock cục bộ vì callable bị CORS trong emulator — đúng thiết kế "nút không bao giờ chết").
- **Nghiệm thu**: typecheck (root + functions) xanh · suite 343/343 · vite build ngầm OK.
- **Còn treo Pha 4/vận hành**: bật Claude API thật (set secret ANTHROPIC_API_KEY khi
  deploy) — hiện dùng mock; parity brief số Ống là tiền-v3.7 (đã ghi rõ, không đuổi
  theo số cũ).

## Đóng hở Pha 3 (rule + test backend)
- **Rule `adviceAudit`** (ADR-022 §6, trước bỏ sót): thêm vào `firestore.rules` —
  admin/pricing đọc, client cấm ghi (chỉ Admin SDK). + 3 test rules mới.
- **Test functions `advise-scenario.test.ts`** (3 test, emulator thật): pricing/admin
  gọi được → CeoAdviceResult mock + ghi adviceAudit (không lưu nội dung); sales/
  production `permission-denied`, anon `unauthenticated`; request sai schema
  `invalid-argument`.
- **Sửa emulator treo**: `defineString('ADVISE_MODEL')` bị hỏi tương tác khi vắng
  trong `.env` → làm `emulators:exec` treo. Đổi model đọc thẳng `process.env.ADVISE_MODEL
  ?? 'claude-opus-4-8'` (bỏ param). Giữ `defineSecret('ANTHROPIC_API_KEY')` cho prod.
- **Chạy đủ 3 bộ test backend**: `test:rules` **61/61**, `test:functions` **16/16**,
  `npm test` **343/343**. typecheck root + functions xanh.

## Hoàn thiện bảng báo giá + seed owner
- **A#3 — cột "Ren KL/cái"**: engine `ceo-planner.ts` tính ren/cái = `insertQtyPerUnit
  × pricingPrice` (khóa giá ren ADR-008, tái dùng `out.priceLock.metalInsertByCatalogEntry`).
  11 SKU họ ren hiện đúng (16.200/21.100/28.800 đ/cái — khớp tuyệt đối prototype). Thêm
  cột trên màn. Parity 7/7 giữ nguyên (chỉ kiểm sellingPrice).
- **A#4 — Corzan +10% đơn trọng**: KHÔNG cần sửa — `buildCorzanScenarioInput` đã nhân
  `pipeUnitWeightFactor` (1.1) vào ống Corzan; bảng DN đọc `unitWeightKgPerM` trực tiếp
  nên tự đúng cho cả 2 dòng.
- **Seed owner `tuannq6886@gmail.com` (role admin = toàn quyền)**: thêm vào `seed-emulator.ts`
  (dev) + `create-demo-users.ts` (production). Verify emulator: `✓ OWNER tuannq6886@gmail.com
  (role=admin)`. Suite 343/343, typecheck xanh.

## Còn treo / việc phiên sau
- **Siết đăng nhập production thật**: hiện auto sign-in demo admin (posture y như
  role switcher cũ — 1 click, không mật khẩu). Khi mở cho nhiều người dùng thật cần
  form login + tắt tài khoản demo. Hoãn, ghi rõ ở ADR-020 §Hệ quả.
- **Progressive disclosure cho các màn cấu hình nặng** (Cấu Hình, Tham Số): hiện mới
  Dashboard chia tab (ADR-018); các màn nhập liệu vẫn đổ một lượt. Cân nhắc sau nếu
  người dùng thấy rối.
- Chờ danh mục ống SCH80 chuẩn từ nhà cung cấp để cập nhật Seed Data (từ ROADMAP cũ).

## Vi phạm constitution phát hiện được (nếu có)
- Không có. Đổi thiết kế UI đã ghi ADR-020 trước khi code (đúng luật "đổi thiết kế
  Pha 3 → ghi ADR mới"). Engine/schema/rules không bị chạm nên parity giữ nguyên.

## Đăng nhập production thật (ADR-023)
- **Bỏ auto sign-in** demo admin (ADR-020) → **form đăng nhập email/mật khẩu**
  (`src/features/auth/LoginScreen.tsx`). `useAuth` thêm `signIn`/`signOut`;
  `lib/firebase.ts` thêm `signInWithEmail`.
- **Cổng vào = admin/pricing** (tầng chiến lược ADR-006). sales/production/chưa
  có vai → màn "Không có quyền truy cập" + Đăng xuất. Truyền vai THẬT xuống màn
  (bỏ hard-code 'admin').
- **Nút Đăng xuất** trên sidebar; footer hiện vai (CHỦ/TOÀN QUYỀN vs ĐỊNH GIÁ) + email.
- **Lối tắt demo** (admin/pricing/sales/production) CHỈ hiện khi `isEmulatorMode`
  — production chỉ có form. Tắt tài khoản demo ở project thật = bước vận hành.
- **Verify emulator + Playwright**: form hiện → owner tuannq6886@gmail.com đăng nhập
  → view CEO (footer email + nút Đăng xuất) → Đăng xuất về login → sales bị chặn
  → sai mật khẩu báo "Email hoặc mật khẩu không đúng". typecheck + build + suite 343/343 xanh.

## View "Giá Vốn Theo Lô" (ADR-024)
- **Câu hỏi điều hành** (user): *"Nếu 5 lô hàng có giá vật liệu khác nhau thì điều
  gì xảy ra, và giá bán thế nào là phù hợp?"* — nhấn mạnh **KHÔNG phải ERP**, và là
  **câu hỏi KHÁC** với Trợ Lý CEO → user chốt **tách một view riêng cho đỡ rối**.
- **Màn `src/features/lot-costing/LotCostingScreen.tsx`** (tab "Giá Vốn Theo Lô",
  nhóm ĐIỀU HÀNH): **CHỈ đọc** `ScenarioOutput` + `ScenarioInput` — không engine/schema
  mới, không sửa dữ liệu (nhập/sửa lô vẫn ở màn Tồn Kho). Mỗi nguyên liệu 1 thẻ:
  1. Các lô đang tồn (tấn + USD/kg) → **giá vốn bình quân** (`weightedAvgUsdPerKg`).
  2. **Giá tái tạo** + lệch so baseline + trạng thái khóa (±ngưỡng ADR-004).
  3. **Lãi/lỗ giữ kho** (`holdingGainLossVnd`); cảnh báo dự phòng VAS-02 nếu LỖ.
  4. **2 giá bán**: sổ sách (`bookCostPerKg×(1+markupVf)`) vs chính thức (`priceLadder.targetPrice`).
  5. **Khuyến nghị**: vượt ngưỡng (mở khóa) → NÊN CHỐT LẠI; trong ngưỡng → giữ giá.
- **Seed emulator (demo, không đụng parity)**: làm giàu Ống BM thành 3 lô khác giá
  (10@3.03, 8@2.80, 6@3.25) + giá tái tạo 3.40 (lệch +12,2% vượt ngưỡng) để minh hoạ.
- **Bug đã sửa**: banner đỏ "Lỗ giữ kho — dự phòng VAS-02" hiện SAI ở cả 4 thẻ dù đang
  LÃI — do trigger theo truthiness của `provisionWarning` (engine luôn trả string mô tả
  kể cả khi OK). Sửa: trigger theo **dấu** `holdingGainLossVnd < 0`.
- **Verify Playwright trên emulator**: BM Orange (mở khóa, lãi 266,5 triệu, lệch 12,2%)
  → banner vàng "NÊN CHỐT LẠI"; 3 thẻ trong ngưỡng → banner xanh "giữ nguyên". Không còn
  banner đỏ sai. typecheck + suite 343/343 xanh.
