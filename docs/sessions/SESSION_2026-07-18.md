# SESSION 2026-07-18 — Đăng nhập production + tái cấu trúc sidebar theo tình huống CEO
Pha hiện tại: 4 (vận hành thực tế) | Agent: Claude Code

## Đã quyết (nếu là quyết định kiến trúc → tạo ADR, ghi số ADR ở đây)
- **ADR-039** — Mở rules cho vai pricing sửa products[] (sửa hợp đồng product.md);
  moldAssets giữ khóa admin (tài sản vốn, 66 khuôn không unroll per-field được)
  → gán khuôn là thao tác admin, quy trình 2 nhịp (pricing tạo SKU chờ khuôn →
  admin gán). Rules test 63/63.
- **ADR-038** — Mở lại Danh Mục Sản Phẩm (nhóm Thiết Lập): tạo mới SP đủ tiêu
  chuẩn/kích thước/đơn trọng, gán KHUÔN DÙNG CHUNG (badge có khuôn/chờ khuôn),
  chặn trùng khóa; quyền khớp rules (chỉ admin lưu — mở cho pricing cần ADR
  riêng); phiếu giá chọn 3 tầng Nguyên liệu→SP→Size·tiêu chuẩn; lọc nguyên
  liệu ở dạng danh sách; Nhận Đơn nhãn size kèm tiêu chuẩn+nguyên liệu.
- **ADR-034** — Sidebar nhóm theo TÌNH HUỐNG CEO (Hằng Ngày / Khi Có Việc /
  Hoạch Định / Thiết Lập), mỗi mục kèm caption 1 dòng; gộp 3 tab giá thành hub
  "Bảng Giá" (VF | NPP | Phân tích); 3 link chéo theo mạch làm việc.
- **ADR-035** — NGUYÊN TẮC "giải thích tại chỗ": mọi số cho CEO phải tự giải
  thích gốc gác ngay trên màn, ngôn ngữ kinh doanh (UI cho CEO xem, không phải
  cho lập trình viên). ExplainPanel mọi màn + truy nguyên giá per-SKU ở Bảng
  Giá + vá đường nhập lô (Tồn Kho gắn vào Giá Vốn Theo Lô) + đường chốt what-if.
- **ADR-037** — Đơn nhiều dòng: decideOrder nhận materialId (additive, +3 test,
  suite 373/373); màn Nhận Đơn thành bảng nhiều dòng SP (kết luận từng dòng +
  cả đơn); sửa truy nguyên giá Ống (biến phí/định phí thay vì "chế biến = 0").
- **ADR-036** — Bảng Giá 2 dạng xem (danh sách | phiếu giá từng sản phẩm) +
  Nhận Đơn theo SKU cụ thể & đơn vị thương mại (mét/cái, quy đổi kg ở UI bằng
  đơn trọng danh mục, engine decideOrder giữ nguyên, gợi ý sẵn giá VF niêm yết).
- Đăng nhập: gỡ HẲN lối tắt demo theo vai (kể cả emulator); thêm Quên mật khẩu
  (email đặt lại) + Đăng nhập bằng Google (popup). Phân quyền custom claim giữ nguyên.

## Đã làm
- Xác nhận 3 suite trên emulator: engine 370/370 + rules 61/61 + functions 16/16.
- LoginScreen/useAuth/firebase.ts: bỏ signInAsRole/switchRole, thêm
  sendPasswordReset + signInWithGoogle (message lỗi tiếng Việt).
- PR #5 merge vào nhánh mặc định (`claude/project-knowledge-setup-2au4hr`).
- AppShell: NAV_GROUPS 4 nhóm + caption; id tab `pricing:vf|npp|analytics`;
  PricingHub.tsx mới bọc PriceList/DistributorPriceList/PricingAnalyticsScreen.
- Link chéo: LotCosting (banner chốt lại → Bảng Giá), Sensitivity (→ So Sánh
  Kịch Bản), OrderAcceptance (→ Giá Vốn Theo Lô) — prop `onNavigate?` optional.
- Chẩn đoán bản Render chạy nhầm chế độ emulator (thiếu 4 biến VITE_FIREBASE_*).

## Còn treo / việc phiên sau
- **User thao tác tay** (không làm được từ repo): điền 4 biến VITE_FIREBASE_* trên
  Render (Clear build cache & deploy); bật provider Google + thêm domain Render
  vào Authorized domains (Firebase Console); disable 4 user `*@demo.local`.
- Roll-out design system đen–trắng (ADR-033) cho ~13 màn còn lại — chưa đụng.
- Seed emulator (`seed:emulator`) tạo user demo giờ chỉ dùng được qua form
  email/mật khẩu (`admin@demo.local` / `demo-password`) vì nút lối tắt đã gỡ.

## Vi phạm constitution phát hiện được (nếu có)
- Không.
