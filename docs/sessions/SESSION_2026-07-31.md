# SESSION 2026-07-31 — Bảng Giá (đơn vị kg + cột BQGQ), đề xuất tái thiết kế bố cục
Pha hiện tại: 3 (vá UI) cho phần đã merge · **Pha 1 (chưa duyệt)** cho đề xuất tái thiết kế bố cục | Agent: Claude Code

## Đã quyết
- Không làm trạng thái "lô kế hoạch/đang về" (đã ghi ở phiên trước, xác nhận lại — chỉ cần đã-nhập-kho).
- Giữ nguyên `src/features/order-acceptance/` (Quyết Định Nhận Đơn) — code còn trong repo nhưng KHÔNG route được từ `AppShell.tsx` nữa (dead code từ trước). User xác nhận: không xóa, không đụng.
- **Chưa có ADR/ quyết định kiến trúc nào cho đề xuất tái thiết kế bố cục** — mới là bản thảo luận (artifact), CHƯA duyệt layout, CHƯA sang Pha 2. Xem "Còn treo" bên dưới.

## Đã làm
1. **PriceList — thêm cột BQGQ + Chênh lệch vào danh sách chính** (`src/features/price-list/PriceList.tsx`): trước đây 2 số so sánh (Giá VF bình quân gia quyền, chênh lệch so với giá đang niêm yết) chỉ hiện khi bấm mở từng dòng. Giờ đưa thẳng thành 2 cột trong bảng danh sách (`LIST_GRID_COLS`, 10 cột, bảng cuộn ngang) — tính từ `bookOutput` đã có sẵn trong component, không thêm công thức mới. **PR #32, đã merge** vào `claude/project-knowledge-setup-2au4hr`.
2. **Nhập lô nguyên liệu theo kg thay vì tấn** (UI-level): `InventoryScreen.tsx`, `DataSetupScreen.tsx`, `LotCostingScreen.tsx` — ô nhập/hiển thị đổi sang kg, lưu trữ vẫn ở field `tons` (schema đóng băng `InventoryLotSchema`, không đổi). Quy đổi ×1000/÷1000 ngay tại ô nhập/hiển thị — không đụng schema, không đụng công thức engine, không cần migrate dữ liệu Firestore cũ. **PR #33, đã merge**.
3. Test: `npm run typecheck` sạch, `npm test` **404/404 xanh** (không đổi số so với đầu phiên — 2 việc trên đều không chạm engine).

## Còn treo / việc phiên sau — QUAN TRỌNG, đọc trước khi code
**User phản hồi UI hiện tại "rối", đã thảo luận 3 vòng một đề xuất tái thiết kế bố cục (Pha 1, artifact tương tác, CHƯA duyệt).** Bản cuối cùng (vòng 3) đã lưu lại tại `prototype/layout-redesign-proposal.html` (mở trực tiếp bằng trình duyệt, tự chạy, không cần server — đúng luật skill `prototype`). Tóm tắt hướng đi (CHƯA CHỐT, cần user duyệt trước khi viết code thật):

- **3 mối quan tâm chính của user**: (1) giá vốn nguyên liệu, (2) chi phí sản xuất cố định theo ca (tách riêng khỏi giá vốn nguyên liệu — không được gộp!), (3) giá bán theo Baseline vs Bình quân gia quyền + lời lãi.
- **Vòng 1-2**: đề xuất ban đầu là thêm 2 mục sidebar mới ("Giá Vốn Nguyên Liệu", "Chi Phí Sản Xuất") — user chỉ ra lỗi: (a) demo chỉ hiện 1 SKU trong khi Bảng Giá có ~91 SKU thật; (b) không rõ 2 mục mới này có thay thế Thiết Lập Dữ Liệu hay chỉ là xem nhanh — đã sửa: xác nhận rõ 2 mục là CHỈ XEM, nhập liệu vẫn ở Thiết Lập Dữ Liệu.
- **Vòng 3 (bản mới nhất, thay đổi lớn)**: user muốn **tối giản menu item** — gộp mọi màn "chỉ xem" thành 1 danh sách, bấm vào 1 dòng thì mở **panel bên phải lồng nhau (nested), kiểu Twenty CRM** (nền mờ đi, không mất ngữ cảnh, panel con có thể mở tiếp panel cháu). Sidebar rút còn **5 mục + Trợ Giúp ghim**: Tổng Quan / Nguyên Liệu / Bảng Giá / Kịch Bản & Hoạch Định / Thiết Lập. "Giá Vốn Theo Lô" (chốt lại giá) không còn là mục riêng — nằm NGAY TRONG panel của đúng nguyên liệu đó. **Bảng Giá đã chốt đổi sang cùng kiểu danh sách + panel** (không còn kiểu bấm-mở-dòng-tại-chỗ hiện tại).
- File mockup có demo bấm-thử-được thật (vanilla JS, không React — do Artifact CSP chặn CDN): danh sách Nguyên Liệu ⇄ Bảng Giá chuyển qua lại, panel lồng 2 cấp, dữ liệu số lấy từ fixture thật + vài kịch bản giả lập có ghi chú rõ.
- **Việc phiên sau (theo đúng thứ tự)**:
  1. Trình bày lại `prototype/layout-redesign-proposal.html` cho user duyệt CHỐT layout (đủ trạng thái rỗng/có dữ liệu, xem checklist skill `prototype`).
  2. Sau khi duyệt → viết ADR cho quyết định điều hướng mới (đổi cấu trúc sidebar là thay đổi kiến trúc UI, cần ADR) → sang Pha 2 (schema/API contract nếu cần) → Pha 3 (code thật, tái dùng nguyên component/engine hiện có, KHÔNG viết lại logic).
  3. Trang Trợ Giúp (mục 4 trong mockup) — nội dung lấy từ `TermInfo.tsx` + `ExplainPanel.tsx` + `docs/GLOSSARY.md`, cần viết thành 1 trang thật, ghim cuối sidebar.
  4. `AssumptionsScreen.tsx`/`ConfigScreen` (dead code từ ADR-049) và `src/features/order-acceptance/` (dead code, user xác nhận giữ nguyên) — vẫn còn trong repo, không dọn trong đợt tái thiết kế này trừ khi user yêu cầu.

## Vi phạm constitution phát hiện được
- Không. 2 việc đã code (PR #32/#33) đều KHÔNG đụng schema/công thức đóng băng — thuần hiển thị (tái dùng `bookOutput` đã tính) hoặc quy đổi đơn vị ở tầng UI. Đề xuất tái thiết kế bố cục CHƯA viết code production nào — đúng luật "chưa có prototype được duyệt → không viết code thật", đang đúng ở Pha 1.
