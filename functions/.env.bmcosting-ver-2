# Nạp TỰ ĐỘNG bởi Cloud Functions Gen2 khi deploy/emulate với project ID
# "bmcosting-ver-2" (không áp dụng cho project demo "demo-costing-app" dùng ở
# Emulator/test — những nơi đó rơi về default "(default)" định nghĩa trong
# code, xem ADR-016). Không phải secret — an toàn commit.
FIRESTORE_DATABASE_ID=manufacture
