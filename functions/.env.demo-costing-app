# Khai báo tường minh để Firebase CLI (`firebase emulators:exec`/`emulators:start`)
# KHÔNG hỏi tương tác giá trị `FIRESTORE_DATABASE_ID` mỗi lần chạy — không hỏi
# được vì `npm run test:functions`/`test:rules` chạy non-interactive (không có
# TTY), sẽ TREO VÔ HẠN chờ input nếu thiếu file này (đã verify thật khi thêm
# tham số ở ADR-016). Project demo dùng Emulator → giữ đúng "(default)" như
# trước giờ, không đổi hành vi bộ test hiện có.
FIRESTORE_DATABASE_ID=(default)
