// M12.5 — App = AppShell (sidebar + tab theo vai, xem features/shell/).
// Trang milestone scaffold cũ (M12.2) đã hoàn thành nhiệm vụ xác nhận
// pipeline Vite+React+Tailwind — thay bằng khung app thật từ M12.5.
import AppShell from './features/shell/AppShell.js';

export default function App() {
  return <AppShell />;
}
